/**
 * client/src/components/agent/DiffViewer.tsx
 *
 * Thin wrapper — re-exports the shared simple diff viewer for BatchPanel usage.
 * Props: { oldContent: string, newContent: string }
 */

export { default } from "../diff/DiffViewer";
