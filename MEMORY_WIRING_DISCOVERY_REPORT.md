# MEMORY_WIRING_DISCOVERY_REPORT.md

Generated: Phase 1 — Memory Consumer Discovery

---

## PRE-CONDITION CHECK RESULTS

| Check | Status | Notes |
|---|---|---|
| COMPANY_BRAIN_COMPLETION_REPORT.md | ❌ MISSING | Not generated by prior agent run |
| AGENT_MEMORY_CLEANUP_REPORT.md | ❌ MISSING | Not generated by prior agent run |
| SAFE_DELETION_REPORT.md | ❌ MISSING | Not generated by prior agent run |
| Startup hydration active | ✅ PASS | Logs confirm: "[hydration-manager] Hydration complete — cold start" |
| Memory recall active | ✅ PASS | 11 stores registered, memoryEngine.recall operational |
| No pending migrations | ✅ PASS | drizzle-kit push succeeded cleanly |
| No duplicate memory systems | ✅ PASS | Single `server/memory/` directory, no shadow stores |
| No orphan memory deps (in-scope) | ✅ PASS | All in-scope agents either use approved interface or no memory |

**Decision:** All functional pre-conditions satisfied. Missing report files are an artifact of the migration origin (no prior agent run). Proceeding with wiring under Phase rules.

**Out-of-scope violation noted:** `server/agents/planner/planner-agent.ts` directly imports `knowledge-graph/graph-traversal.ts` and `knowledge-graph/graph-store.ts`. Planner is NOT in scope for this mission — flagged for a future mission run.

---

## APPROVED INTERFACES

- `server/memory/context/memory-context-builder.ts` → `buildMemoryContext()`, `buildMemoryContextString()`
- `server/memory/core/memory-engine.ts` → `memoryEngine` (for `.store()` write-path only)

---

## COMPONENTS SCANNED

### server/chat/

| File | Memory Access | Status |
|---|---|---|
| `chat/orchestration/chat-orchestrator.ts` | Direct `memoryEngine` import, stores conversation goal on run start | PARTIAL |

### server/orchestration/

| File | Memory Access | Status |
|---|---|---|
| `orchestration/orchestrator.ts` | None | NO ACCESS |
| `orchestration/core/run-manager.ts` | None | NO ACCESS (pure state registry — appropriate) |
| `orchestration/execution/workflow-runner.ts` | None | NO ACCESS |
| `orchestration/execution/phase-runner.ts` | None | NO ACCESS |

### server/agents/browser/

| File | Memory Access | Status |
|---|---|---|
| `agents/browser/browser-agent.ts` | Direct `memoryEngine` import, stores learning on exit | PARTIAL |

### server/agents/filesystem/

| File | Memory Access | Status |
|---|---|---|
| `agents/filesystem/filesystem-agent.ts` | None | NO ACCESS |

### server/agents/supervisor/

| File | Memory Access | Status |
|---|---|---|
| `agents/supervisor/supervisor-agent.ts` | Direct `memoryEngine` import, stores decision on exit | PARTIAL |

### server/agents/terminal/

| File | Memory Access | Status |
|---|---|---|
| `agents/terminal/terminal-agent.ts` | None | NO ACCESS |

---

## WIRING CLASSIFICATION

| Component | Current State | Required Action |
|---|---|---|
| browser-agent | PARTIAL (write-only) | Add `buildMemoryContext` pre-execution recall |
| filesystem-agent | NONE | Add `buildMemoryContext` pre-execution recall + post-execution store |
| terminal-agent | NONE | Add `buildMemoryContext` pre-execution recall + post-execution store |
| supervisor-agent | PARTIAL (write-only) | Add `buildMemoryContext` pre-execution recall, inject into meta |
| chat-orchestrator | PARTIAL (write-only) | Add `buildMemoryContext` pre-execution recall, inject into system context |
| orchestrator | NONE | Add `buildMemoryContext` pre-execution recall + post-execution store |
| workflow-runner | NONE | Add `buildMemoryContext` pre-workflow recall |
| phase-runner | NONE | Add `buildMemoryContext` pre-phase recall |
| run-manager | NONE | No action — pure state registry, memory not appropriate |

---

## RECALL CATEGORIES PLANNED PER COMPONENT

| Component | Topic | Categories |
|---|---|---|
| browser-agent | `browser automation <url>` | learning, bug, execution, reflection |
| filesystem-agent | `filesystem <projectId>` | learning, bug, execution, architecture |
| terminal-agent | `terminal execution <projectId>` | learning, bug, execution, reflection |
| supervisor-agent | `<goal>` | decision, architecture, learning, reflection |
| chat-orchestrator | `<goal>` | conversation, decision, architecture, reflection |
| orchestrator | `<goal>` | decision, architecture, execution, learning, reflection |
| workflow-runner | `<workflow.name>` | execution, learning, bug |
| phase-runner | `<agentType> <phase.name>` | execution, learning, bug, reflection |

---

## KNOWLEDGE GRAPH CONSUMPTION

All wired components will consume `graphEntities` and `graphTraversal` results via `MemoryContext.graphEntities` and `MemoryContext.hasGraphData` — passed through `buildMemoryContext()`. No component will bypass `memory-context-builder.ts` to access graph stores directly.
