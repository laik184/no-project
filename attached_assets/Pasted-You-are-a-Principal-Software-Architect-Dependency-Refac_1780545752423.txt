You are a Principal Software Architect, Dependency Refactoring Specialist, and Clean Architecture Enforcer.

MISSION

Fix ONLY the architecture violations listed below.

DO NOT create new features.

DO NOT change runtime behavior.

DO NOT add business logic.

DO NOT modify APIs.

DO NOT rename public contracts.

DO NOT touch unrelated files.

ONLY fix the dependency violations and import direction problems.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FIX #1
BROWSER TOOL ↔ AGENT COUPLING
(PRIORITY: CRITICAL)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Current violation:

server/tools/browser/**

imports

server/agents/browser/**

Examples:

server/tools/browser/session/browser-context.ts
→ server/agents/browser/core/browser-session.ts

server/tools/browser/session/page-manager.ts
→ server/agents/browser/core/browser-session.ts

server/tools/browser/capture/crash-detector.ts
→ server/agents/browser/telemetry/browser-logger.ts
→ server/agents/browser/telemetry/browser-metrics.ts

server/tools/browser/capture/screenshot-taker.ts
→ server/agents/browser/events/browser-events.ts
→ server/agents/browser/telemetry/action-trace.ts
→ server/agents/browser/utils/screenshot-utils.ts

server/tools/browser/interaction/*
→ server/agents/browser/events/*
→ server/agents/browser/utils/*
→ server/agents/browser/telemetry/*

Problem:

Tool layer must NEVER import Agent layer.

Current dependency:

Tools
↓
Agents

while Agents already use Tools.

This creates bidirectional coupling.

Required Fix:

Create a shared browser foundation layer:

server/shared/browser/

Move ONLY shared assets:

server/agents/browser/core/browser-session.ts

server/agents/browser/events/*

server/agents/browser/telemetry/*

server/agents/browser/types/*

server/agents/browser/utils/*

into:

server/shared/browser/

Update imports everywhere.

Final dependency:

shared/browser
↑
tools/browser
↑
agents/browser

NO tool may import anything from:

server/agents/browser/**

after refactor.

Verify with scan.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FIX #2
MEMORY → AGENT INVERSION
(PRIORITY: HIGH)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Current violation:

server/memory/bootstrap/memory-hydrator.ts

imports

server/agents/executor/index.ts

Current violation:

server/memory/bootstrap/memory-loader.ts

imports

server/agents/executor/index.ts

Problem:

Memory is foundation.

Memory must never import agents.

Required Fix:

Extract these stores/types:

executionHistory

failureMemory

learningStore

ExecutionHistoryEntry

FailurePattern

LearnedEntry

into:

server/memory/stores/

OR

server/shared/stores/

Choose whichever requires fewer changes.

Update:

server/memory/bootstrap/memory-hydrator.ts

server/memory/bootstrap/memory-loader.ts

server/agents/executor/**

to consume the extracted store module.

Final dependency:

shared-store
↑
memory
↑
agent

Memory must not import:

server/agents/**

after refactor.

Verify with scan.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FIX #3
TOOL → INFRASTRUCTURE BUS ACCESS
(PRIORITY: MEDIUM)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Current files:

server/tools/terminal/process/process-started.ts

server/tools/terminal/process/process-exited.ts

Current import:

import { bus } from '../../../infrastructure/index.ts'

Problem:

Tools should not directly access infrastructure bus.

Required Fix:

Create:

server/tools/terminal/events/terminal-events.ts

Only this file may import:

server/infrastructure/index.ts

Move all bus.emit() logic into terminal-events.ts

Update:

server/tools/terminal/process/process-started.ts

server/tools/terminal/process/process-exited.ts

to call terminal-events.ts instead.

Final dependency:

process-started.ts
↓
terminal-events.ts
↓
infrastructure

Verify imports removed.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FIX #4
AGENT → INFRASTRUCTURE BUS ACCESS
(PRIORITY: MEDIUM)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Current file:

server/agents/browser/events/browser-bus-bridge.ts

Current import:

import { bus } from '../../../infrastructure/index.ts'

Problem:

Agent directly accesses infrastructure.

Required Fix:

Create:

server/agents/browser/events/browser-event-publisher.ts

Only publisher imports infrastructure bus.

Move bus.emit() logic there.

Update:

server/agents/browser/events/browser-bus-bridge.ts

to call publisher.

Final dependency:

browser-bus-bridge.ts
↓
browser-event-publisher.ts
↓
infrastructure

Verify direct infrastructure import removed.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FIX #5
CHAT PERSISTENCE REPOSITORY EXTRACTION
(PRIORITY: LOW)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Current files:

server/chat/persistence/attachment-store.ts

server/chat/persistence/checkpoint-store.ts

server/chat/persistence/message-store.ts

server/chat/persistence/run-store.ts

server/chat/persistence/run-writer.ts

Current state:

These files import db directly.

Required Fix:

Create:

server/repositories/chat/

Move database access into repository layer.

Suggested files:

server/repositories/chat/attachment.repository.ts

server/repositories/chat/checkpoint.repository.ts

server/repositories/chat/message.repository.ts

server/repositories/chat/run.repository.ts

server/repositories/chat/index.ts

Repository owns:

db queries

db writes

db transactions

Update persistence files to consume repositories only.

Final dependency:

chat/persistence
↓
repositories/chat
↓
infrastructure/db

Verify no direct db imports remain in:

server/chat/persistence/**

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FINAL VALIDATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

After all fixes:

Generate report.

Show:

1. Updated dependency graph

2. Remaining violations

3. Browser tool imports scan

4. Memory imports scan

5. Tool infrastructure scan

6. Agent infrastructure scan

7. Chat persistence db scan

8. Architecture score

Expected result:

Tool → Agent violations = 0

Memory → Agent violations = 0

Tool → Infrastructure violations = 0

Agent → Infrastructure violations = 0

Direct DB access in chat/persistence = 0

Return exact file paths changed.

Do not perform any other refactor.

Do not add features.

Do not change runtime behavior.

Only apply the dependency fixes above.