You are a Principal Software Architect, Runtime Auditor, Dependency Graph Analyst, Multi-Agent Systems Expert, and Architecture Compliance Reviewer.

MISSION

Perform a COMPLETE X-RAY SCAN of the backend.

Do not guess.

Do not assume.

Do not summarize without evidence.

Use actual code only.

Your goal is to determine:

1. How the backend actually works.
2. How execution flows from main.ts to the final side effect.
3. Where architecture violations exist.
4. Why File Explorer agents cannot successfully write/update code.
5. Whether dependency directions are correct.
6. Whether runtime flow is correct.
7. Whether circular dependencies exist.
8. Whether recursion or infinite orchestration loops exist.
9. Whether ownership boundaries are respected.
10. Whether the current architecture matches the intended architecture.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SCAN SCOPE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Start from:

main.ts

Then recursively scan:

server/chat/

server/orchestration/

server/agents/

server/tools/

server/services/

server/repositories/

server/file-explorer/

server/infrastructure/

server/memory/

Do not stop at folder boundaries.

Trace everything until leaf nodes.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 1
RUNTIME EXECUTION GRAPH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Starting from:

main.ts

Build the ACTUAL runtime execution graph.

Trace:

Routes
Controllers
Services
Orchestrators
Coordinators
Agents
Tools
Repositories
Infrastructure

Generate:

main.ts
↓
exact file
↓
exact file
↓
exact file

Continue until:

Database
Filesystem
Terminal
Browser
LLM
Redis
SSE
WebSocket

Show exact file paths.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 2
DEPENDENCY GRAPH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Build the complete import graph.

For every file:

FILE
↓ imports
FILE
↓ imports
FILE

Generate:

1. Direct imports
2. Indirect imports
3. Dependency chains

Identify:

- Deep dependency chains
- Barrel export chains
- Hidden imports
- Cross-module imports

Show exact file paths.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 3
OWNERSHIP GRAPH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Determine actual ownership.

For every layer:

main.ts

chat

orchestration

agents

tools

services

repositories

infrastructure

memory

Identify:

OWNER

ALLOWED IMPORTS

FORBIDDEN IMPORTS

CURRENT IMPORTS

VIOLATIONS

Generate ownership matrix.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 4
FILE EXPLORER ROOT CAUSE ANALYSIS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Deep scan:

server/file-explorer/

Determine:

Why File Explorer cannot reliably create/update/write code.

Verify:

Routes
Controllers
Services
Repositories
Orchestrator
Tool integrations

Check:

1. Missing service wiring
2. Missing repository wiring
3. Missing tool registration
4. Wrong dependency direction
5. Missing orchestration path
6. Sandbox permission issues
7. Runtime execution failures
8. Tool dispatcher integration failures
9. Missing write pipeline
10. Missing event propagation

Generate root-cause report.

Use evidence only.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 5
AGENT EXECUTION ANALYSIS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Trace:

SupervisorAgent

PlannerAgent

ExecutorAgent

VerifierAgent

BrowserAgent

FilesystemAgent

TerminalAgent

CoderXAgent

ChatAgent

For each agent:

Identify:

- entry point
- coordinator
- dependencies
- tools used
- services used

Verify:

Does the agent directly import tools?

Or:

Does the agent use:

server/tools/registry/tool-dispatcher.ts

Show evidence.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 6
TOOL EXECUTION ANALYSIS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Trace:

server/tools/registry/tool-dispatcher.ts

Verify:

- tool registration
- dispatch flow
- execution flow
- return flow

Build:

Agent
↓
Dispatcher
↓
Tool
↓
Service
↓
Repository
↓
Infrastructure

Show actual implementation path.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 7
SERVICE & REPOSITORY VALIDATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Verify:

Service
↓
Repository
↓
Infrastructure

Check:

- repositories importing services
- services importing repositories
- tools importing repositories
- tools bypassing services
- services bypassing repositories

Generate violations.

Show exact files.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 8
CIRCULAR DEPENDENCY ANALYSIS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Detect:

DIRECT CYCLE

A
↓
B
↓
A

INDIRECT CYCLE

A
↓
B
↓
C
↓
D
↓
A

Generate:

Every cycle found

with:

- exact file paths
- import chain
- risk level

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 9
RUNTIME LOOP ANALYSIS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Trace:

orchestrate()

execute()

dispatch()

coordinate()

run()

startRun()

Detect:

Orchestrator
↓
Agent
↓
Tool
↓
Orchestrator

or

Pipeline
↓
Dispatcher
↓
Pipeline

or

Chat
↓
Orchestration
↓
Chat

Generate exact execution loops.

Classify:

SAFE
WARNING
CRITICAL

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 10
ARCHITECTURE RULE COMPLIANCE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Validate:

Rule 1

main.ts owns wiring only.

Rule 2

Chat may call orchestration.

Rule 3

Orchestration must not import chat.

Rule 4

Agents must not import orchestration.

Rule 5

Agents must not import chat.

Rule 6

Tools must not import agents.

Rule 7

Tools must not import orchestration.

Rule 8

Services must not import controllers.

Rule 9

Repositories must not import services.

Rule 10

Infrastructure must not import business layers.

Rule 11

File Explorer must not bypass service/repository boundaries.

Check every rule.

Show violations with evidence.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 11
IDEAL ARCHITECTURE COMPARISON
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Compare:

CURRENT ARCHITECTURE

vs

IDEAL ARCHITECTURE

Ideal:

main.ts
↓
routes
↓
controllers
↓
orchestrators
↓
agent coordinator
↓
agents
↓
tool dispatcher
↓
tools
↓
services
↓
repositories
↓
infrastructure
↓
external systems

Show:

missing layers

extra layers

wrong ownership

wrong dependency direction

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FINAL OUTPUT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Complete Runtime Graph

2. Complete Dependency Graph

3. Ownership Graph

4. File Explorer Root Cause Report

5. Agent Execution Report

6. Tool Execution Report

7. Service/Repository Audit

8. Circular Dependency Report

9. Runtime Loop Report

10. Architecture Rule Violations

11. Ideal vs Current Architecture

12. Critical Bugs

13. Recommended Fix Order

14. Final Verdict

Choose one:

✅ SAFE

⚠️ ARCHITECTURE SMELLS

🚨 HIGH RISK

💥 CRITICAL ARCHITECTURE FAILURE

Do not guess.

Use actual code only.

Show exact file paths for every conclusion.