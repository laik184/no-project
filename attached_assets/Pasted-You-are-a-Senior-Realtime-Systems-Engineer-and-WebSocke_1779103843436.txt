You are a Senior Realtime Systems Engineer and WebSocket Architecture Auditor.

Your task is to deeply scan the entire codebase and fix the issue:

C3 — WebSocket Unused / Wasted Architecture

━━━━━━━━━━━━━━━━━━━━
PROBLEM
━━━━━━━━━━━━━━━━━━━━

The backend has multiple WebSocket systems implemented:
- /ws/terminal
- /ws/execute/:id
- /ws/agent/:runId
- /ws/files/:projectId

But the frontend primarily uses SSE/EventSource.

This creates:
- duplicated realtime infrastructure
- wasted architecture
- unnecessary memory usage
- unused socket handlers
- listener leaks
- fragmented event delivery
- maintenance complexity

Some WebSocket routes may be completely unused.
Some may duplicate SSE behavior.
Some may be partially wired.

━━━━━━━━━━━━━━━━━━━━
YOUR TASK
━━━━━━━━━━━━━━━━━━━━

Perform a FULL DEEP SCAN of:

Frontend:
- client/
- hooks/
- providers/
- components/
- pages/

Backend:
- server/chat/
- server/streams/
- server/api/
- server/agents/
- server/infrastructure/

━━━━━━━━━━━━━━━━━━━━
ANALYZE
━━━━━━━━━━━━━━━━━━━━

1. Find ALL WebSocket server implementations
2. Find ALL frontend WebSocket consumers
3. Detect dead WebSocket paths
4. Detect duplicate SSE behavior
5. Detect fragmented realtime architecture
6. Detect listener leaks
7. Detect duplicated event streams
8. Detect stale socket connections
9. Detect unused handlers
10. Detect unnecessary realtime layers

━━━━━━━━━━━━━━━━━━━━
IMPORTANT
━━━━━━━━━━━━━━━━━━━━

TRACE COMPLETE FLOW:

Frontend Hook/Component
→ WebSocket Connection
→ Backend Route
→ Event Source
→ Bus/EventEmitter
→ Runtime/Agent/FileSystem

Find:
- which sockets are ACTUALLY used
- which sockets are NEVER used
- which should be SSE instead
- which should remain WebSocket
- which should be removed completely

━━━━━━━━━━━━━━━━━━━━
GOAL
━━━━━━━━━━━━━━━━━━━━

Create ONE CLEAN REALTIME ARCHITECTURE.

Requirements:
- minimal complexity
- production-grade structure
- scalable
- low coupling
- high cohesion
- no duplicate systems
- single source of truth
- clear folder structure
- maintainable architecture

━━━━━━━━━━━━━━━━━━━━
REFACTOR RULES
━━━━━━━━━━━━━━━━━━━━

Use:
- high cohesion
- low coupling
- modular architecture
- unified realtime layer
- centralized connection management
- reusable event handlers
- proper cleanup
- typed event contracts

DO NOT:
- rewrite working systems unnecessarily
- break SSE streams
- remove terminal streaming if truly needed
- create duplicate abstractions
- introduce overengineering

━━━━━━━━━━━━━━━━━━━━
EXPECTED OUTPUT
━━━━━━━━━━━━━━━━━━━━

1. Current WebSocket Architecture
2. All Active WebSocket Consumers
3. All Dead/Unused WebSocket Routes
4. SSE vs WebSocket Comparison
5. Recommended Final Architecture
6. Which routes to DELETE
7. Which routes to MERGE
8. Which routes to KEEP
9. Memory leak risks
10. Listener cleanup issues
11. Realtime architecture flaws
12. Exact files causing problems
13. Exact line references
14. Refactored folder structure
15. Production-grade implementation plan

━━━━━━━━━━━━━━━━━━━━
IMPLEMENTATION REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Generate:
- optimized architecture
- exact code changes
- exact files to modify
- exact functions to replace
- reusable socket manager design
- frontend connection manager
- backend realtime gateway
- unified event layer

IMPORTANT:
- Keep every modified file UNDER 250 LINES
- Split giant files into modules if necessary
- Use clear naming
- Use scalable structure
- Avoid circular dependencies

━━━━━━━━━━━━━━━━━━━━
FINAL GOAL
━━━━━━━━━━━━━━━━━━━━

Transform the current fragmented realtime system into:

ONE:
- unified realtime layer
- clean event architecture
- scalable streaming system
- production-grade WebSocket/SSE design

with:
- no dead code
- no wasted sockets
- no duplicated streams
- no listener leaks
- no fragmented realtime logic