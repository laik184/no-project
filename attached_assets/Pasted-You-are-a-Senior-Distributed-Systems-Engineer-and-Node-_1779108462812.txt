You are a Senior Distributed Systems Engineer and Node.js Event Architecture Auditor.

Your task is to deeply scan and refactor the global event bus system to eliminate EVENT BUS OVERLOAD RISK, LISTENER LEAKS, SCALING BOTTLENECKS, and SSE FAN-OUT PERFORMANCE ISSUES.

━━━━━━━━━━━━━━━━━━━━
MAIN PROBLEM
━━━━━━━━━━━━━━━━━━━━

Current architecture uses:
- one global singleton EventEmitter
- many SSE endpoints
- many duplicated subscriptions
- overlapping event channels
- unbounded listeners
- no partitioning
- no lifecycle cleanup guarantees

This creates:
- memory leaks
- CPU spikes
- duplicate event delivery
- scaling bottlenecks
- unstable long-running sessions
- SSE overload
- listener accumulation
- event storm amplification

━━━━━━━━━━━━━━━━━━━━
SCAN THESE AREAS FIRST
━━━━━━━━━━━━━━━━━━━━

Backend:
- server/infrastructure/events/
- server/chat/streams/
- server/chat/
- server/api/
- server/preview/
- server/agents/
- any EventEmitter usage
- any bus.subscribe usage
- any SSE subscription logic
- any req.on("close") cleanup logic
- any duplicated event stream logic

Frontend:
- all EventSource usage
- all SSE hooks
- all websocket hooks
- all duplicated stream consumers

━━━━━━━━━━━━━━━━━━━━
FIND ALL ISSUES
━━━━━━━━━━━━━━━━━━━━

Detect:
- duplicated SSE endpoints
- duplicated event emissions
- overlapping channels
- missing unsubscribe cleanup
- listener leaks
- EventEmitter abuse
- memory retention
- unbounded listeners
- firehose event streams
- redundant subscriptions
- event rebroadcast loops
- duplicate writes
- stale subscribers
- excessive fan-out
- unnecessary global broadcasts
- blocking event handlers
- event storms
- memory-heavy payloads
- giant event objects
- missing stream partitioning
- no backpressure handling
- no event batching
- missing connection lifecycle handling

━━━━━━━━━━━━━━━━━━━━
CRITICAL REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

1. REFACTOR TO MODULAR EVENT ARCHITECTURE

Replace:
- one global firehose bus

With:
- partitioned event channels
- scoped subscriptions
- typed event domains
- project-level isolation
- lifecycle-safe subscriptions

━━━━━━━━━━━━━━━━━━━━
TARGET ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━

Create clean structure:

server/infrastructure/events/
├── core/
│   ├── typed-event-bus.ts
│   ├── subscription-manager.ts
│   ├── event-cleanup.ts
│   └── backpressure.ts
│
├── channels/
│   ├── agent-events.ts
│   ├── console-events.ts
│   ├── runtime-events.ts
│   ├── preview-events.ts
│   ├── file-events.ts
│   └── lifecycle-events.ts
│
├── sse/
│   ├── sse-manager.ts
│   ├── connection-pool.ts
│   ├── stream-router.ts
│   └── heartbeat.ts
│
└── types/
    ├── event.types.ts
    └── stream.types.ts

━━━━━━━━━━━━━━━━━━━━
IMPORTANT FIXES
━━━━━━━━━━━━━━━━━━━━

Fix:
- duplicated SSE streams
- duplicated /sse/files + /sse/file
- duplicated /api/stream + /events
- duplicated console streams
- duplicated event writes
- malformed SSE formatting
- overlapping subscriptions

━━━━━━━━━━━━━━━━━━━━
SCALABILITY TARGETS
━━━━━━━━━━━━━━━━━━━━

The architecture must support:
- 1000+ concurrent SSE clients
- isolated project streams
- low memory overhead
- low CPU overhead
- automatic cleanup
- connection recovery
- backpressure handling
- batched event delivery
- non-blocking emitters

━━━━━━━━━━━━━━━━━━━━
CODE QUALITY RULES
━━━━━━━━━━━━━━━━━━━━

STRICT RULES:
- maximum 250 lines per file
- high cohesion
- low coupling
- single responsibility
- no god files
- no duplicate logic
- no giant switch statements
- no giant event maps
- no inline anonymous cleanup chaos
- no repeated EventSource handlers
- no shared mutable global state abuse

━━━━━━━━━━━━━━━━━━━━
IMPLEMENT THESE FEATURES
━━━━━━━━━━━━━━━━━━━━

1. Typed event domains
2. Scoped project subscriptions
3. Automatic listener cleanup
4. WeakMap-based connection tracking
5. Heartbeat + stale connection detection
6. Subscription lifecycle manager
7. Event batching
8. Event throttling
9. Backpressure queue
10. Stream multiplexing
11. One unified SSE gateway
12. Topic-based subscriptions
13. Safe unsubscribe wrappers
14. Event payload compression strategy
15. Listener leak detection logs

━━━━━━━━━━━━━━━━━━━━
SSE TARGET DESIGN
━━━━━━━━━━━━━━━━━━━━

Replace many endpoints with:

/api/stream?topics=
- agent
- console
- files
- runtime
- preview
- lifecycle

Example:
EventSource(
  "/api/stream?projectId=1&topics=agent,console,files"
)

━━━━━━━━━━━━━━━━━━━━
OUTPUT REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Full event-system audit
2. Current bottlenecks
3. Listener leak report
4. Duplicate stream report
5. Memory risk report
6. CPU bottleneck analysis
7. Event flow diagram
8. Refactored architecture
9. New folder structure
10. New typed event system
11. New SSE gateway architecture
12. Cleanup lifecycle system
13. Backpressure strategy
14. Scaling strategy
15. Exact files to modify
16. Exact code sections to remove
17. Migration strategy
18. Production-grade implementation plan

━━━━━━━━━━━━━━━━━━━━
IMPORTANT
━━━━━━━━━━━━━━━━━━━━

DO NOT:
- rewrite unrelated systems
- break existing runtime flow
- remove working SSE behavior
- introduce giant abstractions
- create monolithic files

ONLY:
- modularize
- optimize
- isolate
- stabilize
- scale
- clean architecture
- production hardening

Act like you are redesigning the event architecture for a billion-dollar autonomous coding platform.