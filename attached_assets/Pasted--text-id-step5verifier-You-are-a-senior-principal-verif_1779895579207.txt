```text id="step5verifier"
You are a senior principal verification systems engineer and TypeScript infrastructure architect.

Your task is to MIGRATE the existing Verifier Agent tools into the NEW centralized tool architecture.

IMPORTANT:
Before generating ANY code:

1. Deeply inspect:
- server/agents/verifier/
- server/tools/registry/
- server/agents/terminal/
- server/orchestration/
- server/infrastructure/

2. Reuse existing:
- terminal execution systems
- sandbox systems
- logging
- telemetry
- validators
- retry systems
- monitoring systems

3. DO NOT break existing imports

4. DO NOT delete old implementations yet

5. This phase is ONLY:
- extraction
- migration
- compatibility adaptation

==================================================
GOAL
==================================================

Move ALL verifier execution logic OUT of:

server/agents/verifier/

INTO:

server/tools/verifier/

WITHOUT breaking the current system.

==================================================
STRICT RULES
==================================================

1. HIGH COHESION, LOW COUPLING

2. NO FILE > 250 LINES

3. SINGLE RESPONSIBILITY ONLY

4. FAIL-CLOSED VALIDATION

5. STRICT TYPE SAFETY

6. NO CIRCULAR IMPORTS

==================================================
IMPORTANT ARCHITECTURE CHANGE
==================================================

OLD:

Verifier Agent directly implements verification tools.

NEW:

Agents
↓
Tool Registry
↓
Tool Dispatcher
↓
Verifier Tools

==================================================
CREATE THIS STRUCTURE
==================================================

server/tools/verifier/

├── build/
│   ├── run-build.ts
│   ├── build-validator.ts
│   ├── build-output-parser.ts
│   └── build-error-classifier.ts
│
├── tests/
│   ├── run-tests.ts
│   ├── test-result-parser.ts
│   ├── test-failure-classifier.ts
│   └── coverage-validator.ts
│
├── typecheck/
│   ├── run-typecheck.ts
│   ├── typescript-parser.ts
│   ├── type-error-classifier.ts
│   └── typecheck-validator.ts
│
├── runtime/
│   ├── check-server-health.ts
│   ├── endpoint-validator.ts
│   ├── runtime-log-parser.ts
│   ├── runtime-crash-detector.ts
│   └── runtime-validator.ts
│
├── recovery/
│   ├── failure-recovery.ts
│   ├── retry-decision.ts
│   ├── rollback-validator.ts
│   └── checkpoint-validator.ts
│
├── validation/
│   ├── execution-validator.ts
│   ├── output-validator.ts
│   ├── schema-validator.ts
│   ├── dependency-validator.ts
│   └── verification-validator.ts
│
├── diagnostics/
│   ├── error-analyzer.ts
│   ├── stacktrace-parser.ts
│   ├── rootcause-detector.ts
│   └── diagnostics-report.ts
│
├── monitoring/
│   ├── verification-monitor.ts
│   ├── verification-metrics.ts
│   ├── runtime-monitor.ts
│   └── health-monitor.ts
│
├── shared/
│   ├── verifier-context.ts
│   ├── verifier-errors.ts
│   ├── verifier-result.ts
│   └── verifier-types.ts
│
├── registry/
│   └── register-verifier-tools.ts
│
└── index.ts

==================================================
MIGRATION REQUIREMENTS
==================================================

Migrate ALL existing verifier tools from:

server/agents/verifier/

INCLUDING:

- runVerification
- runBuild
- runTests
- runTypecheck
- checkServerHealth
- failureRecovery.handle

and ALL related verifier operations.

==================================================
IMPORTANT TOOL RULES
==================================================

Every tool MUST:

1. Register itself with:
- tool-registry.ts

2. Export:
- metadata
- handler
- validation schema

3. Use:
- ToolExecutionContext
- ToolExecutionResult

4. Return normalized outputs

==================================================
VERIFICATION REQUIREMENTS
==================================================

MANDATORY:

1. ALL verification runs inside sandbox

2. Normalize ALL:
- compiler errors
- runtime errors
- test failures
- stack traces

3. Detect:
- build failures
- TypeScript failures
- runtime crashes
- failing endpoints
- missing dependencies

4. Fail-closed verification behavior

==================================================
COMPATIBILITY REQUIREMENTS
==================================================

CRITICAL:

DO NOT break existing agents.

Create temporary compatibility adapters.

Example:

OLD:
server/agents/verifier/runBuild()

NEW:
toolDispatcher.execute("run_build")

BUT:
old APIs must still work during migration.

==================================================
CREATE ADAPTER LAYER
==================================================

Inside:

server/agents/verifier/

Create lightweight wrappers ONLY.

Wrappers should:
- call tool dispatcher
- preserve old signatures
- preserve old return types

NO real verification logic remains inside agents.

==================================================
IMPORTANT REFACTOR GOAL
==================================================

AFTER migration:

Verifier Agent becomes:
- orchestration layer
- verification coordination layer
- compatibility layer

NOT:
- verification implementation layer

==================================================
TOOL REGISTRATION
==================================================

Each tool MUST register:

- tool name
- category
- permissions
- timeout
- retry policy

Example categories:
- verifier.build
- verifier.tests
- verifier.typecheck
- verifier.runtime
- verifier.validation

==================================================
TELEMETRY
==================================================

Track:
- verification count
- build failures
- test failures
- typecheck failures
- runtime crashes
- recovery attempts

==================================================
EXPECTED OUTPUT
==================================================

Generate:

1. Full migrated verifier tool architecture
2. Fully typed TypeScript
3. Registry integration
4. Dispatcher integration
5. Compatibility wrappers
6. Centralized validation
7. Sandbox-safe verification
8. Production-grade verifier tools

==================================================
FINAL RULES
==================================================

1. DO NOT delete old APIs yet

2. DO NOT break executor

3. DO NOT break orchestration

4. Migrate safely in phases

5. Preserve backwards compatibility

Everything must be:
- modular
- deterministic
- secure
- maintainable
- production-ready
- Replit-level
```
