You are now operating in PRODUCTION-GRADE TYPESCRIPT VERIFICATION HARDENING MODE.

Your mission is to redesign and implement a REAL deterministic TypeScript verification system using:

tsc --noEmit

The goal is to eliminate fake TypeScript success states and push hallucination resistance toward Replit-level reliability.

CRITICAL ENGINEERING RULES:

- Every file MUST stay under 250 LOC
- High cohesion
- Low coupling
- No God files
- No mixed responsibilities
- No hidden shared mutable state
- Prefer composition over inheritance
- Use dependency injection everywhere appropriate
- Explicit interfaces only
- Immutable data flow preferred
- Deterministic execution only
- Fail-closed architecture
- No implicit global state
- No runtime guessing
- Runtime evidence > LLM assumptions

==================================================
PRIMARY OBJECTIVE
==================================================

Completely replace the current fake TypeScript validation architecture based on:
- log scanning
- regex matching
- console heuristics
- inferred build success

WITH:

REAL compiler-grounded verification using:

tsc --noEmit

The system must NEVER claim:
"TypeScript valid"
unless the actual compiler exits with code 0.

==================================================
SECTION 1 — ROOT CAUSE ANALYSIS
==================================================

First analyze why the current system fails.

Explain:
- why log scanning is unreliable
- why hot reload hides errors
- why console heuristics hallucinate success
- why partial rebuilds miss failures
- why async rebuild timing creates race conditions
- why dev server logs are not source-of-truth
- how fake success propagates through the agent loop

Then map:
Current Flow
vs
Target Deterministic Flow

==================================================
SECTION 2 — NEW ARCHITECTURE
==================================================

Design a modular verification architecture.

STRICT REQUIREMENTS:
- Every file under 250 LOC
- Single responsibility per module
- No shared mutable singleton state
- Pure interfaces where possible

Design these modules:

1. TypeScriptVerificationOrchestrator
2. TSCProcessRunner
3. TSConfigResolver
4. VerificationResultParser
5. RuntimeEvidenceStore
6. VerificationCache
7. FailureClassifier
8. RetryPolicyEngine
9. VerificationAuditLogger
10. VerificationStateMachine

For EACH module provide:

- exact responsibility
- inputs
- outputs
- lifecycle
- failure modes
- injected dependencies
- anti-coupling strategy
- cohesion guarantees

==================================================
SECTION 3 — REAL TSC EXECUTION
==================================================

Implement deterministic compiler execution.

Requirements:

- Use actual subprocess execution:
    tsc --noEmit

- Never trust:
    - logs
    - vite output
    - webpack output
    - dev server messages
    - HMR state

- Always verify using:
    - exit code
    - stderr
    - parsed diagnostics

Implement:

1. isolated subprocess execution
2. execution timeout protection
3. cancellation support
4. retry-safe execution
5. deterministic stdout/stderr capture
6. concurrent verification isolation
7. workspace-scoped execution

==================================================
SECTION 4 — FAIL-CLOSED DESIGN
==================================================

CRITICAL RULE:

Verification exhaustion MUST NEVER produce success.

Implement:

PASS:
- only when exit code == 0

FAIL:
- timeout
- diagnostics found
- process crash
- incomplete execution
- corrupted output
- cancellation
- parser failure

NO:
ok:true with warnings

NO:
soft success

NO:
assumed success

==================================================
SECTION 5 — DIAGNOSTIC PARSING
==================================================

Build deterministic diagnostic parsing.

Parse:
- file path
- line number
- column
- error code
- severity
- message
- category

Output typed immutable diagnostics.

NO raw string handling outside parser layer.

==================================================
SECTION 6 — IMPORT GRAPH VALIDATION
==================================================

Add static import validation.

Requirements:

Detect:
- broken imports
- missing exports
- circular dependencies
- invalid path aliases
- orphan modules
- duplicate exports

Use:
- AST parsing
- dependency graph walking

NOT regex.

==================================================
SECTION 7 — RUNTIME EVIDENCE SYSTEM
==================================================

Build RuntimeEvidenceStore.

Store:
- verification timestamp
- compiler version
- tsconfig hash
- file checksum
- diagnostics
- exit codes
- duration
- workspace path

Rules:
- immutable records
- append-only audit trail
- replayable verification history

==================================================
SECTION 8 — VERIFICATION STATE MACHINE
==================================================

Implement explicit states.

States:

IDLE
→ STARTING
→ RUNNING
→ PARSING
→ PASSED
→ FAILED
→ TIMEOUT
→ CANCELLED
→ CORRUPTED

Transitions MUST be deterministic.

No hidden transitions.

No implicit state mutation.

==================================================
SECTION 9 — RETRY ENGINE
==================================================

Retries must be intelligent.

Rules:
- retries only for transient infrastructure failures
- NEVER retry actual TS errors automatically
- classify failures before retrying

Retryable:
- subprocess spawn failure
- temporary filesystem lock
- ENOMEM
- transient IO

NOT retryable:
- TS2304
- TS2322
- syntax errors
- missing imports
- type mismatch

==================================================
SECTION 10 — CACHING SYSTEM
==================================================

Implement safe verification cache.

Requirements:

Cache key:
- tsconfig hash
- workspace checksum
- dependency checksum
- file timestamps

Cache invalidation:
- deterministic only
- no stale cache reuse

Never cache FAILED state indefinitely.

==================================================
SECTION 11 — CONCURRENCY SAFETY
==================================================

Prevent race conditions.

Requirements:
- isolated verification sessions
- no shared mutable process state
- deterministic locking
- cancellation tokens
- atomic result publishing

Handle:
- multiple agents
- simultaneous writes
- overlapping verifications

==================================================
SECTION 12 — OBSERVABILITY
==================================================

Build full auditability.

Log:
- subprocess lifecycle
- diagnostics
- retries
- timings
- cancellations
- failures
- parser output
- cache hits/misses

Logs must be:
- structured
- typed
- replayable
- deterministic

==================================================
SECTION 13 — FILE STRUCTURE
==================================================

Design exact folder structure.

Constraints:
- every file under 250 LOC
- no utility dumping ground
- no massive managers
- no circular dependencies
- feature-oriented structure

Show:
- folders
- interfaces
- contracts
- boundaries

==================================================
SECTION 14 — ANTI-HALLUCINATION GUARANTEES
==================================================

Explain EXACTLY how this architecture eliminates:

- fake TS success
- hidden compile failures
- stale diagnostics
- partial rebuild lies
- inferred success
- HMR desync
- log hallucination
- verification races
- retry loops

==================================================
SECTION 15 — PRODUCTION HARDENING
==================================================

Add:
- timeout budgets
- memory limits
- subprocess sandboxing
- watchdogs
- deadlock prevention
- orphan process cleanup
- resource isolation

==================================================
FINAL OUTPUT REQUIREMENTS
==================================================

Provide:

1. Full architecture
2. State diagrams
3. Module boundaries
4. Interfaces
5. Execution lifecycle
6. Failure lifecycle
7. Retry lifecycle
8. Cache lifecycle
9. Audit flow
10. Import graph validation flow
11. Concurrency model
12. Folder structure
13. Dependency graph
14. Example TypeScript interfaces
15. Production hardening strategy
16. Anti-hallucination guarantees

IMPORTANT:
- Think like a compiler engineer
- Think like a distributed systems architect
- Think like a reliability engineer
- Think like Replit/Cursor infrastructure team

NO MARKETING LANGUAGE.
NO GENERIC ADVICE.
NO GIANT FILES.
NO GOD OBJECTS.
NO MAGIC STATE.

Design this as a deterministic production-grade TypeScript verification subsystem.