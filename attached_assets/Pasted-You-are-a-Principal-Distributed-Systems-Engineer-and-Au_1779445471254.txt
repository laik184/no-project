You are a Principal Distributed Systems Engineer and Autonomous Runtime Architect.

Your task is to CREATE and INTEGRATE a production-grade:

DISTRIBUTED FILE SCANNER

for the NURA-X autonomous agent backend.

━━━━━━━━━━━━━━━━━━━━
MAIN GOAL
━━━━━━━━━━━━━━━━━━━━

Implement a Quantum-Inspired Distributed File Scanner capable of:

✅ parallel file scanning
✅ parallel bug detection
✅ parallel dependency analysis
✅ parallel import tracing
✅ realtime progress events
✅ conflict-safe execution
✅ deterministic aggregation
✅ fail-closed execution

The scanner must support:
- huge repositories
- multi-module projects
- large frontend/backend systems
- realtime orchestration integration

━━━━━━━━━━━━━━━━━━━━
MANDATORY ENGINEERING RULES
━━━━━━━━━━━━━━━━━━━━

1. HIGH COHESION / LOW COUPLING
- each module has ONE responsibility
- no hidden cross-domain mutation
- no tight runtime dependencies

2. FILE SIZE LIMIT
- NO file > 250 LOC

3. TARGETED PLACEMENT
- every file must live in correct bounded context

4. ROOT CAUSE ENGINEERING
- no symptom-level fixes
- permanent architecture-safe implementation only

5. SENIOR ENGINEERING QUALITY
- readability
- scalability
- strong typing
- defensive programming
- security-aware
- zero hacks

6. CODE REVIEW STANDARD
- architecture-safe interfaces
- typed contracts
- deterministic behavior

7. FAIL-CLOSED DESIGN
- execution blocked if:
  - scanner crashes
  - aggregation fails
  - file lock fails
  - verification fails

8. TELEMETRY REQUIRED
Log:
✅ scan.started
✅ scan.partitioned
✅ worker.started
✅ worker.completed
✅ worker.failed
✅ scan.completed
✅ aggregation.completed
✅ conflict.detected

━━━━━━━━━━━━━━━━━━━━
TARGET ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━

Create NEW bounded context:

server/quantum/scanner/

Required structure:

server/quantum/scanner/
├── distributed-file-scanner.ts
├── file-partitioner.ts
├── scan-worker.ts
├── scan-aggregator.ts
├── dependency-scanner.ts
├── import-scanner.ts
├── bug-pattern-scanner.ts
├── telemetry/
│   └── scan-telemetry.ts
├── locks/
│   └── scan-lock-manager.ts
├── types/
│   ├── scan.types.ts
│   └── worker.types.ts
└── utils/
    ├── file-batch.ts
    └── scan-filter.ts

━━━━━━━━━━━━━━━━━━━━
SYSTEM RESPONSIBILITIES
━━━━━━━━━━━━━━━━━━━━

# 1. DISTRIBUTED FILE SCANNER

Main orchestrator.

Responsibilities:
✅ initialize scan
✅ partition files
✅ spawn workers
✅ coordinate execution
✅ aggregate results
✅ emit telemetry
✅ fail-safe handling

Must support:
- async execution
- cancellation
- timeout protection
- retry policy

━━━━━━━━━━━━━━━━━━━━
# 2. FILE PARTITIONER

Responsibilities:
✅ split file graph into partitions
✅ balance workload
✅ avoid oversized worker batches
✅ classify by:
  - frontend
  - backend
  - infra
  - agents
  - tests

Rules:
- deterministic partitioning
- max files per worker configurable
- configurable partition strategy

━━━━━━━━━━━━━━━━━━━━
# 3. SCAN WORKER

Responsibilities:
✅ isolated worker execution
✅ scan assigned file batch
✅ detect:
  - imports
  - dependencies
  - suspicious patterns
  - dead code
  - circular references
  - runtime risks
  - orchestration risks

Rules:
- no direct shared state mutation
- readonly file analysis
- timeout-safe
- retry-safe

━━━━━━━━━━━━━━━━━━━━
# 4. IMPORT SCANNER

Detect:
✅ import graph
✅ dynamic imports
✅ circular imports
✅ dead imports
✅ invalid paths
✅ cross-domain violations

━━━━━━━━━━━━━━━━━━━━
# 5. DEPENDENCY SCANNER

Detect:
✅ module dependencies
✅ ownership violations
✅ architecture leaks
✅ runtime dependency risks
✅ invalid orchestration dependencies

━━━━━━━━━━━━━━━━━━━━
# 6. BUG PATTERN SCANNER

Detect:
✅ sequential bottlenecks
✅ unsafe Promise.all
✅ missing await
✅ race conditions
✅ stale runtime state
✅ unsafe singleton usage
✅ memory leaks
✅ infinite retry loops
✅ orphaned event listeners
✅ unhandled errors

━━━━━━━━━━━━━━━━━━━━
# 7. SCAN AGGREGATOR

Responsibilities:
✅ merge worker results
✅ deduplicate findings
✅ severity ranking
✅ deterministic ordering
✅ evidence collection

Rules:
- no race conditions
- stable output ordering
- confidence scoring

━━━━━━━━━━━━━━━━━━━━
# 8. SCAN LOCK MANAGER

Prevent:
❌ duplicate scans
❌ concurrent mutation
❌ overlapping execution

Rules:
- per-project scan lock
- timeout auto-release
- deterministic ownership

━━━━━━━━━━━━━━━━━━━━
EVENT BUS INTEGRATION
━━━━━━━━━━━━━━━━━━━━

Integrate with:

server/infrastructure/events/bus.ts

Emit:
- quantum.scan.started
- quantum.scan.partitioned
- quantum.worker.started
- quantum.worker.completed
- quantum.worker.failed
- quantum.scan.completed
- quantum.scan.failed

━━━━━━━━━━━━━━━━━━━━
ORCHESTRATION INTEGRATION
━━━━━━━━━━━━━━━━━━━━

Integrate with:

server/orchestration/
server/engine/graph/
server/agents/

Scanner must support:
✅ orchestration-triggered scans
✅ DAG-triggered scans
✅ verification-triggered scans
✅ recovery-triggered scans

━━━━━━━━━━━━━━━━━━━━
PARALLEL EXECUTION RULES
━━━━━━━━━━━━━━━━━━━━

SAFE TO PARALLELIZE:
✅ readonly file analysis
✅ import scanning
✅ dependency scanning
✅ bug pattern analysis

NOT SAFE:
❌ file mutation
❌ runtime mutation
❌ memory writes

━━━━━━━━━━━━━━━━━━━━
WORKER EXECUTION MODEL
━━━━━━━━━━━━━━━━━━━━

Execution flow:

DistributedFileScanner
    ↓
FilePartitioner
    ↓
N Worker Batches
    ↓
Promise.allSettled()
    ↓
ScanAggregator
    ↓
Final Scan Report

Rules:
- configurable MAX_PARALLEL_WORKERS
- timeout per worker
- partial failure recovery
- deterministic merge ordering

━━━━━━━━━━━━━━━━━━━━
SCAN OUTPUT
━━━━━━━━━━━━━━━━━━━━

Each worker returns:

{
  imports: [],
  dependencies: [],
  circularRefs: [],
  deadCode: [],
  runtimeRisks: [],
  orchestrationRisks: [],
  bugPatterns: [],
  telemetry: {},
  confidenceScore: number
}

━━━━━━━━━━━━━━━━━━━━
REQUIRED DETECTIONS
━━━━━━━━━━━━━━━━━━━━

Detect:
✅ circular dependencies
✅ orchestration violations
✅ domain boundary leaks
✅ runtime ownership conflicts
✅ unsafe event subscriptions
✅ duplicate singleton ownership
✅ dead orchestrators
✅ sequential execution bottlenecks
✅ missing fail-closed checks
✅ preview desync risks
✅ unsafe recovery loops
✅ memory corruption risks

━━━━━━━━━━━━━━━━━━━━
CONFIG REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Create:

server/quantum/scanner/config/

Configurable:
- max workers
- batch size
- timeout
- retry count
- excluded folders
- scan depth
- severity thresholds

━━━━━━━━━━━━━━━━━━━━
FAIL-CLOSED REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

If:
- aggregation fails
- worker crashes
- invalid partitions
- lock failure

THEN:
❌ scan MUST fail
❌ orchestration notified
❌ telemetry emitted
❌ recovery hook triggered

━━━━━━━━━━━━━━━━━━━━
TEST REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Create tests for:
✅ partitioning
✅ aggregation
✅ worker isolation
✅ timeout handling
✅ retry logic
✅ circular dependency detection
✅ telemetry emission
✅ lock manager
✅ deterministic ordering

━━━━━━━━━━━━━━━━━━━━
REQUIRED FINAL REPORT
━━━━━━━━━━━━━━━━━━━━

Create:

DISTRIBUTED_FILE_SCANNER_IMPLEMENTATION_REPORT.md

Include:

1. Architecture Overview
2. File Tree
3. Worker Lifecycle
4. Parallel Execution Model
5. Event Flow
6. Aggregation Strategy
7. Lock Strategy
8. Timeout Strategy
9. Retry Strategy
10. Deterministic Merge Strategy
11. Circular Dependency Detection
12. Runtime Risk Detection
13. Orchestration Risk Detection
14. Telemetry Integration
15. Fail-Closed Validation
16. Performance Expectations
17. Scalability Analysis
18. Replit-Level Similarity %
19. Quantum-Inspired Readiness %
20. Remaining Weaknesses
21. Recommended Next Integrations

━━━━━━━━━━━━━━━━━━━━
FINAL GOAL
━━━━━━━━━━━━━━━━━━━━

Transform the backend from:

"single-threaded sequential repository analysis"

INTO:

"parallel distributed autonomous repository intelligence system"

using:
✅ safe parallelism
✅ bounded contexts
✅ deterministic orchestration
✅ production-grade telemetry
✅ fail-closed architecture