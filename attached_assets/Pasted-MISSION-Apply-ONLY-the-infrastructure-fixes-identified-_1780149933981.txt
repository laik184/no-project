MISSION

Apply ONLY the infrastructure fixes identified in:

INFRASTRUCTURE_ARCHITECTURE_REPORT.md

Do NOT modify any files outside:

server/infrastructure/

and the explicitly identified violation consumers.

Do NOT perform refactors unrelated to this mission.

Do NOT introduce new architecture.

Do NOT modify agents.

Do NOT modify memory.

Do NOT modify orchestration.

Do NOT modify chat.

---

AUTHORIZED CHANGES ONLY

1. Create:
   server/infrastructure/index.ts

2. Fix:
   stream-topics usage

3. Fix:
   runtime.service.ts → processRegistry

4. Fix:
   crud.service.ts → safe-fs only

Nothing else.

---

PHASE 1 — CREATE PUBLIC INFRASTRUCTURE ENTRY POINT

Create:

server/infrastructure/index.ts

Re-export ONLY approved public APIs:

db/index.ts

events/bus.ts

events/file-change-emitter.ts

events/sse/sse-manager.ts

process/process-registry.ts

realtime/stream-topics.ts

runtime/runtime-manager.ts

sandbox/sandbox.util.ts

checkpoints/safe-fs.util.ts

Do NOT export:

runtime/runtime-types.ts

checkpoints/atomic-write.util.ts

These remain internal.

---

PHASE 2 — STREAM TOPICS DISCIPLINE FIX

Scan:

All consumers of:

events/bus.ts

events/sse/sse-manager.ts

Find raw topic strings:

agent.event

run.lifecycle

checkpoint

Replace with:

TOPIC.AGENT

TOPIC.LIFECYCLE

TOPIC.CHECKPOINT

Import from:

server/infrastructure/realtime/stream-topics.ts

Do NOT change event behavior.

Do NOT rename topics.

Only replace raw strings with constants.

---

PHASE 3 — PROCESS REGISTRY FACADE FIX

Inspect:

server/preview/runtime/runtime.service.ts

Current violation:

Direct runtimeManager import.

Replace read-only access with:

server/infrastructure/process/process-registry.ts

Rules:

Read operations
→ processRegistry

Write operations
(start/stop/restart)
→ keep runtimeManager if required

Do NOT break runtime control.

Do NOT remove required runtimeManager mutations.

Goal:

Use facade wherever possible.

---

PHASE 4 — SAFE FILESYSTEM DISCIPLINE FIX

Inspect:

server/file-explorer/crud/crud.service.ts

Current violation:

Direct import:

checkpoints/atomic-write.util.ts

Remove direct dependency.

Use ONLY:

server/infrastructure/checkpoints/safe-fs.util.ts

Ensure:

No functionality loss

No backup loss

No duplicate backup creation

No behavior change

---

PHASE 5 — VALIDATION

Verify:

No circular dependencies

No broken imports

No TypeScript errors

No build failures

No runtime failures

No API changes

No event behavior changes

No infrastructure ownership violations

---

PHASE 6 — FINAL REPORT

Generate:

INFRASTRUCTURE_DISCIPLINE_FIX_REPORT.md

Include:

1. Files Modified

2. Files Created

3. Public API Surface

4. Stream Topic Replacements

5. Process Registry Fixes

6. Safe-FS Fixes

7. Validation Results

8. Remaining Infrastructure Risks

9. Infrastructure Score Before

10. Infrastructure Score After

---

SUCCESS CRITERIA

server/infrastructure/index.ts exists.

All approved infrastructure APIs exposed.

Raw event topic strings replaced by TOPIC constants.

runtime.service.ts uses processRegistry for reads.

crud.service.ts no longer imports atomic-write.util.ts directly.

No unrelated code changes.

No architectural expansion.

No modifications outside approved scope.