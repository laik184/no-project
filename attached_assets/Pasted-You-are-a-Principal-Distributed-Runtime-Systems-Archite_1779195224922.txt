You are a Principal Distributed Runtime Systems Architect.

Your mission is to upgrade NURA X Runtime Synchronization Architecture from ~75% reliability to TRUE Replit-level (99%) runtime consistency.

FOCUS AREAS:
- server/infrastructure/runtime/
- server/preview/
- server/infrastructure/events/
- server/infrastructure/proxy/
- client/src/pages/preview/
- client/src/hooks/
- process-registry.ts
- runtime-manager.ts
- preview-orchestrator.ts
- tunnel.service.ts
- preview-proxy.ts

━━━━━━━━━━━━━━━━━━━━
MAIN GOAL
━━━━━━━━━━━━━━━━━━━━

Eliminate ALL runtime desynchronization risks.

Make runtime lifecycle:
- deterministic
- event-driven
- observable
- recoverable
- fully synchronized
- production-grade

Target:
Realtime Replit/Cursor/Lovable-level runtime experience.

━━━━━━━━━━━━━━━━━━━━
CURRENT VERIFIED PROBLEMS
━━━━━━━━━━━━━━━━━━━━

1. Port Routing Desync
- processRegistry assigns random port
- tunnelService defaults to 5000
- risk of preview mismatch

2. Runtime State Fragmentation
- runtime state spread across:
  - processRegistry
  - runtimeManager
  - tunnelService
  - previewOrchestrator
  - frontend lifecycle state

3. Ghost Console Pipeline
- captureService.attach() never called
- structured runtime observation bypassed

4. Missing Runtime Events
- debug.lifecycle not forwarded to SSE
- tool.execution not forwarded

5. Silent Crash Detection Lag
- 15s health monitor delay

6. Multiple Runtime Truth Sources
- frontend state
- runtime manager
- process registry
- tunnel state
- iframe state

Potential inconsistency risk exists.

━━━━━━━━━━━━━━━━━━━━
YOUR TASK
━━━━━━━━━━━━━━━━━━━━

Deep scan the ENTIRE runtime lifecycle.

Trace COMPLETE flow:

AI Agent
→ Tool Execution
→ Process Spawn
→ Runtime Registration
→ Port Detection
→ Preview Proxy
→ SSE Broadcast
→ Frontend Preview State
→ iframe Reload
→ Runtime Health
→ Crash Recovery
→ Reconnect
→ Self-Healing

Find EVERY desync possibility.

━━━━━━━━━━━━━━━━━━━━
BUILD TRUE REPLIT-LEVEL RUNTIME SYNC
━━━━━━━━━━━━━━━━━━━━

Implement a SINGLE SOURCE OF TRUTH architecture.

Create:

server/infrastructure/runtime/
├── runtime-store/
│   ├── runtime-store.ts
│   ├── runtime-events.ts
│   ├── runtime-state-machine.ts
│   ├── runtime-sync.ts
│   ├── runtime-health.ts
│   ├── runtime-recovery.ts
│   └── runtime-types.ts

This becomes the ONLY authority for:
- ports
- process state
- runtime lifecycle
- preview URL
- restart state
- reconnect state
- crash state
- verification state

━━━━━━━━━━━━━━━━━━━━
REQUIRED RUNTIME STATES
━━━━━━━━━━━━━━━━━━━━

Implement deterministic state machine:

IDLE
→ BUILDING
→ INSTALLING
→ STARTING
→ VERIFYING
→ READY
→ UPDATING
→ RESTARTING
→ RECONNECTING
→ CRASHED
→ RECOVERING
→ FAILED

INVALID transitions MUST be rejected.

━━━━━━━━━━━━━━━━━━━━
REQUIRED FEATURES
━━━━━━━━━━━━━━━━━━━━

1. CENTRALIZED PORT AUTHORITY
- ONE source of port truth
- no hardcoded defaults
- no duplicate port maps
- no stale ports

2. REALTIME RUNTIME SYNCHRONIZATION
- backend ↔ frontend lockstep
- iframe lifecycle sync
- preview state sync
- runtime state replay on reconnect

3. PROCESS LIFECYCLE TRACKING
Track:
- pid
- port
- start time
- memory
- cpu
- uptime
- crash reason
- restart count
- logs
- health status

4. EVENT-DRIVEN ARCHITECTURE
ALL changes emit:
- runtime.lifecycle
- runtime.health
- runtime.metrics
- runtime.logs
- runtime.sync
- runtime.crashed
- runtime.recovered

5. RUNTIME REPLAY SYSTEM
On frontend reconnect:
- replay latest runtime state
- replay latest lifecycle
- replay preview status
- replay crash info

6. SMART HEALTH MONITORING
Replace 15s polling with:
- heartbeat
- stdout activity
- websocket health
- proxy health
- memory thresholds
- startup timeout
- crash prediction

7. SELF-HEALING RUNTIME
When runtime crashes:
- detect instantly
- preserve logs
- preserve last state
- preserve iframe state
- trigger AI recovery
- restart safely
- verify runtime healthy
- restore preview automatically

8. PREVIEW SYNCHRONIZATION
Preview must NEVER:
- point to stale port
- show stale iframe
- remain loading forever
- mismatch runtime state

9. RUNTIME CONSISTENCY GUARDS
Add:
- stale runtime detector
- duplicate process detector
- orphan process cleanup
- zombie runtime cleanup
- invalid transition guard
- runtime lock protection

━━━━━━━━━━━━━━━━━━━━
FRONTEND REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

client/src/pages/preview/

Implement:
- runtime state machine UI
- realtime lifecycle overlay
- reconnect UI
- runtime health indicator
- memory/cpu indicator
- crash overlay
- self-healing animation
- startup progress
- verification progress
- iframe sync validation

Frontend MUST reflect REAL backend state ONLY.

NO fake loading states.

━━━━━━━━━━━━━━━━━━━━
IMPORTANT ENGINEERING RULES
━━━━━━━━━━━━━━━━━━━━

✅ HIGH COHESION
✅ LOW COUPLING
✅ EVENT-DRIVEN
✅ SINGLE SOURCE OF TRUTH
✅ DETERMINISTIC STATE MACHINE
✅ REPLAYABLE STATE
✅ RECOVERABLE RUNTIME
✅ MODULAR FILES
✅ FILES UNDER 250 LINES
✅ PRODUCTION-GRADE CODE

❌ NO duplicated runtime maps
❌ NO hardcoded ports
❌ NO fragmented state
❌ NO timer hacks
❌ NO fake states
❌ NO direct iframe mutations
❌ NO stale runtime cache

━━━━━━━━━━━━━━━━━━━━
VERIFY THESE SCENARIOS
━━━━━━━━━━━━━━━━━━━━

1. npm install running
→ realtime installing state

2. vite restart
→ iframe reconnect smoothly

3. runtime crash
→ instant crash detection
→ self-healing
→ preview restored

4. browser refresh
→ runtime state replayed correctly

5. multiple rapid file writes
→ runtime stable
→ no duplicate restarts

6. websocket disconnect
→ reconnect + replay works

7. port reassignment
→ preview updates automatically

━━━━━━━━━━━━━━━━━━━━
OUTPUT FORMAT
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Runtime Desync Root Causes
2. Full Runtime Lifecycle Diagram
3. Current vs Replit Architecture
4. Runtime Fragmentation Report
5. Port Synchronization Problems
6. Runtime State Problems
7. Process Lifecycle Problems
8. SSE Synchronization Problems
9. Frontend Sync Problems
10. iframe Lifecycle Problems
11. Recovery System Problems
12. Exact Runtime Breakpoints
13. Runtime State Machine Design
14. New Folder Structure
15. Runtime Store Architecture
16. Event Flow Architecture
17. Health Monitoring Architecture
18. Recovery Architecture
19. Frontend Synchronization Architecture
20. Step-by-Step Refactor Plan
21. Production-Grade Final Runtime Architecture

Then IMPLEMENT the fixes safely.

IMPORTANT:
Do NOT rewrite unrelated systems.
Preserve existing functionality.
Upgrade runtime consistency to true production-grade reliability.