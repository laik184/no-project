You are a Senior Security Engineer and Autonomous Runtime Auditor.

Your task is to deeply scan and harden the shell_exec tool system.

━━━━━━━━━━━━━━━━━━━━
MAIN TARGET
━━━━━━━━━━━━━━━━━━━━

Problem ID: T6
Issue: shell_exec unsafe
Risk Level: CRITICAL

Current issue:
The shell_exec tool allows dangerous command execution patterns that could lead to:
- Remote Code Execution (RCE)
- Sandbox escape
- Arbitrary script execution
- Data exfiltration
- Prompt injection abuse
- Malicious dependency execution

The current allowlist is NOT enough.

━━━━━━━━━━━━━━━━━━━━
SCAN TARGETS
━━━━━━━━━━━━━━━━━━━━

Deep scan these folders first:

- server/agents/tools/
- server/agents/core/
- server/infrastructure/
- server/chat/
- server/api/
- server/runtime/

Especially scan:
- shell-tools.ts
- tool orchestrator
- process spawning
- child_process usage
- exec/spawn wrappers
- npm install execution
- package manager execution
- runtime execution pipeline

━━━━━━━━━━━━━━━━━━━━
FIND ALL SECURITY RISKS
━━━━━━━━━━━━━━━━━━━━

Detect:

1. shell injection risks
2. unsafe spawn/exec usage
3. unsafe argument passing
4. pipe injection
5. command chaining
6. dangerous redirects
7. URL-based attacks
8. malicious npm installs
9. arbitrary script execution
10. environment variable leakage
11. cwd escape risks
12. process privilege risks
13. unsafe PATH usage
14. unvalidated shell args
15. node/python/bash execution abuse
16. curl/wget abuse
17. child_process.exec usage
18. shell:true usage
19. prompt injection attack paths
20. unrestricted package installs

━━━━━━━━━━━━━━━━━━━━
IMPORTANT RULES
━━━━━━━━━━━━━━━━━━━━

DO NOT:
- rewrite the whole architecture
- break working functionality
- add over-engineered enterprise abstractions
- exceed 250 lines per file
- create deeply nested folders
- add unnecessary dependencies

ONLY:
- make targeted safe fixes
- keep code simple
- keep structure clean
- keep files modular
- keep logic production-safe
- use minimal changes
- preserve current architecture

━━━━━━━━━━━━━━━━━━━━
SECURITY HARDENING GOALS
━━━━━━━━━━━━━━━━━━━━

Implement:

✅ strict command validation
✅ strict argument validation
✅ deny dangerous patterns
✅ deny pipes/redirections
✅ deny shell metacharacters
✅ URL allowlist validation
✅ safe spawn wrappers
✅ execution timeout protection
✅ process cleanup
✅ sandbox path validation
✅ env filtering
✅ package install restrictions
✅ audit logging
✅ execution tracing

━━━━━━━━━━━━━━━━━━━━
IMPORTANT IMPLEMENTATION RULES
━━━━━━━━━━━━━━━━━━━━

1. Prefer spawn() over exec()
2. NEVER use shell:true unless absolutely required
3. Validate command BEFORE execution
4. Validate EACH argument
5. Reject dangerous symbols:
   - |
   - &&
   - ;
   - >
   - >>
   - <
   - `
   - $()
6. Restrict npm scripts
7. Restrict package managers
8. Restrict network access where possible
9. Block dangerous domains
10. Add centralized validation utility

━━━━━━━━━━━━━━━━━━━━
ARCHITECTURE REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Create a CLEAN structure like:

server/
  security/
    command-validator.ts
    safe-spawn.ts
    execution-policy.ts

Keep:
- each file under 250 lines
- clean naming
- reusable validation
- modular functions

━━━━━━━━━━━━━━━━━━━━
OUTPUT REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Full vulnerability report
2. Exact vulnerable files
3. Exact vulnerable lines
4. Attack examples
5. Root cause analysis
6. Hardened architecture
7. Safe execution flow
8. Refactor plan
9. Minimal safe patches
10. Final secure implementation

━━━━━━━━━━━━━━━━━━━━
IMPORTANT
━━━━━━━━━━━━━━━━━━━━

Use evidence-based analysis only.

Mention:
- exact file paths
- exact vulnerable code
- exact fixes

Keep the implementation:
- simple
- modular
- secure
- production-grade
- maintainable

Do not generate fake security.
Do not overcomplicate the system.