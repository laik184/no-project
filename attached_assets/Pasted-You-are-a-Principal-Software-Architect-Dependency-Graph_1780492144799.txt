You are a Principal Software Architect, Dependency Graph Auditor, Import Governance Specialist, and Infrastructure Ownership Reviewer.

MISSION

Perform a COMPLETE DEEP SCAN of the entire codebase and generate a factual Infrastructure Import Compliance Report.

Do NOT guess.

Do NOT assume.

Use actual code only.

Use actual imports only.

Show file-level evidence for every conclusion.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SCAN TARGET
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

server/

Focus:

server/infrastructure/index.ts

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 1
BUILD INFRASTRUCTURE EXPORT MAP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Open:

server/infrastructure/index.ts

Generate a complete export inventory.

Example:

db
bus
sseManager
runtimeManager
processRegistry
TOPIC
safeWriteFile
safeDeleteFile
getProjectDir
captureGitSha

Show:

Export Name
↓
Source File

Generate a full export table.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 2
FIND ALL CONSUMERS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Scan the entire server/ directory.

Find every file that imports:

server/infrastructure/index.ts

OR

server/infrastructure

Generate:

File Path
Import Statement
Imported Symbols

Example:

server/chat/realtime/event-publisher.ts

import { bus } from '../../infrastructure';

Uses:

- bus

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 3
DIRECT SUB-PATH VIOLATION SCAN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Search for ALL imports that bypass the public entry point.

Examples:

server/infrastructure/events/bus.ts

server/infrastructure/runtime/runtime-manager.ts

server/infrastructure/db/index.ts

server/infrastructure/events/sse/sse-manager.ts

server/infrastructure/realtime/stream-topics.ts

server/infrastructure/checkpoints/*

server/infrastructure/sandbox/*

Generate:

File Path
Actual Import Path
Imported Symbol

Example:

server/orchestration/events/event-publisher.ts

import { bus } from '../../infrastructure/events/bus.ts'

Violation:
YES

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 4
CLASSIFY IMPORTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Generate 3 sections.

SECTION A

✅ COMPLIANT

Files importing via:

server/infrastructure/index.ts

SECTION B

⚠️ VIOLATION

Files importing internal infrastructure sub-paths directly.

Show:

File
Import
Correct Import

SECTION C

🔴 MISSING IMPORT

Files that appear to use infrastructure-owned resources but are NOT importing from infrastructure.

Examples:

db

bus

sseManager

runtimeManager

TOPIC

safeWriteFile

emitFileChange

For each file provide:

File Path
Evidence
Required Import

Do NOT guess.

Only report if actual code proves it.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 5
BROKEN IMPORT DETECTION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Search for imports that resolve to non-existent locations.

Examples:

import { db } from "../../db"

Verify whether the target exists.

Generate:

File Path
Import Statement
Resolved Path
Exists?
Risk

Classify:

SAFE
WARNING
CRITICAL

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 6
UNUSED INFRASTRUCTURE EXPORTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

For every export in:

server/infrastructure/index.ts

Check whether it is consumed anywhere.

Generate:

Export
Used By Count
Consumer Files

Example:

runtimeManager
0 consumers

processRegistry
1 consumer

If zero consumers:

Mark as:

UNUSED

Do NOT recommend deletion automatically.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 7
ARCHITECTURE VALIDATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Verify these rules:

Rule 1

Infrastructure must be imported through:

server/infrastructure/index.ts

Rule 2

No direct imports from:

server/infrastructure/events/*

server/infrastructure/runtime/*

server/infrastructure/db/*

server/infrastructure/checkpoints/*

server/infrastructure/sandbox/*

Rule 3

Infrastructure must never import:

chat

orchestration

agents

tools

services

repositories

Rule 4

Infrastructure is a dependency.

Nothing below infrastructure may be imported by infrastructure.

Check actual code.

Show evidence.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FINAL REPORT FORMAT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Infrastructure Export Inventory

2. Infrastructure Consumer List

3. Direct Sub-Path Violations

4. Missing Infrastructure Imports

5. Broken Import Report

6. Unused Infrastructure Exports

7. Architecture Rule Validation

8. Compliance Score

9. Final Verdict

One of:

✅ FULLY COMPLIANT

⚠️ MINOR VIOLATIONS

🚨 HIGH RISK

💥 CRITICAL ARCHITECTURE FAILURE

Use actual code only.

Show exact file paths for every conclusion.