You are a Principal Runtime Infrastructure Engineer.

TASK:
Implement a production-grade `waitForPort()` runtime synchronization system for the autonomous runtime architecture.

MAIN GOAL:
Eliminate runtime race conditions, preview blank states, premature verification, and unstable startup timing by introducing deterministic port readiness verification.

━━━━━━━━━━━━━━━━━━━━
PROBLEM ROOT CAUSE
━━━━━━━━━━━━━━━━━━━━

Current runtime startup flow relies on:
❌ health-check polling
❌ optimistic runtime assumptions
❌ non-deterministic startup timing

This causes:
- preview blank screens
- iframe load races
- premature verification
- false runtime.ready states
- unstable startup synchronization
- flaky runtime lifecycle transitions

Current architecture is PARTIAL Replit-level.

Goal:
Upgrade runtime startup lifecycle to deterministic Replit-grade synchronization.

━━━━━━━━━━━━━━━━━━━━
TARGET ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━

OLD FLOW ❌

server_start
→ spawn()
→ optimistic polling
→ maybe ready
→ preview loads
→ race condition possible

━━━━━━━━━━━━━━━━━━━━

NEW FLOW ✅

server_start
→ spawn()
→ waitForPort(port)
→ runtime verification
→ emit runtime.ready
→ preview transition
→ iframe load
→ verification pipeline
→ completion authority

━━━━━━━━━━━━━━━━━━━━
SCAN REQUIRED
━━━━━━━━━━━━━━━━━━━━

Deep scan ALL runtime startup related systems:

server/infrastructure/runtime/
server/infrastructure/process/
server/preview/
server/fail-closed/
server/runtime/
server/orchestration/
server/tools/categories/
server/infrastructure/events/
client/src/pages/preview/
client/src/realtime/

Trace:
- startup lifecycle
- process ownership
- port lifecycle
- preview lifecycle
- verification lifecycle
- event lifecycle
- SSE synchronization

━━━━━━━━━━━━━━━━━━━━
IMPLEMENT waitForPort()
━━━━━━━━━━━━━━━━━━━━

Create a production-grade utility:

Suggested location:
server/infrastructure/runtime/wait-for-port/

Required files:
- wait-for-port.ts
- wait-for-port.types.ts
- wait-for-port.errors.ts
- index.ts

━━━━━━━━━━━━━━━━━━━━
FUNCTION REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

waitForPort() MUST:

✅ wait until port actually accepts TCP connections
✅ support timeout
✅ support retries
✅ support abort signal
✅ support cancellation
✅ support configurable polling interval
✅ support telemetry logging
✅ support lifecycle events
✅ support deterministic verification
✅ support structured errors

━━━━━━━━━━━━━━━━━━━━
REQUIRED API
━━━━━━━━━━━━━━━━━━━━

Example:

```ts
await waitForPort({
  host: "127.0.0.1",
  port,
  timeoutMs: 30000,
  retryIntervalMs: 250,
  signal,
  projectId,
  runId
});

━━━━━━━━━━━━━━━━━━━━ IMPLEMENTATION RULES ━━━━━━━━━━━━━━━━━━━━

Use: ✅ native net.Socket ✅ Promise-based polling ✅ timeout cleanup ✅ socket cleanup ✅ abort controller support

DO NOT use: ❌ random delays ❌ setTimeout hacks ❌ optimistic assumptions ❌ external dependencies

━━━━━━━━━━━━━━━━━━━━ RUNTIME INTEGRATION ━━━━━━━━━━━━━━━━━━━━

Integrate into:

server/infrastructure/runtime/runtime-manager.ts

Current flow: spawn() → polling

NEW FLOW: spawn() → waitForPort() → StartupVerifier → runtime.ready

━━━━━━━━━━━━━━━━━━━━ PREVIEW LIFECYCLE INTEGRATION ━━━━━━━━━━━━━━━━━━━━

Preview MUST NOT transition to:

ready

UNTIL:

waitForPort() success

Integrate with:

PreviewLifecycleManager

PreviewLifecycleBridge

RuntimeStore


━━━━━━━━━━━━━━━━━━━━ EVENT BUS INTEGRATION ━━━━━━━━━━━━━━━━━━━━

Emit structured events:

runtime.port.waiting runtime.port.ready runtime.port.timeout runtime.port.failed

Include:

projectId

runId

port

duration

retryCount


━━━━━━━━━━━━━━━━━━━━ SSE INTEGRATION ━━━━━━━━━━━━━━━━━━━━

Frontend MUST receive:

waiting state

retry state

timeout state

ready state


Realtime sync required.

━━━━━━━━━━━━━━━━━━━━ VERIFICATION PIPELINE INTEGRATION ━━━━━━━━━━━━━━━━━━━━

Verification MUST NOT start UNTIL:

waitForPort() complete

Integrate with:

StartupVerifier

VerificationCoordinator

CompletionAuthority


━━━━━━━━━━━━━━━━━━━━ FAIL-SAFE DESIGN ━━━━━━━━━━━━━━━━━━━━

If:

waitForPort() timeout

THEN: ❌ runtime.ready MUST NOT emit ❌ preview MUST NOT load ❌ verification MUST NOT continue ❌ completion MUST fail-closed

Instead: ✅ runtime.failed ✅ recovery trigger ✅ telemetry log ✅ structured error propagation

━━━━━━━━━━━━━━━━━━━━ TELEMETRY REQUIREMENTS ━━━━━━━━━━━━━━━━━━━━

Log ALL events:

wait started

retries

timeout

success

cancellation

duration

retry count


Integrate with: server/telemetry/

━━━━━━━━━━━━━━━━━━━━ CODE QUALITY RULES ━━━━━━━━━━━━━━━━━━━━

STRICTLY FOLLOW:

1. High Cohesion, Low Coupling



waitForPort handles ONLY port readiness.

No preview logic inside utility.

No orchestration logic inside runtime utility.


2. File Size Limit



NO file above 250 lines.

Split logic cleanly.


3. Correct Placement



Runtime utilities ONLY inside runtime/.

No misplaced infra logic.


4. Root Cause Fix



Fix startup race condition itself.

NOT symptom masking.


5. Production-Grade Standards



Strong typing

Clear naming

Structured errors

Safe cleanup

Security-first design


6. Mandatory Review Readiness



Readable architecture

Self-documenting flow

Minimal side effects


7. Fail-Closed Enforcement



No false ready states.

No optimistic startup success.


8. Full Telemetry



Every retry/event logged.


━━━━━━━━━━━━━━━━━━━━ SECURITY REQUIREMENTS ━━━━━━━━━━━━━━━━━━━━

✅ socket cleanup mandatory ✅ timeout cleanup mandatory ✅ no dangling handles ✅ no memory leaks ✅ no infinite retry loops ✅ abort-safe design

━━━━━━━━━━━━━━━━━━━━ VALIDATION REQUIRED ━━━━━━━━━━━━━━━━━━━━

After implementation:

Verify: ✅ no preview blank state ✅ no race condition ✅ no premature verification ✅ deterministic runtime.ready ✅ frontend synchronized ✅ SSE updates working ✅ recovery works on timeout ✅ runtime cleanup safe

━━━━━━━━━━━━━━━━━━━━ GENERATE FINAL REPORT ━━━━━━━━━━━━━━━━━━━━

Create:

WAIT_FOR_PORT_IMPLEMENTATION_REPORT.md

━━━━━━━━━━━━━━━━━━━━ REPORT MUST INCLUDE ━━━━━━━━━━━━━━━━━━━━

1. Root Cause Analysis


2. Old Runtime Flow


3. New Runtime Flow


4. waitForPort Architecture


5. Files Created


6. Files Modified


7. Runtime Integration Points


8. Preview Integration Points


9. SSE Integration Points


10. Verification Integration Points


11. EventBus Integration


12. Telemetry Integration


13. Fail-Safe Verification


14. Cleanup Verification


15. Race Condition Resolution


16. Startup Determinism Verification


17. Timeout Handling Verification


18. Abort Handling Verification


19. Memory Leak Verification


20. Runtime Stability Impact


21. Replit Similarity Improvement %


22. Production Readiness Improvement %


23. Remaining Runtime Weaknesses



━━━━━━━━━━━━━━━━━━━━ SUCCESS CRITERIA ━━━━━━━━━━━━━━━━━━━━

System MUST behave like:

spawn
→ deterministic wait
→ verified ready
→ synchronized preview
→ verified runtime
→ completion

NOT:

spawn
→ hope
→ maybe ready
→ preview race

━━━━━━━━━━━━━━━━━━━━ FINAL GOAL ━━━━━━━━━━━━━━━━━━━━

Upgrade runtime startup lifecycle from: “partial optimistic startup”

TO:

“deterministic Replit-grade runtime synchronization system”