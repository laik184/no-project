DEEP SCAN — INFRASTRUCTURE CONSUMER DISCOVERY

TARGET:

server/**

PRIMARY TARGET:

server/infrastructure/index.ts

GOAL:

AI khud poore backend ko scan kare aur identify kare ki kaunsi files ko:

server/infrastructure/index.ts

import karna chahiye.

Koi assumptions nahi.
Koi hardcoded list nahi.
Sirf actual code usage ke basis par report banao.

==================================================
PHASE 1 — GLOBAL SCAN

Scan:

server/**/*.ts

For every file collect:

- Current imports
- Current exports
- Used symbols
- Runtime dependencies
- Infrastructure usage

==================================================
PHASE 2 — INFRASTRUCTURE SYMBOL DETECTION

Find usage of:

db
bus
sseManager
TOPIC
runtimeManager
processRegistry
emitFileChange
safeWriteFile
safeDeleteFile
safeBackup
captureGitSha
getProjectDir
getNuraDir
seedDefaultProject

For every usage report:

File:
Symbol:
Current Import:
Correct Import:

==================================================
PHASE 3 — IMPORT OWNERSHIP ANALYSIS

Determine:

Which files SHOULD import:

server/infrastructure/index.ts

Which files SHOULD NOT.

Rules:

Import infrastructure ONLY if file actually uses infrastructure exports.

Do NOT add infrastructure imports to:

- Types
- Schemas
- Validators
- Contracts
- Mappers
- Pure utilities
- Pure business logic

==================================================
PHASE 4 — SUB-PATH VIOLATION DETECTION

Detect all imports like:

../../infrastructure/db/*
../../infrastructure/events/*
../../infrastructure/runtime/*
../../infrastructure/process/*
../../infrastructure/realtime/*
../../infrastructure/checkpoints/*

Report them.

Replace with:

import { ... } from '../../infrastructure/index.ts'

ONLY if symbol is exported by infrastructure/index.ts.

==================================================
PHASE 5 — MISSING IMPORT DETECTION

Find files that:

- Use db but don't import infrastructure
- Use bus but don't import infrastructure
- Use sseManager but don't import infrastructure
- Use filesystem helpers but don't import infrastructure

Report as:

MISSING IMPORT

File:
Used Symbol:
Required Import:

==================================================
PHASE 6 — AUTO FIX

After scan:

1. Add missing infrastructure imports.
2. Remove unused infrastructure imports.
3. Replace direct infrastructure sub-path imports.
4. Preserve runtime behavior.
5. Do not modify business logic.

==================================================
FINAL REPORT

Generate:

INFRASTRUCTURE_CONSUMER_REPORT.md

Include:

1. Every file importing infrastructure/index.ts
2. Every file that should import infrastructure/index.ts
3. Every direct sub-path violation
4. Every missing import
5. Every removed import
6. Every added import
7. Infrastructure export usage frequency
8. Final dependency graph

Expected architecture:

Higher Layers
↓
server/infrastructure/index.ts
↓
Infrastructure internals

No file should bypass infrastructure/index.ts.
No file should import infrastructure unless it actually uses infrastructure exports.