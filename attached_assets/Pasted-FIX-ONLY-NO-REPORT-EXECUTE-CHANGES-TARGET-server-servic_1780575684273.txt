FIX ONLY — NO REPORT — EXECUTE CHANGES

TARGET:

server/services/chat/**
server/services/filesystem/**

GOAL:

Remove all Layered Architecture violations found in the audit.

Do not redesign the system.
Do not rename APIs.
Do not change runtime behavior.
Do not add new features.

Only fix dependency direction and import ownership.

==================================================
FIX GROUP 1 — SERVICE → ORCHESTRATION
==================================================

FILE:

server/services/chat/chat.service.ts

CURRENT PROBLEM:

Imports:

../../orchestration/index.ts

Service layer must not import orchestration.

RULE:

Chat
  ↓
Orchestration
  ↓
Services

NOT:

Service
  ↓
Orchestration

ACTION:

1. Locate every import from:

server/orchestration/**

inside:

server/services/chat/chat.service.ts

2. Remove orchestration imports.

3. Move orchestration execution responsibility upward.

4. Chat layer should invoke orchestration.

5. Service should only expose business operations.

6. Keep public API unchanged.

==================================================
FIX GROUP 2 — SERVICE → CHAT
==================================================

FILES:

server/services/chat/chat.service.ts

server/services/chat/clarification.service.ts

server/services/chat/checkpoint.service.ts

CURRENT PROBLEM:

Services import:

server/chat/orchestration/conversation-manager.ts

server/chat/messages/message-builder.ts

server/chat/questions/question-manager.ts

server/chat/persistence/checkpoint-store.ts

RULE:

Service layer must not import Chat layer.

ACTION:

1. Scan all imports inside:

server/services/chat/**

2. Detect imports targeting:

server/chat/**

3. Remove all Service → Chat imports.

4. Move shared logic into proper locations.

5. Service may consume:

repositories
infrastructure
shared
types

6. Chat layer may consume services.

7. Service layer must never consume chat layer.

==================================================
FIX GROUP 3 — FILESYSTEM SERVICE → HTTP CIRCULAR DEPENDENCY
==================================================

FILES:

server/services/filesystem/tree/tree.service.ts

server/services/filesystem/upload/upload.service.ts

CURRENT PROBLEM:

Imports:

../../../file-explorer/mappers/index.ts

Functions:

buildTreeFromDir()

toUploadedFile()

RULE:

Service layer must not import HTTP layer.

ACTION:

1. Remove imports from:

server/file-explorer/**

2. Find:

buildTreeFromDir()

toUploadedFile()

3. Move these mapper helpers into:

server/shared/filesystem/

OR

server/services/filesystem/shared/

Choose the smallest change.

4. Update imports.

TARGET:

tree.service.ts
      ↓
shared helper

upload.service.ts
      ↓
shared helper

NO imports from file-explorer.

==================================================
FIX GROUP 4 — HTTP → REPOSITORY BYPASS
==================================================

FILE:

server/file-explorer/mappers/tree.mapper.ts

CURRENT PROBLEM:

Imports:

repositories/file-system/index.ts

RULE:

HTTP layer must not access repositories.

ACTION:

1. Remove repository imports.

2. Route through:

server/services/filesystem/tree/tree.service.ts

or equivalent tree service.

TARGET FLOW:

file-explorer
      ↓
tree service
      ↓
repository

==================================================
FIX GROUP 5 — VALIDATION
==================================================

After fixes:

Verify:

NO Service imports Chat.

NO Service imports Orchestration.

NO Filesystem Service imports file-explorer.

NO Mapper imports Repository.

==================================================
EXPECTED FLOW
==================================================

HTTP
   ↓
Chat
   ↓
Orchestration
   ↓
Agents
   ↓
Tools
   ↓
Services
   ↓
Repositories
   ↓
Infrastructure

==================================================
CONSTRAINTS
==================================================

Do not modify behavior.

Do not change route URLs.

Do not change request/response contracts.

Do not create feature changes.

Do not generate a report.

Apply fixes directly and ensure build passes.