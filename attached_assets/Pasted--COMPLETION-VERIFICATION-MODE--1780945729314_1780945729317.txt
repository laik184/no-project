━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COMPLETION VERIFICATION MODE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Do NOT assume implementation is complete.

After generating code:

Perform a FULL PREVIEW MODULE AUDIT.

Scan every file.

Verify:

✓ File exists

✓ Imports resolve

✓ Exports resolve

✓ index.ts exports correct symbols

✓ No missing implementations

✓ No TODOs

✓ No stubs

✓ No placeholder functions

✓ No mock implementations

✓ No unimplemented interfaces

✓ No dead code

✓ No orphaned files

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PREVIEW FEATURE COVERAGE AUDIT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Verify backend fully supports frontend preview requirements.

Compare implementation against preview requirements:

✓ Lifecycle States

✓ SSE Streaming

✓ Runtime Health

✓ Reload Events

✓ Crash Detection

✓ Reconnect Logic

✓ Runtime Monitoring

✓ Preview Sessions

✓ DevTools Capture

✓ Self-Healing States

✓ Hot Reload Events

✓ Health Polling

✓ Preview State API

✓ Runtime Health API

✓ Preview Stream API

If any feature is partially implemented:

Automatically complete it.

If any feature is missing:

Automatically implement it.

Do not stop until feature coverage is 100%.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SELF REPAIR MODE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

If missing code is found:

1. Generate missing files

2. Generate missing interfaces

3. Generate missing services

4. Generate missing repositories

5. Generate missing persistence adapters

6. Generate missing exports

7. Generate missing route registration

8. Generate missing lifecycle transitions

9. Generate missing SSE topics

10. Generate missing event emitters

Automatically wire everything together.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FRONTEND COMPATIBILITY AUDIT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Verify backend supports frontend preview flow.

Required support:

Lifecycle Overlay

Preview Status Pill

Runtime Health Widget

Auto Refresh

Crash Overlay

Reconnect State

DevTools Panel

Preview Session

Iframe Reload Events

Runtime State Updates

SSE Events

Health Polling

If frontend expects data that backend does not provide:

Automatically implement backend support.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FINAL REPORT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Output:

1. Files Created

2. Files Updated

3. Missing Features Found

4. Missing Features Implemented

5. Missing APIs Implemented

6. Missing Events Implemented

7. Missing SSE Topics Implemented

8. Missing Lifecycle States Implemented

9. Import/Export Validation

10. Architecture Validation

11. Frontend Compatibility Score

12. Backend Completion Score

13. Remaining Missing Items

14. Production Readiness Score

If completion score is below 100%:

Continue implementation automatically.

Do not stop until Preview Module is fully implemented and verified.