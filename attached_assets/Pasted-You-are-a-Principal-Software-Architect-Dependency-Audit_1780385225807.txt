You are a Principal Software Architect, Dependency Auditor, Runtime Inspector, Import Graph Analyzer, and Clean Architecture Enforcer.

MISSION

Perform a FORENSIC DEEP SCAN of:

server/tools/filesystem/

server/file-explorer/services/

server/file-explorer/repositories/

server/infrastructure/

I do NOT want assumptions.

I do NOT want architecture suggestions.

I want ACTUAL CODE EVIDENCE.

Your only goal is to verify whether the system is truly following:

Tool
↓
Service
↓
Repository
↓
Infrastructure

or not.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ARCHITECTURE RULES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Rule 1

Every filesystem tool must import a Service.

Tool
↓
Service

NOT:

Tool
↓
lib/
↓
node:fs

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Rule 2

Every Service must import a Repository.

Service
↓
Repository

NOT:

Service
↓
node:fs

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Rule 3

Every Repository must import Infrastructure only.

Repository
↓
Infrastructure

NOT:

Repository
↓
Service

NOT:

Repository
↓
Tool

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Rule 4

No tool may access:

node:fs
node:fs/promises
child_process
database
repositories

directly.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Rule 5

No service may access:

node:fs
node:fs/promises

directly.

Only repositories may perform persistence.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 1
SCAN ALL FILESYSTEM TOOLS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Scan:

server/tools/filesystem/

including all nested folders.

For every tool generate:

Tool Name

File Path

Imports

Exports

Runtime Entry Point

Dependency Chain

Example:

Tool:
fs_read_file

Imports:
readService

Dependency Chain:

fs_read_file
↓
readService
↓
filesystemRepository
↓
node:fs

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 2
VERIFY CRITICAL TOOLS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Specifically inspect:

fs_find_imports

fs_find_exports

fs_find_symbol_usages

fs_scan_folder

fs_scan_extension

Verify:

Do they actually import:

dependencyAnalysisService

scannerService

or not?

Show exact import statement.

Show exact file path.

Show line numbers.

Classification:

WIRED

PARTIAL

BROKEN

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 3
SCAN SERVICES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Scan:

server/file-explorer/services/

For every service generate:

Service Name

Imports

Exports

Repositories Used

Dependency Chain

Example:

readService
↓
filesystemRepository
↓
Infrastructure

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 4
SERVICE → REPOSITORY VERIFICATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Verify:

readService

writeService

createService

deleteService

renameService

duplicateService

searchService

treeService

metadataService

historyService

clipboardService

gitStatusService

insightsService

downloadService

uploadService

openEditorsService

pinnedService

recentService

dependencyAnalysisService

scannerService

For each service show:

Imports Repository?

YES / NO

Repository Name

File Path

Evidence

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 5
REPOSITORY AUDIT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Scan:

server/file-explorer/repositories/

Verify:

Which infrastructure layer is imported.

Show:

Repository
↓
Infrastructure

Example:

filesystemRepository
↓
node:fs/promises

metadataRepository
↓
LRU Cache

historyRepository
↓
JSON Storage

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 6
DIRECT ACCESS VIOLATIONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Search entire codebase for:

node:fs

node:fs/promises

fs

fs/promises

child_process

execSync

execFile

inside:

server/tools/filesystem/

server/file-explorer/services/

Generate report:

VALID

WARNING

VIOLATION

Show exact file path.

Show exact line.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 7
IMPORT GRAPH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Build complete graph:

Tool
↓
Service
↓
Repository
↓
Infrastructure

for every filesystem operation.

Generate actual runtime graph.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 8
BROKEN WIRING DETECTION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Find:

Tools with no service

Services with no repository

Repositories with wrong dependencies

Unused services

Unused repositories

Direct filesystem access

Generate:

MATCHED

MISSING

PARTIAL

BROKEN

DEAD

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 9
FINAL SCORE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Output:

Total Tools

Total Services

Total Repositories

MATCHED

PARTIAL

MISSING

BROKEN

DEAD

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FINAL VERDICT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Choose exactly one:

✅ CLEAN ARCHITECTURE

⚠️ PARTIAL IMPLEMENTATION

🚨 BROKEN WIRING

❌ ARCHITECTURE VIOLATION

Use actual code evidence only.

Show exact import chains.

Show exact file paths.

Show exact line numbers.

Do not guess.

Do not summarize.

Verify everything.