You are a Senior Full-Stack Refactor Engineer.

TASK:
Deep scan the entire frontend + backend architecture and FIX the following critical issue:

━━━━━━━━━━━━━━━━━━━━
BUG ID: F2
PROBLEM:
No auto-save system in editor

RESULT:
- file conflicts
- lost changes
- stale editor state
- user edits overwritten by AI
- AI-generated files desync from editor
- dirtyFiles state becomes unreliable
━━━━━━━━━━━━━━━━━━━━

MAIN GOAL:
Implement a SAFE, production-grade AUTO-SAVE architecture.

IMPORTANT REQUIREMENTS:
- use HIGH cohesion
- use LOW coupling
- maintain CLEAN folder structure
- DO NOT create giant files
- EVERY file must stay UNDER 250 lines
- split logic into reusable modules
- avoid duplicated logic
- preserve existing architecture
- avoid breaking current SSE system
- avoid breaking Monaco editor integration

━━━━━━━━━━━━━━━━━━━━
DEEP SCAN REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

First deeply analyze:

Frontend:
- Monaco editor integration
- dirtyFiles state
- file explorer
- current save APIs
- editor state synchronization
- file refresh logic
- agent-generated file flow
- local state handling
- debouncing behavior
- SSE file change handling

Backend:
- file save routes
- sandbox writes
- file-change event bus
- SSE file streams
- concurrent writes
- file locking risks
- overwrite handling

Find:
- all file write paths
- all editor update paths
- all dirty state paths
- all places where conflicts happen
- all missing synchronization points

━━━━━━━━━━━━━━━━━━━━
FIX REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Implement:

1. AUTO-SAVE ENGINE
- debounce-based autosave
- configurable delay
- safe retry mechanism
- pending-save tracking
- save queue

2. CONFLICT PREVENTION
- prevent overwriting newer files
- detect external changes
- detect AI-generated updates
- detect stale editor state

3. FILE VERSION TRACKING
- lightweight version/timestamp tracking
- compare before write
- reject stale writes safely

4. REALTIME SYNCHRONIZATION
- editor updates after AI writes
- auto refresh after external changes
- SSE-driven synchronization

5. SAVE STATUS SYSTEM
- saving...
- saved
- failed
- conflict detected

6. SAFE WRITE FLOW
USER EDIT
→ debounce
→ version check
→ safe save
→ emit file.change
→ synchronize editor
→ update dirty state

━━━━━━━━━━━━━━━━━━━━
IMPORTANT ARCHITECTURE RULES
━━━━━━━━━━━━━━━━━━━━

DO:
✅ modular architecture
✅ reusable hooks
✅ reusable services
✅ reusable utilities
✅ typed APIs
✅ isolated state management
✅ clear separation of concerns
✅ event-driven sync
✅ minimal re-renders
✅ proper cleanup
✅ proper debounce cancellation

DO NOT:
❌ giant components
❌ business logic inside UI
❌ direct fs writes from frontend
❌ duplicated save logic
❌ circular dependencies
❌ blocking UI saves
❌ unsafe overwrite behavior
❌ memory leaks
❌ race conditions

━━━━━━━━━━━━━━━━━━━━
EXPECTED FILE STRUCTURE
━━━━━━━━━━━━━━━━━━━━

Create/refactor using clean architecture like:

client/src/features/editor/
  hooks/
  services/
  state/
  utils/
  types/

server/files/
  services/
  events/
  routes/
  utils/

━━━━━━━━━━━━━━━━━━━━
OUTPUT REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Root-cause analysis
2. All affected files
3. Conflict flow analysis
4. Refactor plan
5. New architecture diagram
6. Exact implementation steps
7. Optimized file structure
8. Production-grade autosave system
9. Conflict-resolution strategy
10. Performance considerations
11. Race-condition prevention
12. Final code patches

IMPORTANT:
Mention:
- exact files
- exact functions
- exact state flows
- exact synchronization issues
- exact save conflict causes

Do a REAL deep scan before implementing anything.