You are a Principal Software Architect, Dependency Mapper, Runtime Analyst, and Codebase Refactoring Auditor.

MISSION

Perform a COMPLETE DEEP SCAN and MATCHING ANALYSIS between:

1. server/tools/filesystem/
2. server/file-explorer/

I do NOT want assumptions.

I do NOT want theoretical architecture.

I want ACTUAL CODE EVIDENCE.

Read every file.

Trace imports.

Trace exports.

Trace runtime usage.

Build a complete Tool → Service → Repository mapping.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SCAN TARGETS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

server/tools/filesystem/

server/file-explorer/

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 1
SCAN ALL FILESYSTEM TOOLS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Scan every file inside:

server/tools/filesystem/

Including:

clone/
delete/
edit/
folders/
move/
read/
registry/
search/
shared/
structure/
validation/
write/

and all nested files.

For every tool identify:

Tool Name
File Path
Purpose
Exports
Imports
Dependencies
Runtime Entry Point

Generate:

TOOL INVENTORY TABLE

Example:

Tool Name:
read-file

Path:
server/tools/filesystem/read/read-file.tool.ts

Purpose:
Read file content

Imports:
x
y
z

Exports:
execute()

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 2
SCAN FILE EXPLORER MODULE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Deep scan:

server/file-explorer/

Scan:

controllers/
contracts/
guards/
mappers/
orchestrator/
realtime/
repositories/
routes/
services/
types/
utils/
validators/
watchers/

and all nested files.

Generate:

SERVICE INVENTORY TABLE

For every service identify:

Service Name
File Path
Purpose
Exports
Imports
Dependencies

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 3
BUILD TOOL → SERVICE MATRIX
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

For every filesystem tool determine:

Which file-explorer service SHOULD own the business logic.

Build table:

Tool
Current Imports
Current Runtime Path
Target Service
Reason

Example:

read-file.tool.ts
↓
file.service.ts

Reason:
File reading logic belongs to File Service

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 4
MATCHING REPORT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Classify every tool:

MATCHED

Example:

Tool exists
Service exists
Responsibility matches

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

MISSING

Example:

Tool exists
No service exists

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

DUPLICATE

Example:

Tool logic duplicated in multiple services

or

Multiple services doing same work

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

DEAD

Example:

Tool exists
No imports
No runtime usage
No registrations

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 5
SERVICE CONSOLIDATION ANALYSIS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Detect over-engineering.

Example:

read.service.ts
write.service.ts
rename.service.ts
move.service.ts

If all belong to one domain:

Recommend:

file.service.ts

Show evidence.

Detect:

Services that should be merged.

Detect:

Services that should stay separate.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 6
IMPORT WIRING ANALYSIS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

For every tool determine:

Current Import Chain

Example:

ExecutorAgent
↓
tool-dispatcher
↓
read-file.tool.ts
↓
???

Identify missing wiring.

Generate:

Current State

Target State

Example:

read-file.tool.ts
should import
file.service.ts

write-file.tool.ts
should import
file.service.ts

search-files.tool.ts
should import
search.service.ts

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 7
RUNTIME FLOW VALIDATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Trace complete runtime execution:

Agent
↓
Tool Dispatcher
↓
Tool
↓
Service
↓
Repository
↓
Infrastructure

Verify:

Can every filesystem operation complete successfully?

Detect:

Broken chains

Missing services

Missing repositories

Wrong imports

Circular imports

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 8
CREATE MISSING SERVICES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Only after evidence-based analysis.

Generate list:

Services To Create

Example:

metadata.service.ts
history.service.ts
clipboard.service.ts

Only if actually required.

Do not invent services.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 9
REMOVE DUPLICATES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Identify:

Duplicate services

Duplicate utilities

Duplicate tool implementations

Generate:

Merge Recommendations

With exact file paths.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 10
FINAL ARCHITECTURE REPORT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Output:

1. Tool Inventory

2. Service Inventory

3. Tool → Service Mapping Matrix

4. MATCHED

5. MISSING

6. DUPLICATE

7. DEAD

8. Services To Create

9. Services To Merge

10. Current Runtime Flow

11. Target Runtime Flow

12. Import Wiring Plan

13. Repository Mapping

14. Architecture Violations

15. Final Readiness Score

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FINAL VERDICT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Provide:

MATCHED COUNT

MISSING COUNT

DUPLICATE COUNT

DEAD COUNT

and final status:

✅ READY

⚠️ NEEDS REFACTOR

🚨 HIGH RISK

❌ BROKEN ARCHITECTURE

Use actual code evidence only.

Show exact file paths for every conclusion.

Do not guess.

Do not skip files.

Deep scan everything.