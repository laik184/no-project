You are a Principal Autonomous Multi-Agent Systems Architect.

Your mission is to DEEP SCAN, DESIGN, FIX, and FULLY IMPLEMENT a production-grade MULTI-AGENT ORCHESTRATION SYSTEM inside the existing NURA-X architecture.

ROOT:
server/

━━━━━━━━━━━━━━━━━━━━
MANDATORY ENGINEERING RULES
━━━━━━━━━━━━━━━━━━━━

1. HIGH COHESION + LOW COUPLING
- Every module must have ONE responsibility only.
- No tight cross-domain dependencies.
- No direct agent-to-agent mutation.
- Communication MUST be event-driven or contract-driven.

2. FILE SIZE LIMIT
- NO file may exceed 250 lines.
- Split responsibilities cleanly.
- Extract helpers/services/interfaces when needed.

3. TARGETED PLACEMENT
- Every file MUST live in the correct bounded context.
- AI systems → server/agents/
- orchestration → server/orchestration/
- runtime infra → server/infrastructure/
- reusable systems → server/services/

4. ROOT CAUSE FIXING
- NEVER patch symptoms.
- ALWAYS trace:
  imports
  event flows
  execution lifecycle
  orchestration wiring
  ownership
  retry logic
  concurrency

5. PRODUCTION-GRADE STANDARDS
- Strong typing
- Clear naming
- Readable code
- Modular architecture
- Secure execution
- Deterministic lifecycle
- Replay-safe orchestration

6. CODE REVIEW REQUIRED
Every generated system MUST include:
- architecture notes
- dependency explanation
- validation explanation
- review checklist

7. FAIL-CLOSED DESIGN
NO completion allowed unless:
✅ verification passed
✅ orchestration stable
✅ runtime healthy
✅ agents synchronized
✅ telemetry emitted

8. CONSISTENT TELEMETRY
Every:
- agent execution
- retry
- delegation
- failure
- policy block
- orchestration decision
- completion
- recovery

MUST emit telemetry.

NO silent execution allowed.

━━━━━━━━━━━━━━━━━━━━
MAIN GOAL
━━━━━━━━━━━━━━━━━━━━

Transform the current partially-sequential agent system into:

✅ TRUE MULTI-AGENT ARCHITECTURE
✅ PARALLEL EXECUTION
✅ SPECIALIZED AGENTS
✅ HIERARCHICAL ORCHESTRATION
✅ EVENT-DRIVEN COLLABORATION
✅ FAIL-CLOSED COORDINATION
✅ REPLAYABLE EXECUTION
✅ REPLIT/CURSOR-LEVEL AUTONOMOUS EXECUTION

━━━━━━━━━━━━━━━━━━━━
DEEP SCAN REQUIRED
━━━━━━━━━━━━━━━━━━━━

Perform ULTRA-DEEP ANALYSIS of:

server/agents/
server/orchestration/
server/runtime/
server/infrastructure/
server/tools/
server/fail-closed/
server/telemetry/
server/engine/
server/preview/

Trace:
- all agent systems
- all orchestration systems
- all runtime systems
- all execution routing
- all tool execution
- all retry systems
- all recovery systems
- all event flows

━━━━━━━━━━━━━━━━━━━━
DETECT CURRENT STATE
━━━━━━━━━━━━━━━━━━━━

Find:

1. Which agents already exist?
2. Which are fake/partial?
3. Which are sequential only?
4. Which support parallel execution?
5. Which have telemetry?
6. Which participate in orchestration?
7. Which are isolated/orphaned?
8. Which duplicate responsibilities?
9. Which violate bounded contexts?
10. Which directly mutate runtime?

━━━━━━━━━━━━━━━━━━━━
TARGET MULTI-AGENT ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━

Create or fix:

server/agents/
├── planning/
├── generation/
├── verification/
├── recovery/
├── runtime/
├── browser/
├── memory/
├── orchestration/
├── review/
├── coordination/
└── reflection/

━━━━━━━━━━━━━━━━━━━━
REQUIRED AGENTS
━━━━━━━━━━━━━━━━━━━━

1. PlannerAgent
Role:
- architecture planning
- dependency planning
- DAG generation
- execution phases

2. BuilderAgent
Role:
- code generation
- file creation
- component generation

3. RuntimeAgent
Role:
- runtime observation
- process monitoring
- health analysis

4. VerificationAgent
Role:
- build verification
- runtime verification
- preview verification

5. RecoveryAgent
Role:
- rollback
- recovery strategy
- restart orchestration

6. ReflectionAgent
Role:
- root cause analysis
- fix planning
- self-healing guidance

7. BrowserAgent
Role:
- preview validation
- hydration detection
- browser runtime observation

8. ReviewAgent
Role:
- code quality
- architecture validation
- policy validation

9. MemoryAgent
Role:
- semantic memory
- previous fixes
- architecture learning

10. CoordinationAgent
Role:
- inter-agent synchronization
- dependency awareness
- execution gating

━━━━━━━━━━━━━━━━━━━━
ORCHESTRATION REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Implement:

✅ hierarchical orchestration
✅ parent-child agent execution
✅ dependency-aware execution
✅ parallel execution
✅ execution gating
✅ fail-closed completion
✅ retry orchestration
✅ replayable workflows

━━━━━━━━━━━━━━━━━━━━
PARALLEL EXECUTION
━━━━━━━━━━━━━━━━━━━━

Implement REAL DAG execution.

Example:

install dependencies
+
generate routes
+
generate components
+
generate state

must run in parallel when safe.

━━━━━━━━━━━━━━━━━━━━
EVENT-DRIVEN COLLABORATION
━━━━━━━━━━━━━━━━━━━━

Agents MUST communicate ONLY through:
- EventBus
- orchestration contracts
- typed events
- execution context

NO direct mutable cross-agent state.

━━━━━━━━━━━━━━━━━━━━
FAIL-CLOSED MULTI-AGENT FLOW
━━━━━━━━━━━━━━━━━━━━

Completion ONLY allowed if:

✅ planner success
✅ builder success
✅ runtime healthy
✅ verification passed
✅ preview reachable
✅ browser verification passed
✅ review passed
✅ telemetry complete

Otherwise:
→ recovery
→ reflection
→ retry

━━━━━━━━━━━━━━━━━━━━
REFLECTION + SELF-HEALING
━━━━━━━━━━━━━━━━━━━━

Implement:

failure
→ root cause analysis
→ recovery strategy
→ fix generation
→ retry execution
→ verification rerun

━━━━━━━━━━━━━━━━━━━━
TELEMETRY REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Every agent MUST emit:

agent.started
agent.completed
agent.failed
agent.retry
agent.blocked
agent.recovered
agent.parallel.started
agent.parallel.completed

Include:
- timestamps
- duration
- dependencies
- orchestration phase
- executionId
- runId
- projectId

━━━━━━━━━━━━━━━━━━━━
CONCURRENCY SAFETY
━━━━━━━━━━━━━━━━━━━━

Implement:
✅ execution locks
✅ runtime locks
✅ retry locks
✅ spawn locks
✅ recovery mutexes

Prevent:
❌ double execution
❌ double runtime start
❌ race conditions
❌ parallel mutation conflicts

━━━━━━━━━━━━━━━━━━━━
REQUIRED VALIDATION
━━━━━━━━━━━━━━━━━━━━

Verify:
✅ imports
✅ runtime wiring
✅ orchestration wiring
✅ event subscriptions
✅ replay safety
✅ parallel safety
✅ runtime stability
✅ memory cleanup
✅ telemetry completeness

━━━━━━━━━━━━━━━━━━━━
GENERATE FINAL REPORT
━━━━━━━━━━━━━━━━━━━━

Create:

MULTI_AGENT_ORCHESTRATION_REPORT.md

━━━━━━━━━━━━━━━━━━━━
REPORT MUST INCLUDE
━━━━━━━━━━━━━━━━━━━━

1. Existing Agent Audit
2. Missing Agent Systems
3. Fake/Partial Agents
4. Agent Responsibility Matrix
5. Multi-Agent Architecture Graph
6. Parallel Execution Graph
7. DAG Execution Flow
8. Agent Event Flow
9. Runtime Ownership Graph
10. Orchestration Hierarchy
11. Coordination Flow
12. Verification Flow
13. Recovery Flow
14. Reflection Flow
15. Telemetry Flow
16. Concurrency Safety Analysis
17. Replay Safety Analysis
18. Fail-Closed Validation Analysis
19. Exact Files Created
20. Exact Files Modified
21. Exact Imports Updated
22. Exact Events Added
23. Exact Locks Added
24. Exact Runtime Integrations
25. Exact Recovery Integrations
26. Exact Verification Integrations
27. Architecture Quality Score
28. Multi-Agent Maturity Score
29. Parallel Execution Score
30. Replit/Cursor Similarity %
31. Remaining Weak Areas
32. Recommended Next Upgrades

━━━━━━━━━━━━━━━━━━━━
FINAL GOAL
━━━━━━━━━━━━━━━━━━━━

Convert the current system from:

“single-agent sequential execution”

INTO:

“production-grade autonomous multi-agent operating system”

with:
- deterministic orchestration
- replayable execution
- fail-closed validation
- self-healing recovery
- parallel agent collaboration
- production-grade runtime safety

Evidence-based implementation only.
No fake orchestration.
No simulated multi-agent behavior.