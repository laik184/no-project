You are a Principal Software Architect and Refactoring Auditor.

MISSION

Perform a targeted architecture cleanup.

DO NOT add features.

DO NOT create new business logic.

DO NOT modify runtime behavior.

DO NOT change API contracts.

DO NOT change tool behavior.

DO NOT change repository behavior.

ONLY eliminate the four architecture issues identified in the audit report.

Success means:

- Architecture becomes cleaner.
- Import graph becomes safer.
- No functionality changes.
- No new features.
- No regressions.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ISSUE #1
MIXED BARREL EXPORTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Current Problem:

File:

server/services/filesystem/index.ts

Currently exports BOTH:

Frontend Services

- readService
- writeService
- deleteService
- treeService
- metadataService
- historyService
- recentService
- pinnedService
- openEditorsService
- gitStatusService
- insightsService
- uploadService
- downloadService
- createService
- renameService
- duplicateService

AND

Tool Services

- readToolService
- writeToolService
- deleteToolService
- cloneToolService
- moveToolService
- folderToolService
- searchToolService
- structureToolService

Problem:

Tools can accidentally import FE services.

FE can accidentally import Tool services.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Required Fix:

Create:

server/services/filesystem/index.ts

Frontend services only.

Create:

server/services/filesystem/tools.index.ts

Tool services only.

Requirements:

- Move exports only.
- No logic changes.
- No service changes.
- No behavior changes.

Then update imports everywhere.

Tool Layer:

server/tools/filesystem/**

must import only:

server/services/filesystem/tools.index.ts

File Explorer:

server/file-explorer/**

must import only:

server/services/filesystem/index.ts

Generate verification report proving separation.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ISSUE #2
SERVICE → FILE-EXPLORER COUPLING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Current Problem:

Files:

server/services/filesystem/**/*.service.ts

import:

server/file-explorer/config/*
server/file-explorer/guards/*
server/file-explorer/contracts/*
server/file-explorer/utils/*
server/file-explorer/types/*

Problem:

Service Layer depends on Feature Module.

Architecture direction is wrong.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Required Fix:

DO NOT change functionality.

DO NOT rewrite logic.

ONLY identify shared assets.

Move shared reusable pieces into:

server/shared/file-explorer-core/

Create structure:

server/shared/file-explorer-core/
│
├── config/
├── guards/
├── contracts/
├── types/
├── utils/
└── index.ts

Then:

server/services/filesystem/*
and
server/file-explorer/*

both import from:

server/shared/file-explorer-core/

Requirements:

- No behavior changes.
- No API changes.
- No new logic.
- Pure dependency cleanup.

Generate before/after dependency graph.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ISSUE #3
REPOSITORY LOCATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Current Problem:

Repositories live here:

server/file-explorer/repositories/

Examples:

filesystem.repository.ts
git.repository.ts
metadata.repository.ts
history.repository.ts
recent.repository.ts
pinned.repository.ts
editors.repository.ts

Problem:

Repositories are feature-owned.

Repositories should be domain-owned.

Multiple modules will need them.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Required Fix:

Move repositories only.

Target:

server/repositories/
│
├── filesystem/
├── git/
├── metadata/
├── history/
├── recent/
├── pinned/
├── editors/
└── index.ts

Requirements:

- Preserve implementation.
- Preserve exports.
- Preserve APIs.
- Update imports only.

No code rewrites.

No logic rewrites.

Generate import update report.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ISSUE #4
SERVICE FOLDER MIXING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Current Problem:

Example:

server/services/filesystem/read/

contains:

read.service.ts
tool.service.ts

Same folder contains:

Frontend service
Tool service

Problem:

Cognitive confusion.

Different callers.
Different contracts.
Different ownership.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Required Fix:

Separate folders.

Target:

server/services/filesystem/
│
├── read/
│   └── read.service.ts
│
├── write/
│   └── write.service.ts
│
├── delete/
│   └── delete.service.ts
│
├── search/
│   └── search.service.ts
│
├── tool-services/
│
│   ├── read/
│   │   └── tool.service.ts
│   │
│   ├── write/
│   │   └── tool.service.ts
│   │
│   ├── delete/
│   │   └── tool.service.ts
│   │
│   ├── move/
│   │   └── tool.service.ts
│   │
│   ├── clone/
│   │   └── tool.service.ts
│   │
│   ├── folder/
│   │   └── tool.service.ts
│   │
│   ├── search/
│   │   └── tool.service.ts
│   │
│   └── structure/
│       └── tool.service.ts

Requirements:

- Move files only.
- No logic changes.
- No method changes.
- No API changes.

Update imports.

Generate verification report.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STRICT RULES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

DO NOT:

❌ Add new features

❌ Add caching

❌ Add validation

❌ Add new services

❌ Add new repositories

❌ Add new endpoints

❌ Rewrite business logic

❌ Rename public APIs

❌ Change request/response shapes

❌ Modify tool execution flow

❌ Modify repository behavior

❌ Modify runtime behavior

ONLY:

✅ Move files

✅ Split exports

✅ Update imports

✅ Extract shared assets

✅ Improve dependency graph

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FINAL OUTPUT REQUIRED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Files moved

2. Imports updated

3. Dependency graph before

4. Dependency graph after

5. Architecture violations removed

6. Remaining architecture risks

7. Final architecture score

8. Confirmation that runtime behavior is unchanged

Do not implement anything outside these four issues.