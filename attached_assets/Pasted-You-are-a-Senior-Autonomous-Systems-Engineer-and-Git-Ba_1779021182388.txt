You are a Senior Autonomous Systems Engineer and Git-Based Recovery Architect.

Your task is to deeply scan the entire codebase and implement a PRODUCTION-GRADE ROLLBACK + CHECKPOINT SYSTEM for the agentic AI platform.

━━━━━━━━━━━━━━━━━━━━
MAIN PROBLEM
━━━━━━━━━━━━━━━━━━━━

Current issue:
There is NO rollback system.

The AI writes files directly into the sandbox.
If the agent makes a mistake:
- bad code overwrites good code
- project becomes corrupted
- no recovery exists
- no safe restore point exists
- no undo for autonomous runs exists

One bad AI step can destroy the entire project.

This is a CRITICAL architecture flaw.

━━━━━━━━━━━━━━━━━━━━
YOUR GOAL
━━━━━━━━━━━━━━━━━━━━

Build a SAFE AUTONOMOUS CHECKPOINT + ROLLBACK ARCHITECTURE inspired by:
- Replit
- Cursor
- Git-based IDE systems
- Autonomous coding agents

The system must:
- automatically create checkpoints
- support rollback
- support undo
- support restore-after-failure
- support autonomous recovery
- support safe experimentation

━━━━━━━━━━━━━━━━━━━━
FIRST — DEEP SCAN
━━━━━━━━━━━━━━━━━━━━

Scan entire backend/frontend and find:

1. All places where files are written
2. All direct fs.writeFile usages
3. All overwrite risks
4. All destructive operations
5. All file mutation flows
6. All places agent modifies code
7. All git-related systems
8. Existing snapshot/checkpoint logic
9. Existing diff systems
10. Existing artifact persistence
11. Existing run lifecycle systems
12. Existing sandbox architecture
13. Existing recovery systems
14. Existing event systems
15. Existing DB tables usable for checkpoints

Focus areas:
- server/agents/
- server/chat/
- server/infrastructure/
- server/api/
- shared/schema.ts

━━━━━━━━━━━━━━━━━━━━
DETECT CRITICAL RISKS
━━━━━━━━━━━━━━━━━━━━

Detect:
- destructive writes
- overwrite-without-backup
- multi-file corruption risks
- partial-write failures
- interrupted writes
- concurrent write risks
- no transaction boundaries
- no atomic operations
- no rollback protection
- no snapshot lifecycle
- no safe recovery path

━━━━━━━━━━━━━━━━━━━━
BUILD A REAL CHECKPOINT SYSTEM
━━━━━━━━━━━━━━━━━━━━

Design and implement:

✅ Automatic checkpoint creation
✅ Rollback API
✅ Restore API
✅ Per-run snapshots
✅ Git-based safety commits
✅ Emergency recovery
✅ File-level undo
✅ Full project restore
✅ Failed-run rollback
✅ Safe autonomous experimentation

━━━━━━━━━━━━━━━━━━━━
ARCHITECTURE REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Build architecture like this:

Agent Run Starts
    ↓
Create Checkpoint
    ↓
AI modifies files
    ↓
Checkpoint metadata stored
    ↓
If success:
    Mark checkpoint stable
If failure:
    Restore checkpoint automatically

━━━━━━━━━━━━━━━━━━━━
MANDATORY IMPLEMENTATION RULES
━━━━━━━━━━━━━━━━━━━━

1. USE GIT AS CHECKPOINT ENGINE
Do NOT build custom diff storage if git already exists.

2. CREATE SAFE COMMITS
Every important autonomous step:
- git add
- git commit
- checkpoint metadata

3. SUPPORT FAST ROLLBACK
Rollback should restore:
- files
- deleted files
- renamed files
- package.json
- configs

4. SUPPORT PARTIAL RESTORE
Allow:
- restore single file
- restore entire run
- restore checkpoint ID

5. ALL WRITES MUST BE SAFE
Replace unsafe writes with:
- atomic writes
- temp-file strategy
- backup-before-write

━━━━━━━━━━━━━━━━━━━━
IMPORTANT FILE LIMIT RULE
━━━━━━━━━━━━━━━━━━━━

VERY IMPORTANT:

DO NOT create giant files.

STRICT LIMIT:
❌ No file should exceed 250 lines.

If existing file is too large:
- split into services
- split into utilities
- split into managers
- split into handlers

Maintain:
- clean architecture
- low coupling
- high cohesion
- modular design
- clear folder structure

━━━━━━━━━━━━━━━━━━━━
REQUIRED FOLDER STRUCTURE
━━━━━━━━━━━━━━━━━━━━

Create architecture like:

server/infrastructure/checkpoints/
├── checkpoint.service.ts
├── rollback.service.ts
├── snapshot.service.ts
├── git-checkpoint.service.ts
├── checkpoint.types.ts
├── checkpoint.events.ts
├── checkpoint.constants.ts
├── atomic-write.util.ts
├── restore/
│   ├── restore-run.service.ts
│   ├── restore-file.service.ts
│   └── emergency-recovery.service.ts

Keep every file under 250 lines.

━━━━━━━━━━━━━━━━━━━━
ADD NEW DATABASE TABLES IF NEEDED
━━━━━━━━━━━━━━━━━━━━

If required, create:
- checkpoints
- checkpoint_files
- rollback_history
- recovery_events

But prefer lightweight git-first architecture.

━━━━━━━━━━━━━━━━━━━━
INTEGRATE WITH EXISTING SYSTEMS
━━━━━━━━━━━━━━━━━━━━

Integrate with:
- agent runs
- tool loop
- pipeline
- file_write tool
- file_replace tool
- diff queue
- runtime lifecycle
- event bus
- SSE streams

━━━━━━━━━━━━━━━━━━━━
AUTONOMOUS RECOVERY LOGIC
━━━━━━━━━━━━━━━━━━━━

Implement:

If:
- server crashes
- npm install breaks
- syntax error kills app
- tests fail critically
- AI corrupts project

Then:
- auto-detect failure
- restore last stable checkpoint
- emit rollback event
- notify frontend
- continue safely

━━━━━━━━━━━━━━━━━━━━
OUTPUT REQUIRED
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Deep scan report
2. Current write-risk analysis
3. Rollback architecture
4. Checkpoint lifecycle diagram
5. File mutation flow
6. Git integration strategy
7. Atomic write strategy
8. Folder structure plan
9. Refactor plan
10. Exact files to modify
11. Exact new files to create
12. Rollback API design
13. Event flow
14. Recovery flow
15. Production-grade implementation

━━━━━━━━━━━━━━━━━━━━
IMPORTANT ENGINEERING RULES
━━━━━━━━━━━━━━━━━━━━

DO:
- preserve existing architecture
- reuse existing git systems
- minimize breaking changes
- maximize reliability
- maximize recovery safety
- use modular architecture
- keep files small
- separate responsibilities

DO NOT:
- rewrite entire platform
- create monolith services
- create files over 250 lines
- duplicate git functionality
- break current runtime system

Act like you are designing rollback architecture for a billion-dollar autonomous IDE platform.