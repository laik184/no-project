━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 11
ARCHITECTURE OWNERSHIP ANALYSIS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

After scanning the entire codebase:

Determine the CORRECT ownership model.

For every layer identify:

1. Who owns it
2. Who can import it
3. Who cannot import it
4. Who can call it
5. Who cannot call it

Analyze:

main.ts

server/chat/

server/orchestration/

server/agents/

server/tools/

server/services/

server/repositories/

server/infrastructure/

Generate ownership matrix.

Example:

Layer:
server/orchestration/

Can Import:

- agents
- services

Cannot Import:

- chat
- controllers
- routes

Reason:
Avoid circular orchestration ownership.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 12
IMPORT DIRECTION ANALYSIS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Build the FINAL import architecture.

For every major file show:

FILE
↓ imports
FILE

Example:

main.ts
↓ imports
server/chat/index.ts

server/chat/index.ts
↓ imports
server/chat/routes/chat.routes.ts

server/chat/routes/chat.routes.ts
↓ imports
server/chat/controllers/chat.controller.ts

Continue until execution reaches leaf nodes.

Generate COMPLETE import tree.

Identify:

VALID IMPORTS

INVALID IMPORTS

DANGEROUS IMPORTS

CIRCULAR IMPORTS

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 13
RUNTIME OWNERSHIP DESIGN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Determine the correct runtime flow.

Do NOT use current architecture.

First analyze current flow.

Then design the IDEAL flow.

Example:

User
↓
Routes
↓
Controller
↓
Service
↓
Orchestrator
↓
Agent Coordinator
↓
Agent
↓
Tool Dispatcher
↓
Tool
↓
Service
↓
Repository
↓
Filesystem

Verify whether the current implementation follows this flow.

If not:

Generate corrected runtime flow.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 14
SERVICE BOUNDARY ANALYSIS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Determine:

Which logic belongs in:

- Routes
- Controllers
- Services
- Orchestrators
- Agents
- Tools
- Repositories

Identify violations.

Example:

Tool executing orchestration
❌ Wrong

Agent directly writing database
❌ Wrong

Controller running business logic
❌ Wrong

Repository importing service
❌ Wrong

Generate fix plan.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 15
FINAL TARGET ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Generate the FINAL architecture that should exist after refactoring.

Show:

Folder Structure

Import Graph

Runtime Graph

Ownership Graph

Dependency Graph

Execution Graph

For EVERY layer explain:

Purpose

Responsibilities

Allowed Imports

Forbidden Imports

Allowed Calls

Forbidden Calls

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 16
IMPLEMENTATION PLAN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Generate exact implementation steps.

Step 1
Files to modify

Step 2
Imports to move

Step 3
Dependencies to remove

Step 4
Services to extract

Step 5
Tools to refactor

Step 6
Agents to refactor

Step 7
Orchestrators to refactor

Step 8
Final validation

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FINAL GOAL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Produce a production-grade architecture where:

- No circular dependency exists
- No recursive orchestration exists
- No infinite execution loop exists
- No tool can re-enter orchestration
- No agent can own orchestration
- No child module owns parent module
- Runtime flow is deterministic
- Import direction is clean
- Service boundaries are clear
- Every file has a single responsibility

Finally answer:

1. Current Architecture Status
2. Root Cause of Problems
3. Exact Files Causing Risk
4. Correct Import Structure
5. Correct Runtime Flow
6. Correct Service Flow
7. Correct Agent Flow
8. Correct Tool Flow
9. Final Refactor Architecture
10. Production Readiness Score (0-100)
11. Final Verdict

Choose one:

✅ Production Safe

⚠️ Architecture Smell

🚨 High Risk

💥 Critical Infinite Loop Risk