You are a Principal Runtime Infrastructure Auditor.

Your task is to perform an ULTRA-DEEP XRAY ANALYSIS of the ENTIRE Runtime Management System.

MAIN GOAL:
Verify whether the current runtime system actually works like a Replit-level runtime lifecycle manager OR if it is only partially implemented/fake infrastructure.

━━━━━━━━━━━━━━━━━━━━
SCAN TARGETS
━━━━━━━━━━━━━━━━━━━━

Deep scan ALL related runtime systems:

server/infrastructure/runtime/
server/runtime/
server/preview/
server/orchestration/
server/console/
server/chat/
server/infrastructure/events/
server/fail-closed/

Also recursively scan:
- imports
- exports
- singleton ownership
- process ownership
- event flows
- runtime hooks
- preview hooks
- recovery hooks
- verification hooks

━━━━━━━━━━━━━━━━━━━━
MAIN QUESTIONS TO ANSWER
━━━━━━━━━━━━━━━━━━━━

1. Does a REAL RuntimeManager exist?
2. Is it actually used?
3. Is it wired into orchestration?
4. Does it truly manage processes?
5. Does it detect ports?
6. Does it detect crashes?
7. Does it restart runtimes?
8. Does preview sync with runtime?
9. Does SSE receive runtime lifecycle events?
10. Does verification depend on runtime state?
11. Is runtime state centralized?
12. Is runtime cleanup implemented?
13. Is runtime ownership deterministic?
14. Is runtime lifecycle event-driven?
15. Is runtime recovery production-grade?

━━━━━━━━━━━━━━━━━━━━
VERIFY THESE FEATURES
━━━━━━━━━━━━━━━━━━━━

# 1. PROCESS MANAGEMENT

Verify:
✅ spawn()
✅ child_process usage
✅ process ownership
✅ process tracking
✅ process cleanup
✅ zombie prevention
✅ background process handling

Find:
- actual runtime start logic
- actual stop logic
- actual restart logic

━━━━━━━━━━━━━━━━━━━━
# 2. PORT DETECTION

Verify:
✅ dynamic port detection
✅ waitForPort()
✅ health checks
✅ timeout handling
✅ retry handling
✅ stale port cleanup

Find:
- how ports are allocated
- how preview receives ports
- how runtime exposes URLs

━━━━━━━━━━━━━━━━━━━━
# 3. PREVIEW LIFECYCLE

Verify:
✅ idle
✅ starting
✅ running
✅ error

Check:
- state machine exists?
- event driven?
- synchronized with frontend?
- runtime linked to preview?

━━━━━━━━━━━━━━━━━━━━
# 4. EVENT BUS INTEGRATION

Verify runtime emits:
✅ runtime.started
✅ runtime.stopped
✅ runtime.crashed
✅ preview.lifecycle
✅ process.crashed

Check:
- SSE broadcasts
- websocket integration
- frontend realtime sync

━━━━━━━━━━━━━━━━━━━━
# 5. CONSOLE PIPELINE

Verify:
✅ stdout capture
✅ stderr capture
✅ metadata extraction
✅ realtime console streaming
✅ vite detection
✅ npm detection
✅ crash detection

━━━━━━━━━━━━━━━━━━━━
# 6. CRASH RECOVERY

Verify:
✅ crash detection
✅ restart strategy
✅ checkpoint restore
✅ timeout protection
✅ recovery lock
✅ retry policy

Check:
- does CrashResponder actually work?
- does RecoveryManager actually recover?

━━━━━━━━━━━━━━━━━━━━
# 7. RUNTIME STORE

Find:
- centralized runtime state?
- singleton runtime store?
- ownership map?
- preview state ownership?

Verify:
✅ single source of truth
✅ synchronized frontend/backend state
✅ deterministic lifecycle

━━━━━━━━━━━━━━━━━━━━
# 8. ORCHESTRATION INTEGRATION

Verify:
- orchestration engine calls runtime?
- verification waits for runtime?
- preview depends on runtime?
- recovery depends on runtime?

Trace COMPLETE FLOW:

User
→ Chat
→ RunController
→ Orchestration
→ RuntimeManager
→ Process
→ Preview
→ Verification
→ Recovery
→ SSE
→ Frontend

━━━━━━━━━━━━━━━━━━━━
# 9. REALTIME FRONTEND SYNC

Deep scan:

client/src/features/preview/
client/src/pages/preview/
client/src/hooks/

Verify:
✅ EventSource
✅ realtime subscriptions
✅ lifecycle rendering
✅ loading states
✅ error states
✅ reconnect logic
✅ iframe sync

━━━━━━━━━━━━━━━━━━━━
# 10. REPLIT-LEVEL FEATURES

Verify if system supports:

✅ auto runtime start
✅ auto restart
✅ live preview updates
✅ realtime console
✅ process isolation
✅ preview lifecycle animation
✅ runtime crash recovery
✅ instant frontend sync
✅ deterministic runtime ownership

━━━━━━━━━━━━━━━━━━━━
DETECT PROBLEMS
━━━━━━━━━━━━━━━━━━━━

Find:
❌ fake runtime systems
❌ orphan runtime code
❌ duplicate runtime managers
❌ dead lifecycle hooks
❌ broken preview sync
❌ missing crash recovery
❌ stale runtime state
❌ non-deterministic ownership
❌ preview desync
❌ missing SSE events
❌ broken process cleanup
❌ memory leaks
❌ zombie processes
❌ circular runtime ownership

━━━━━━━━━━━━━━━━━━━━
TRACE REAL EXECUTION
━━━━━━━━━━━━━━━━━━━━

Trace ACTUAL execution:

1. User clicks "Run"
2. Which file executes?
3. Which runtime starts?
4. Which process spawns?
5. Which port binds?
6. Which event emits?
7. Which SSE broadcasts?
8. Which frontend component updates?
9. Which iframe loads?
10. Which verification runs?
11. Which recovery triggers on failure?

Use evidence-based tracing only.

━━━━━━━━━━━━━━━━━━━━
GENERATE FINAL REPORT
━━━━━━━━━━━━━━━━━━━━

Create:

RUNTIME_MANAGER_XRAY_REPORT.md

━━━━━━━━━━━━━━━━━━━━
REPORT MUST INCLUDE
━━━━━━━━━━━━━━━━━━━━

1. Runtime Architecture Map
2. Runtime Ownership Graph
3. Runtime Lifecycle Graph
4. Process Management Analysis
5. Port Detection Analysis
6. Preview Lifecycle Analysis
7. EventBus Runtime Integration
8. SSE Runtime Integration
9. WebSocket Runtime Integration
10. Console Pipeline Analysis
11. Crash Recovery Analysis
12. RecoveryManager Analysis
13. CrashResponder Analysis
14. RuntimeStore Analysis
15. Frontend Sync Analysis
16. Preview Sync Analysis
17. Realtime Sync Analysis
18. Runtime Cleanup Analysis
19. Runtime Restart Analysis
20. Runtime Verification Analysis
21. Runtime Memory Leak Risks
22. Zombie Process Risks
23. Runtime Stability Score
24. Runtime Reliability Score
25. Replit Runtime Similarity %
26. Runtime Production Readiness %
27. Missing Runtime Features
28. Fake/Partial Runtime Systems
29. Broken Runtime Wiring
30. Exact Files Responsible
31. Exact Runtime Entry Points
32. Exact Runtime Event Flow
33. Exact Runtime Failure Points
34. Safe Refactor Recommendations
35. Runtime Upgrade Recommendations

━━━━━━━━━━━━━━━━━━━━
MANDATORY ENGINEERING RULES
━━━━━━━━━━━━━━━━━━━━

✅ HIGH COHESION
✅ LOW COUPLING
✅ SINGLE SOURCE OF TRUTH
✅ EVENT-DRIVEN RUNTIME
✅ DETERMINISTIC OWNERSHIP
✅ SAFE PROCESS MANAGEMENT
✅ FAIL-CLOSED RUNTIME
✅ PRODUCTION-GRADE RECOVERY

❌ NO assumptions
❌ NO shallow analysis
❌ NO fake conclusions
❌ NO skipped folders

━━━━━━━━━━━━━━━━━━━━
FINAL GOAL
━━━━━━━━━━━━━━━━━━━━

Determine whether the runtime system is:

1. Fake prototype runtime
2. Partial runtime infrastructure
3. Advanced runtime manager
4. Replit-level autonomous runtime lifecycle system

And explain EXACTLY why.