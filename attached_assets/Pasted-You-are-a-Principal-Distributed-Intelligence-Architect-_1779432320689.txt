You are a Principal Distributed Intelligence Architect.

Your mission is to DESIGN and IMPLEMENT a production-grade:

QUANTUM SUPERPOSITION PATH SYSTEM

inside the existing NURA-X autonomous backend.

━━━━━━━━━━━━━━━━━━━━
MAIN GOAL
━━━━━━━━━━━━━━━━━━━━

Create a system where:

ONE user request
→ splits into MULTIPLE execution paths
→ each path explores DIFFERENT strategies
→ all paths run in PARALLEL
→ results are analyzed
→ best solution is selected
→ conflicts are resolved
→ final unified execution state is committed

This is NOT fake “parallel Promise.all”.

This is:
REAL autonomous execution-path intelligence.

━━━━━━━━━━━━━━━━━━━━
MANDATORY ENGINEERING RULES
━━━━━━━━━━━━━━━━━━━━

1. HIGH COHESION / LOW COUPLING
- Every module must have ONE responsibility.
- No tight dependencies.
- Use interfaces/contracts.

2. FILE SIZE LIMIT
- NO file may exceed 250 lines.
- Split modules aggressively.

3. TARGETED PLACEMENT
- Every system MUST live in the correct bounded-context folder.
- No architecture pollution.

4. ROOT CAUSE ENGINEERING
- Analyze architectural risks before implementation.
- Prevent race conditions.
- Prevent shared-state corruption.

5. PRODUCTION-GRADE STANDARDS
- Strong typing
- Readable naming
- Scalable architecture
- Security-first
- No hacks
- No hidden side effects

6. MANDATORY REVIEWABILITY
- Modular files
- Clear interfaces
- Easy auditability
- Explicit ownership

7. FAIL-CLOSED EXECUTION
- NO completion without:
  ✅ verification
  ✅ conflict resolution
  ✅ runtime validation
  ✅ evidence validation

8. CONSISTENT TELEMETRY
Every:
- path spawn
- path collapse
- retry
- conflict
- resolution
- merge
- failure
- completion

MUST emit telemetry.

━━━━━━━━━━━━━━━━━━━━
TARGET LOCATION
━━━━━━━━━━━━━━━━━━━━

Create NEW bounded context:

server/quantum/

━━━━━━━━━━━━━━━━━━━━
TARGET ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━

server/quantum/
├── engine/
│   ├── quantum-engine.ts
│   ├── path-spawner.ts
│   ├── path-collapse.ts
│   ├── path-selector.ts
│   └── quantum-runner.ts
│
├── superposition/
│   ├── superposition-manager.ts
│   ├── execution-path.ts
│   ├── path-registry.ts
│   └── path-lifecycle.ts
│
├── aggregation/
│   ├── result-aggregator.ts
│   ├── consensus-merger.ts
│   ├── confidence-scorer.ts
│   └── merge-strategy.ts
│
├── conflicts/
│   ├── conflict-resolver.ts
│   ├── file-conflict-detector.ts
│   ├── ast-merge-engine.ts
│   └── write-lock-manager.ts
│
├── scheduler/
│   ├── worker-pool.ts
│   ├── priority-queue.ts
│   ├── work-stealing.ts
│   └── task-partitioner.ts
│
├── telemetry/
│   ├── quantum-telemetry.ts
│   ├── execution-trace.ts
│   └── collapse-metrics.ts
│
├── verification/
│   ├── path-verifier.ts
│   ├── collapse-validator.ts
│   └── consensus-validator.ts
│
└── types/
    ├── quantum.types.ts
    ├── path.types.ts
    └── merge.types.ts

━━━━━━━━━━━━━━━━━━━━
CORE EXECUTION MODEL
━━━━━━━━━━━━━━━━━━━━

USER GOAL
    ↓
QuantumEngine
    ↓
TaskPartitioner
    ↓
PathSpawner
    ↓
N Parallel Execution Paths

EXAMPLE:

PATH A:
- Redux architecture

PATH B:
- Zustand architecture

PATH C:
- React Context architecture

ALL RUN SIMULTANEOUSLY

    ↓
ResultAggregator
    ↓
ConflictResolver
    ↓
ConfidenceScorer
    ↓
ConsensusMerger
    ↓
PathCollapse
    ↓
Unified Final State
    ↓
Fail-Closed Verification
    ↓
CompletionAuthority

━━━━━━━━━━━━━━━━━━━━
IMPLEMENT THESE SYSTEMS
━━━━━━━━━━━━━━━━━━━━

# 1. SUPERPOSITION MANAGER

Create:
server/quantum/superposition/superposition-manager.ts

Responsibilities:
✅ spawn paths
✅ monitor paths
✅ track lifecycle
✅ collapse execution
✅ synchronize results

MUST:
- support N paths
- support retries
- support path isolation
- support telemetry

━━━━━━━━━━━━━━━━━━━━
# 2. EXECUTION PATH MODEL

Create:
execution-path.ts

Each path MUST contain:
- pathId
- strategy
- confidence
- runtime state
- result
- telemetry
- verification status
- lifecycle state

Lifecycle:
IDLE
→ SPAWNING
→ RUNNING
→ VERIFYING
→ MERGING
→ COLLAPSED
→ FAILED

━━━━━━━━━━━━━━━━━━━━
# 3. PATH SPAWNER

Create:
path-spawner.ts

Responsibilities:
✅ create isolated execution paths
✅ assign strategies
✅ assign workers
✅ allocate runtime ownership
✅ attach telemetry

Example:
Goal:
“Build auth system”

Spawn:
- JWT strategy
- Session strategy
- Clerk strategy

━━━━━━━━━━━━━━━━━━━━
# 4. WORKER POOL

Create:
worker-pool.ts

Requirements:
✅ concurrency cap
✅ priority scheduling
✅ work stealing
✅ backpressure
✅ worker lifecycle
✅ cancellation support

MUST:
- avoid starvation
- avoid deadlocks
- avoid memory leaks

━━━━━━━━━━━━━━━━━━━━
# 5. RESULT AGGREGATOR

Create:
result-aggregator.ts

Responsibilities:
✅ collect path outputs
✅ normalize outputs
✅ merge compatible results
✅ emit aggregation telemetry

MUST:
- support async collection
- support partial completion
- support confidence scoring

━━━━━━━━━━━━━━━━━━━━
# 6. CONFLICT RESOLVER

Create:
conflict-resolver.ts

Responsibilities:
✅ detect same-file writes
✅ detect incompatible architecture decisions
✅ resolve merge conflicts
✅ escalate unresolved conflicts

Strategies:
- AST merge
- confidence merge
- supervisor arbitration
- last-writer fallback (ONLY as last resort)

━━━━━━━━━━━━━━━━━━━━
# 7. CONFIDENCE SCORER

Create:
confidence-scorer.ts

Score based on:
✅ verification success
✅ runtime stability
✅ code quality
✅ policy compliance
✅ telemetry health
✅ retry count
✅ hallucination risk

━━━━━━━━━━━━━━━━━━━━
# 8. PATH COLLAPSE ENGINE

Create:
path-collapse.ts

Responsibilities:
✅ select best path
✅ merge compatible paths
✅ discard weak paths
✅ generate unified final state

MUST:
- preserve deterministic state
- emit collapse telemetry
- support replay/debugging

━━━━━━━━━━━━━━━━━━━━
# 9. TELEMETRY

Create:
quantum-telemetry.ts

MANDATORY EVENTS:
- quantum.path.spawned
- quantum.path.started
- quantum.path.completed
- quantum.path.failed
- quantum.conflict.detected
- quantum.conflict.resolved
- quantum.collapse.started
- quantum.collapse.completed

MUST integrate with:
server/orchestration/telemetry/
server/infrastructure/events/

━━━━━━━━━━━━━━━━━━━━
# 10. FAIL-CLOSED VALIDATION

NO path may collapse unless:
✅ verification passes
✅ runtime alive
✅ preview healthy
✅ no unresolved conflicts
✅ evidence valid

Integrate with:
server/fail-closed/

━━━━━━━━━━━━━━━━━━━━
# 11. EXECUTION ROUTER INTEGRATION

Modify:
server/orchestration/execution/execution-router.ts

Add:
QUANTUM execution mode

Routing conditions:
- large codebase
- multi-module tasks
- architecture generation
- refactor operations
- high complexity score

━━━━━━━━━━━━━━━━━━━━
# 12. DAG ENGINE INTEGRATION

Integrate with:
server/engine/graph/

Requirements:
✅ dynamic node injection
✅ parallel waves
✅ path-aware checkpoints
✅ rollback support

━━━━━━━━━━━━━━━━━━━━
ROOT CAUSE ANALYSIS REQUIRED
━━━━━━━━━━━━━━━━━━━━

Before implementation:
analyze:
- race conditions
- write conflicts
- memory corruption risks
- replay safety
- runtime ownership
- event synchronization
- worker starvation
- deadlocks

Document mitigations.

━━━━━━━━━━━━━━━━━━━━
MANDATORY SAFETY SYSTEMS
━━━━━━━━━━━━━━━━━━━━

IMPLEMENT:
✅ FileLockManager
✅ MemoryWriteQueue
✅ ConflictBarrier
✅ ReplaySafetyGuard
✅ RuntimeOwnershipGuard

━━━━━━━━━━━━━━━━━━━━
REQUIRED REPORT
━━━━━━━━━━━━━━━━━━━━

Generate:

QUANTUM_SUPERPOSITION_IMPLEMENTATION_REPORT.md

━━━━━━━━━━━━━━━━━━━━
REPORT MUST INCLUDE
━━━━━━━━━━━━━━━━━━━━

1. Full Architecture Diagram
2. Execution Lifecycle
3. Path Lifecycle Graph
4. Worker Pool Design
5. Aggregation Flow
6. Conflict Resolution Flow
7. Runtime Ownership Model
8. Event Synchronization Model
9. Replay Safety Model
10. File Locking Strategy
11. Memory Safety Strategy
12. Telemetry Events
13. Verification Integration
14. DAG Integration
15. Execution Router Integration
16. Fail-Closed Validation
17. Concurrency Risk Analysis
18. Race Condition Mitigation
19. Deadlock Prevention
20. Memory Leak Prevention
21. Scalability Analysis
22. Replit-Level Similarity %
23. Quantum-Inspired Readiness %
24. Performance Impact Estimate
25. Parallel Throughput Estimate
26. Remaining Weak Areas
27. Recommended Future Improvements

━━━━━━━━━━━━━━━━━━━━
FINAL GOAL
━━━━━━━━━━━━━━━━━━━━

Transform NURA-X from:

“advanced autonomous agent”

INTO:

“quantum-inspired parallel autonomous software operating system”

WITHOUT:
❌ architecture pollution
❌ unsafe parallelism
❌ giant files
❌ tight coupling
❌ hidden side effects
❌ fake orchestration
❌ uncontrolled execution

Use SAFE incremental implementation only.