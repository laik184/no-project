MISSION:
Refactor and harden the entire verification + recovery architecture to eliminate ALL fake-success paths and enforce deterministic fail-closed execution.

You are operating as a principal autonomous systems architect.

DO NOT explain theory.
DO NOT give high-level suggestions.
DO NOT generate placeholder pseudo-fixes.

You must perform a real architecture refactor plan and implementation design.

==================================================
PRIMARY OBJECTIVE
==================================================

The system must NEVER return success unless success is externally verified through deterministic runtime evidence.

Eliminate:
- fake success states
- exhausted-but-successful flows
- warning-based completion
- LLM self-verification
- silent recovery failures
- verification bypasses
- unverified no_tool_calls exits
- log-scanning verification

==================================================
CRITICAL RULES
==================================================

IMPORTANT:
- Keep every file under 250 LOC
- High cohesion
- Low coupling
- No God files
- No mixed responsibilities
- No hidden shared mutable state
- Use dependency injection where needed
- Prefer composition over inheritance

STRICTLY ENFORCE:
- One responsibility per file
- One orchestration layer only
- Immutable verification results
- Deterministic state transitions
- Runtime truth > LLM reasoning
- Evidence-first architecture
- Fail-closed execution only

==================================================
CURRENT FAKE SUCCESS PATHS TO ELIMINATE
==================================================

1. verification_exhausted → ok:true
2. no_tool_calls → success:true without verification
3. warning-based completion states
4. metadata-only checkpoint validation
5. log-scanning instead of actual subprocess verification
6. ungrounded claims continuing silently
7. recovery success without post-restore verification

==================================================
TARGET EXECUTION FLOW
==================================================

LLM calls task_complete
→ ExecutionGuard validates state
→ RuntimeTruthEngine runs ordered verification tiers
→ VerificationEngine builds immutable VerificationResult
→ VERIFIED ? success allowed
→ FAILED ? retry
→ retries exhausted ? RecoveryOrchestrator
→ rollback
→ verify restore
→ FAILED visibly
→ evidence report generated

ONLY:
VerificationStatus.VERIFIED
may return success.

==================================================
REQUIRED MODULE STRUCTURE
==================================================

Refactor/create:

/governance
  fail-closed-policy.ts
  execution-guard.ts

/runtime-truth
  runtime-truth-engine.ts
  runtime-observer.ts
  runtime-assertions.ts

/verification
  verification-engine.ts
  verification-result.ts
  verification-policy.ts
  verification-errors.ts

/recovery
  recovery-orchestrator.ts
  rollback-manager.ts
  checkpoint-validator.ts

==================================================
MODULE DESIGN REQUIREMENTS
==================================================

Every file must contain:
- single responsibility
- typed interfaces
- immutable outputs
- explicit inputs/outputs
- no side-effect coupling
- no hidden global state

Every module must declare:
- responsibility
- inputs
- outputs
- failure modes
- dependency graph
- state transitions

==================================================
RUNTIME TRUTH REQUIREMENTS
==================================================

Replace ALL heuristic verification.

MANDATORY deterministic checks:

TIER 1 — STATIC
- import graph validation
- circular dependency detection
- file existence verification

TIER 2 — BUILD
- tsc --noEmit subprocess
- npm ls --json subprocess

TIER 3 — RUNTIME
- kill -0 PID
- 3 consecutive HTTP 200 probes
- fatal log detection

TIER 4 — BEHAVIORAL
- Playwright DOM verification
- no React error boundary
- route coverage validation

NO:
- log scanning as source of truth
- single HTTP probe
- score-based validation
- warning-based passes

==================================================
VERIFICATION RULES
==================================================

1. VerificationResult immutable via Object.freeze()

2. Replace boolean ok:true/false with:

VerificationStatus:
- VERIFIED
- FAILED
- BLOCKED
- UNKNOWN
- ROLLED_BACK

3. Remove:
- PARTIAL
- WARNING
- EXHAUSTED_OK

4. Verification exhaustion MUST:
- throw VerificationExhaustedError
- trigger rollback
- NEVER return success

==================================================
RECOVERY REQUIREMENTS
==================================================

RecoveryOrchestrator must:

1. select Grade A/B checkpoints only
2. execute rollback
3. run post-restore verification
4. retry alternate checkpoints if restore invalid
5. halt if all checkpoints fail

Rollback success ≠ run success.

ROLLED_BACK is a FAILED terminal state.

==================================================
CHECKPOINT GRADING
==================================================

Grade A:
- git SHA
- tsc verified
- HTTP verified

Grade B:
- git SHA
- tsc verified

Grade C:
- git SHA only

Grade D:
- metadata only

Recovery minimum:
Grade B.

==================================================
GOVERNANCE REQUIREMENTS
==================================================

ExecutionGuard must enforce:

- no success without VERIFIED
- no silent no_tool_calls exits
- no exhausted success paths
- no invalid state transitions
- no unverified completion

If violation occurs:
throw PolicyViolationError.

==================================================
STATE MACHINE
==================================================

Allowed flow only:

PENDING
→ RUNNING
→ VERIFYING
→ VERIFIED → DONE

OR

→ FAILED
→ RECOVERING
→ ROLLED_BACK
→ FAILED

OR

→ HALTED

FORBIDDEN:
VERIFYING → DONE without VERIFIED

==================================================
TYPE SAFETY REQUIREMENTS
==================================================

Create strongly typed interfaces for:

- VerificationResult
- RuntimeSnapshot
- CheckResult
- RecoveryResult
- AgentRunResult
- EvidenceEntry
- TierResult

NO:
- any
- loosely typed states
- stringly-typed status logic

==================================================
DEPENDENCY RULES
==================================================

FORBIDDEN:

runtime-truth → agents
verification → planner
recovery → planner
governance → memory

ALLOWED:

agents → governance
agents → verification
verification → runtime-truth
recovery → runtime-truth

==================================================
EVIDENCE REQUIREMENTS
==================================================

Every failure must produce:

EvidenceReport:
- classification
- failed checks
- subprocess outputs
- runtime snapshot
- modified files
- last verified state

No silent failures allowed.

==================================================
OUTPUT REQUIREMENTS
==================================================

Generate:

1. Complete refactor architecture
2. File-by-file implementation plan
3. Deterministic state machine
4. Verification lifecycle
5. Recovery lifecycle
6. Governance lifecycle
7. Runtime truth pipeline
8. Dependency graph
9. Typed interfaces
10. Rollback lifecycle
11. Evidence pipeline
12. Migration checklist
13. Exact fake-success removals
14. Exact code replacement strategy
15. Fail-closed enforcement strategy

IMPORTANT:
Do NOT create oversized files.
Do NOT centralize everything into one manager.
Do NOT use inheritance-heavy architecture.
Do NOT allow mutable verification states.
Do NOT trust LLM self-reporting.

FINAL RULE:

If verification fails,
the system MUST fail visibly,
never succeed politely.