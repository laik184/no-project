MASTER PROMPT — FIX CRITICAL ARCHITECTURE VIOLATIONS

You are a senior distributed systems + AI orchestration architect.

Your task is to perform a STRICT architecture correction pass on this codebase.

CRITICAL:
You must FIRST perform a DEEP SCAN of the existing architecture before making ANY modifications.

You are ONLY allowed to touch the files explicitly mentioned below.

DO NOT create extra architecture.
DO NOT add unnecessary abstractions.
DO NOT invent new systems.
DO NOT create extra agents.
DO NOT modify unrelated files.
DO NOT change tool execution semantics.
DO NOT redesign the entire framework.

Your job is ONLY to fix the critical orchestration discipline violations.

---

PRIMARY GOAL

Fix these CRITICAL architecture violations:

1. Tool → Agent activation
2. Layer inversion
3. Fake bridge tools
4. Tool ownership confusion
5. Registry pollution

---

TARGET CLEAN ARCHITECTURE

Correct architecture MUST become:

Orchestrator
→ Agent Coordinator
→ Agent
→ Agent Internal Loop
→ Dispatcher Client
→ Tool Dispatcher
→ Tool
→ Execution

STRICT RULE:

✅ Orchestrator → Agent

✅ Agent → Tool

❌ Tool → Agent

❌ Tool → Orchestrator

❌ Tool layer must NEVER activate agents

---

REQUIRED FIXES

FIX #1 — REMOVE TOOL → AGENT ACTIVATION

REMOVE all orchestration bridge patterns like:

- orchestrate_browse
- orchestrate_terminal
- orchestrate_verifier
- orchestrate_executor
- any similar fake orchestration tool

These are NOT real tools.

These are architecture violations.

Tool layer must NEVER activate agents.

---

FIX #2 — DIRECT AGENT OWNERSHIP

"server/orchestration/coordination/agent-coordinator.ts"

MUST directly import and manage ALL agents.

Agent coordinator becomes the ONLY orchestration authority.

It MUST directly import these agents:

import { browserAgent } from "@/server/agents/browser/browser-agent";
import { coderxAgent } from "@/server/agents/coderx/coderx-agent";
import { executorAgent } from "@/server/agents/executor/executor-agent";
import { filesystemAgent } from "@/server/agents/filesystem/filesystem-agent";
import { plannerAgent } from "@/server/agents/planner/planner-agent";
import { supervisorAgent } from "@/server/agents/supervisor/supervisor-agent";
import { terminalAgent } from "@/server/agents/terminal/terminal-agent";
import { verifierAgent } from "@/server/agents/verifier/verifier-agent";

NO dynamic orchestration through tools.

NO string-based fake orchestration.

NO registry-driven agent activation.

---

FIX #3 — REMOVE REGISTRY POLLUTION

Tool registry must ONLY contain REAL executable tools.

Registry must NEVER contain:

- orchestrators
- workflows
- agents
- orchestration bridges
- orchestration wrappers

Registry should ONLY contain execution primitives like:

- write_file
- read_file
- run_command
- browser_navigate
- screenshot
- patch_file

---

FIX #4 — REMOVE LAYER INVERSION

Current bad flow:

Orchestrator
→ Tool
→ Agent

Must become:

Orchestrator
→ Agent
→ Tool

Agent layer owns workflow execution.

Tool layer owns primitive execution ONLY.

---

FIX #5 — REMOVE FAKE BRIDGE TOOLS

Delete/remove/refactor all fake bridge tools that only proxy into agents.

Bridge tools are architecture violations.

If a file exists ONLY to trigger an agent:
REMOVE IT.

---

STRICT FILE SCOPE

ONLY modify:

server/orchestration/coordination/agent-coordinator.ts

server/agents/browser/browser-agent.ts
server/agents/coderx/coderx-agent.ts
server/agents/executor/executor-agent.ts
server/agents/filesystem/filesystem-agent.ts
server/agents/planner/planner-agent.ts
server/agents/supervisor/supervisor-agent.ts
server/agents/terminal/terminal-agent.ts
server/agents/verifier/verifier-agent.ts

DO NOT touch anything else unless absolutely required for compilation.

DO NOT redesign tools.

DO NOT redesign dispatcher.

DO NOT redesign registry.

---

DEEP SCAN PHASE (MANDATORY)

Before modifying anything:

1. Deep scan orchestration flow
2. Deep scan tool registry
3. Deep scan dispatcher usage
4. Deep scan bridge orchestration tools
5. Deep scan agent activation paths
6. Detect layer inversion
7. Detect orchestration pollution
8. Detect registry pollution
9. Detect tool→agent dependencies
10. Detect indirect orchestration wrappers

Produce a detailed architecture analysis FIRST.

---

FIX PHASE

After scan:

1. Refactor orchestration flow
2. Replace tool-based agent activation
3. Add direct agent imports
4. Add direct agent execution ownership
5. Remove orchestration bridge dependencies
6. Remove fake orchestration mappings
7. Clean registry references
8. Ensure no tool imports agents
9. Ensure no registry entry points to agents
10. Ensure orchestration only happens at orchestration layer

---

VALIDATION PHASE

After fixes:

Verify:

- no Tool → Agent activation exists
- no bridge orchestration tools exist
- no registry pollution exists
- orchestration flow is clean
- all agents are directly owned by agent-coordinator
- no indirect orchestration wrappers remain
- dispatcher only dispatches tools
- agents only own workflows
- tools only own primitive execution

---

REPORT PHASE (MANDATORY)

Generate TWO reports.

---

REPORT 1 — BEFORE ARCHITECTURE REPORT

Include:

- current orchestration flow
- current violations
- current bridge tools
- current layer inversion
- current registry pollution
- current execution ownership issues
- current agent activation path

---

REPORT 2 — AFTER ARCHITECTURE REPORT

Include:

- corrected orchestration flow
- removed violations
- removed bridge tools
- fixed ownership boundaries
- corrected layering
- final clean execution path
- final clean orchestration model

---

FINAL TARGET

FINAL FLOW MUST BE:

orchestration-loop.ts
→ agent-coordinator.ts
→ browser-agent.ts
→ browser-loop.ts
→ dispatcher-client.ts
→ tool-dispatcher.ts
→ browser tools

NOT:

orchestration
→ tool dispatcher
→ bridge tool
→ agent

---

IMPORTANT

DO NOT:

- add overengineering
- add new frameworks
- add unnecessary factories
- add dependency injection systems
- add plugin systems
- add microservice abstractions
- add event buses
- add new orchestration engines

Keep it:

- clean
- direct
- disciplined
- layered
- production realistic

This is a SURGICAL architecture correction pass.