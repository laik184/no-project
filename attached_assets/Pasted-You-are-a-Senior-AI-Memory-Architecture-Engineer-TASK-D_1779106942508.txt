You are a Senior AI Memory Architecture Engineer.

TASK:
Deep-scan the entire codebase and FIX the conversation continuity problem.

BUG ID:
C8 — No conversation continuity

CURRENT PROBLEM:
Every new agent run starts with a fresh messages array.
The AI forgets:
- previous goals
- previous tool calls
- previous architecture decisions
- previously generated files
- runtime state
- user preferences
- unfinished tasks

As a result:
- the AI repeats work
- rewrites files incorrectly
- loses project context
- breaks architecture consistency
- behaves statelessly

━━━━━━━━━━━━━━━━━━━━
MAIN GOAL
━━━━━━━━━━━━━━━━━━━━

Implement a REAL persistent conversation memory system.

The agent must:
- remember previous runs
- remember project architecture
- remember generated files
- remember user decisions
- remember failed attempts
- remember runtime state
- continue unfinished work

━━━━━━━━━━━━━━━━━━━━
IMPORTANT REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

1. FIRST — DEEP SCAN

Deeply inspect:
- chat system
- run lifecycle
- tool-loop
- pipeline
- SSE flow
- database schema
- file system
- sandbox
- tool execution history
- agent state handling
- message lifecycle

Find:
- where messages reset
- where memory is lost
- where runs disconnect
- where context gets destroyed
- where state is recreated

━━━━━━━━━━━━━━━━━━━━
FOCUS FILES
━━━━━━━━━━━━━━━━━━━━

Scan deeply:
- server/chat/
- server/agents/core/tool-loop/
- server/chat/run/
- server/orchestration/
- shared/schema.ts
- client/src/hooks/
- client/src/components/chat/
- event bus
- runtime lifecycle
- tool execution flow

━━━━━━━━━━━━━━━━━━━━
ROOT PROBLEMS TO DETECT
━━━━━━━━━━━━━━━━━━━━

Detect:
- local-only message arrays
- in-memory-only state
- missing persistence
- missing conversation history loading
- missing context restoration
- missing project memory
- missing run continuation
- missing checkpoint memory
- missing tool history
- missing summarization system

━━━━━━━━━━━━━━━━━━━━
IMPLEMENT A REAL MEMORY SYSTEM
━━━━━━━━━━━━━━━━━━━━

Design and implement:

1. Persistent Conversation History
Store:
- user prompts
- assistant messages
- tool calls
- tool results
- errors
- decisions
- summaries

2. Project Context Memory
Create:
- .nura/context.md
- .nura/architecture.md
- .nura/tasks.md

Agent must auto-read these before every run.

3. Run Continuation
If previous run stopped:
- restore context
- restore unfinished tasks
- continue execution

4. Context Compression
When messages become large:
- summarize older messages
- preserve critical architecture decisions
- preserve file changes
- preserve unresolved issues

5. Memory Loader
Before runAgentLoop():
- load previous conversation
- load project memory
- load unresolved tasks
- inject into system prompt

6. Memory Writer
After every important step:
- update context.md
- update tasks.md
- persist important reasoning

━━━━━━━━━━━━━━━━━━━━
STRICT ENGINEERING RULES
━━━━━━━━━━━━━━━━━━━━

VERY IMPORTANT:

- Keep code modular
- Keep functions small
- Max 250 lines per file
- Clear folder structure
- Low coupling
- High cohesion
- No god classes
- No giant hooks
- No duplicated memory logic
- No global mutable state
- Use dependency injection where possible

━━━━━━━━━━━━━━━━━━━━
ARCHITECTURE REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Create clean structure like:

server/
  memory/
    conversation/
    project-memory/
    summarizer/
    context-loader/
    task-memory/
    architecture-memory/

Each module must have:
- single responsibility
- clean interfaces
- isolated logic

━━━━━━━━━━━━━━━━━━━━
IMPORTANT FEATURES
━━━━━━━━━━━━━━━━━━━━

The AI must remember:
- current framework
- package choices
- database schema
- file structure
- architecture decisions
- previous bugs
- failed fixes
- user-approved approaches
- pending todos
- runtime issues

━━━━━━━━━━━━━━━━━━━━
OUTPUT REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Root cause analysis
2. Exact files causing memory loss
3. Exact line references
4. Current broken flow
5. Correct memory architecture
6. Refactor plan
7. New folder structure
8. New services/modules
9. Database changes needed
10. Context compression strategy
11. Run continuation strategy
12. Step-by-step implementation plan
13. Safety checks
14. Performance considerations
15. Scalability considerations

━━━━━━━━━━━━━━━━━━━━
IMPORTANT
━━━━━━━━━━━━━━━━━━━━

DO NOT:
- rewrite entire architecture unnecessarily
- create giant files
- create tightly coupled services
- use hacky global variables
- add fake memory

ONLY:
- implement REAL persistent memory
- preserve architecture cleanliness
- keep system scalable
- keep modules maintainable
- make AI truly stateful across runs