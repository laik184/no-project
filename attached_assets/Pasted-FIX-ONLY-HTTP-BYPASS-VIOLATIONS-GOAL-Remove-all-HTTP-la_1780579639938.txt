FIX ONLY — HTTP BYPASS VIOLATIONS

GOAL:

Remove all HTTP layer bypasses.

Controllers, routes, mappers, realtime adapters, and UI-facing modules must not directly access repositories or infrastructure when a service layer exists.

Do not change behavior.
Do not rename APIs.
Do not change request/response contracts.
Do not create new features.

==================================================
SCAN TARGETS
==================================================

server/file-explorer/**
server/chat/**
server/realtime/**
server/controllers/**
server/routes/**

==================================================
FIX 1 — HTTP → REPOSITORY BYPASS
==================================================

FILE:

server/file-explorer/mappers/tree.mapper.ts

CURRENT PROBLEM:

tree.mapper.ts imports:

server/repositories/file-system/**

HTTP layer is directly touching repositories.

RULE:

HTTP
  ↓
Service
  ↓
Repository

NOT:

HTTP
  ↓
Repository

ACTION:

1. Remove all repository imports from tree.mapper.ts

2. Identify repository methods being called.

3. Route those calls through the appropriate filesystem service.

EXPECTED:

tree.mapper.ts
      ↓
filesystem service
      ↓
repository

==================================================
FIX 2 — REALTIME → INFRASTRUCTURE REVIEW
==================================================

FILES:

server/file-explorer/realtime/file-publisher.ts

server/file-explorer/realtime/file-subscriber.ts

CURRENT REVIEW ITEM:

Direct infrastructure access:

bus
sseManager
TOPIC

ACTION:

1. Scan these files.

2. If they directly import low-level infrastructure internals
   beyond event publishing/subscription responsibilities,
   move that logic into a dedicated realtime adapter.

3. Keep publisher/subscriber focused on:

publish()
subscribe()

4. Infrastructure ownership should be isolated to:

file-explorer/realtime/realtime-adapter.ts

OR

file-explorer/realtime/event-gateway.ts

Only if required.

5. If current implementation is already a thin adapter,
   leave unchanged.

==================================================
FIX 3 — CONTROLLER BYPASS AUDIT
==================================================

Scan:

server/**/controllers/*.ts

Detect:

Repository imports
Infrastructure imports

RULE:

Controller
      ↓
Service

NOT:

Controller
      ↓
Repository

Controller
      ↓
Infrastructure

ACTION:

Move access through the appropriate service.

==================================================
FIX 4 — ROUTE BYPASS AUDIT
==================================================

Scan:

server/**/api/*
server/**/routes/*

Detect:

Repository imports
Infrastructure imports

RULE:

Route
    ↓
Controller

NOT:

Route
    ↓
Repository

Route
    ↓
Infrastructure

ACTION:

Move access through controller/service chain.

==================================================
FIX 5 — VALIDATE FINAL FLOW
==================================================

Ensure:

Routes
      ↓
Controllers
      ↓
Services
      ↓
Repositories
      ↓
Infrastructure

Realtime
      ↓
Event Adapter
      ↓
Infrastructure

No direct HTTP → Repository access.

No direct HTTP → Infrastructure access.

==================================================
CONSTRAINTS
==================================================

Do not modify business logic.

Do not change API endpoints.

Do not change payload shapes.

Do not move unrelated files.

Do not refactor unrelated modules.

Apply only bypass fixes required to satisfy the layered architecture.