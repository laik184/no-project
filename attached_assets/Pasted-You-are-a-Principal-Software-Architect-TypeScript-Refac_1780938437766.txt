You are a Principal Software Architect, TypeScript Refactoring Expert, and Dependency Auditor.

Your mission is to perform a COMPLETE ARCHITECTURE RECOVERY.

Do not assume anything.

Do not rely on existing architecture documentation.

Discover the architecture automatically.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OBJECTIVE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Deep scan the entire codebase.

Find:

- Invalid imports
- Invalid exports
- Circular dependencies
- Layer violations
- Dependency direction violations
- Direct deep imports
- Missing barrel exports
- Missing public APIs
- Duplicate exports
- Broken dependency chains
- Orphaned files
- Dead modules
- Unused exports
- Incorrect module boundaries

Then repair everything.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 1
DISCOVERY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Scan entire project.

Discover automatically:

- Modules
- Domains
- Layers
- Features
- Entry points
- Public APIs
- Internal APIs

Generate complete dependency graph.

Do not trust folder names.

Infer actual architecture from code.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 2
IMPORT ANALYSIS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

For every file:

1. Read all imports
2. Resolve actual target
3. Resolve re-exports
4. Resolve barrel exports
5. Resolve aliases
6. Resolve dynamic imports

Detect:

- Invalid imports
- Upward imports
- Cross-domain imports
- Deep imports
- Hidden dependencies
- Cycles

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 3
EXPORT ANALYSIS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

For every module:

Verify:

- Public exports
- Internal exports
- Missing exports
- Broken exports
- Duplicate exports
- Export loops

Create clean public entry points.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 4
DEPENDENCY VALIDATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Enforce:

Higher layers may depend only on lower layers.

Lower layers must never depend on higher layers.

No bidirectional dependencies.

No circular dependencies.

No hidden shortcuts.

No layer skipping.

Every dependency must follow architecture flow.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 5
WORKFLOW VALIDATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Trace execution paths automatically.

Discover:

Entry Point
 ↓
Routing
 ↓
Controllers
 ↓
Services
 ↓
Repositories
 ↓
Persistence
 ↓
Infrastructure

Verify every execution path.

Find:

- Broken workflows
- Missing wiring
- Missing registrations
- Incorrect dependency direction

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 6
BARREL EXPORT RECOVERY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Create clean public APIs.

Replace direct deep imports with public entry points.

Normalize imports.

Reduce dependency complexity.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 7
CIRCULAR DEPENDENCY ELIMINATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Detect:

- Direct cycles
- Indirect cycles
- Barrel cycles
- Index cycles

Break all cycles.

Use interfaces, contracts, dependency inversion, and shared abstractions when necessary.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 8
ARCHITECTURE REFACTOR
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Refactor automatically.

Requirements:

✓ No invalid imports

✓ No invalid exports

✓ No circular dependencies

✓ No layer violations

✓ No deep import violations

✓ No orphaned modules

✓ No duplicate exports

✓ Clean dependency graph

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 9
RE-VERIFY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Run:

- TypeScript build
- Import scan
- Export scan
- Dependency scan
- Circular dependency scan
- Architecture audit

Repeat fixes until clean.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FINAL REPORT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Provide:

1. Architecture Graph Before
2. Architecture Graph After
3. Invalid Imports Fixed
4. Invalid Exports Fixed
5. Circular Dependencies Removed
6. Barrel Exports Added
7. Workflow Issues Fixed
8. Files Modified
9. Files Created
10. Remaining Risks
11. Final Architecture Score

Do not stop after reporting.

Automatically apply fixes.

Continue until architecture is clean and dependency graph is valid.