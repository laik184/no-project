You are a Senior Software Architect, Reverse Engineer, and Runtime Dependency Auditor.

Perform a COMPLETE DEEP SCAN of the following modules:

1. server/chat/**
2. client/src/components/chat/**

Do not modify any file.

Do not create any file.

Do not refactor anything.

Only analyze and generate a detailed architecture report.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 1 — BACKEND CHAT SCAN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Scan the entire:

server/chat/**

directory.

For every file report:

- Full file path
- Purpose
- Responsibility
- Imported by which files
- Imports which files
- Public API exposed
- Runtime role

Identify:

- Entry points
- Controllers
- Routes
- Orchestrators
- Services
- Persistence layer
- Repositories
- Realtime layer
- SSE layer
- WebSocket layer
- Event publishers
- Event subscribers
- Checkpoint system
- Run lifecycle system
- Memory integration
- Agent integration
- Orchestration integration

Generate complete folder tree.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 2 — CHAT SERVICE DETECTION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Identify which files are acting as services.

For each file explain:

- Why it is a service
- Why it is not a service
- Whether it contains business logic
- Whether it should stay where it is
- Whether it belongs in server/services/chat/

Create table:

File Path
Current Role
Actual Role
Should Move?
Reason

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 3 — FRONTEND CHAT SCAN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Scan:

client/src/components/chat/**

Generate:

- Folder structure
- Component hierarchy
- Parent → Child relationships
- State ownership
- Event ownership
- Hooks usage
- Context usage
- Store usage

For every component report:

- Purpose
- Props
- Events emitted
- API calls triggered
- Backend endpoint consumed

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 4 — FRONTEND ↔ BACKEND MAPPING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Trace every interaction.

Example output:

ChatInput.tsx
↓
POST /api/chat/message
↓
chat.controller.ts
↓
chat-orchestrator.ts
↓
message-store.ts
↓
message.repository.ts
↓
db

Do this for:

- Send Message
- New Conversation
- Resume Conversation
- Delete Conversation
- Checkpoint Creation
- Checkpoint Restore
- Agent Run
- Stream Response
- SSE Updates
- WebSocket Updates
- File Attachments

Generate end-to-end flow diagrams.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 5 — LIFECYCLE AUDIT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Determine whether a full lifecycle system exists.

Identify lifecycle stages:

User Message
↓
Request Validation
↓
Conversation Load
↓
Run Creation
↓
Agent Execution
↓
Tool Calls
↓
Memory Update
↓
Checkpoint Save
↓
Streaming Response
↓
Completion
↓
Audit Logging

For each stage:

- File responsible
- Function responsible
- Trigger source
- Next stage

Report missing stages.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 6 — REPLIT-STYLE ARCHITECTURE AUDIT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Determine whether the system behaves like:

- Replit Agent
- Cursor Agent
- Devin-style workflow

Check:

- Run lifecycle
- Checkpoints
- Memory
- Streaming
- SSE
- WebSocket
- Persistence
- Recovery
- Agent coordination
- Multi-step execution
- Event bus integration

For each capability:

Status:
✅ Exists
⚠ Partial
❌ Missing

Provide evidence with exact file paths.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 7 — DEPENDENCY GRAPH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Generate actual dependency graph.

main.ts
↓
server/chat/index.ts
↓
...

Show every layer.

Highlight:

- Circular dependencies
- Service bypasses
- Repository bypasses
- Infrastructure bypasses
- Runtime risks

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 8 — DEAD CODE & UNUSED SYSTEMS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Identify:

- Unused files
- Unused exports
- Unused endpoints
- Unused services
- Unused persistence stores
- Unused frontend components
- Unused hooks

Provide evidence.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FINAL REPORT FORMAT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Folder Structure
2. File Responsibilities
3. Service Detection Report
4. Frontend Component Map
5. Frontend ↔ Backend Flow Map
6. Lifecycle Audit
7. Replit-Style Capability Audit
8. Dependency Graph
9. Dead Code Report
10. Architecture Scorecard
11. Critical Risks
12. Missing Systems
13. Recommended Future Chat Architecture

Use actual imports and actual code only.

No assumptions.

No guesses.

Provide exact file paths and exact function names as evidence.