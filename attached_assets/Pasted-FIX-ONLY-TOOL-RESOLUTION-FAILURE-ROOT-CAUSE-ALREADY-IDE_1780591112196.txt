FIX ONLY — TOOL RESOLUTION FAILURE

ROOT CAUSE ALREADY IDENTIFIED.

Do NOT perform another architecture audit.

Do NOT generate reports.

Do NOT refactor unrelated code.

Do NOT redesign agents.

Only fix the confirmed tool-resolution failure path.

==================================================
CONFIRMED PROBLEMS

PROBLEM 1

File:

server/agents/planner/planning/task-planner.ts

Issue:

Planner generates synthetic tool names:

execute_frontend_task
execute_backend_task
execute_api_task
execute_database_task

These names are not registered in Tool Registry.

==================================================

PROBLEM 2

File:

server/agents/executor/planning/tool-selection.ts

Issue:

A valid CODING_TOOLS mapping already exists.

But execution flow is not using it.

Mapping is effectively dead code.

==================================================

PROBLEM 3

File:

server/tools/registry/tool-resolver.ts

Issue:

Resolver correctly throws ToolNotFoundError.

Failure occurs because invalid names reach resolver.

Resolver itself is not broken.

==================================================
PHASE 1 — DEEP SCAN BEFORE FIX

Trace:

task-planner.ts
↓
executor
↓
tool-selection.ts
↓
tool-dispatcher
↓
tool-resolver.ts
↓
tool-registry

Find:

1. Exact planner output.

2. Exact tool names generated.

3. Exact registered coding tool names.

4. Exact CODING_TOOLS mapping.

5. Exact point where mapping is skipped.

6. Exact execution path.

==================================================
PHASE 2 — FIX PLANNER OUTPUT

File:

server/agents/planner/planning/task-planner.ts

Remove generation of synthetic names:

execute_frontend_task
execute_backend_task
execute_api_task
execute_database_task

Planner must emit only valid registered tool identifiers.

Use actual registered coding tools.

Planner output must match registry exactly.

==================================================
PHASE 3 — FIX TOOL SELECTION

File:

server/agents/executor/planning/tool-selection.ts

Inspect:

CODING_TOOLS

If mapping exists:

Use it.

Do not leave mapping unused.

Ensure planner task types resolve to actual registered tools.

==================================================
PHASE 4 — VALIDATE REGISTRY

Scan:

server/tools/registry/**
server/tools/coding/**
server/tools/codegen/**

Verify:

Every planner-emitted tool name exists.

Every emitted tool is registered.

Every registered tool is exportable.

==================================================
PHASE 5 — VALIDATE EXECUTION

Test scenarios:

Create HTML file

Create landing page

Create React component

Create dashboard

Create REST API

Create database schema

For every scenario verify:

Planner
↓
Registered Tool Name
↓
Resolver
↓
Tool Found
↓
Tool Executed

No ToolNotFoundError.

==================================================
PHASE 6 — CLEANUP

If dead mappings remain:

Remove them.

If duplicate naming systems exist:

Consolidate them.

Single source of truth:

Tool Registry.

==================================================
CONSTRAINTS

Do not modify business logic.

Do not modify orchestration flow.

Do not modify agent architecture.

Do not add services.

Do not add repositories.

Do not change tool behavior.

Only align:

Planner Names
↓
Tool Selection
↓
Tool Registry

and make execution succeed.