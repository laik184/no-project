You are a Principal Distributed Systems Architect.

Your mission is to DESIGN + IMPLEMENT a production-grade:

RESULT AGGREGATOR SYSTEM

for the NURA-X autonomous multi-agent backend.

━━━━━━━━━━━━━━━━━━━━
CRITICAL CONTEXT
━━━━━━━━━━━━━━━━━━━━

Current architecture already supports:
✅ DAG execution
✅ multi-agent orchestration
✅ parallel wave execution
✅ runtime coordination
✅ fail-closed verification

BUT:

❌ NO centralized result aggregation exists
❌ NO parallel result merge system exists
❌ NO unified execution collapse layer exists

This causes:
- conflicting file writes
- inconsistent parallel outputs
- unstable multi-agent state
- unsafe parallel execution
- non-deterministic final outputs

━━━━━━━━━━━━━━━━━━━━
PRIMARY GOAL
━━━━━━━━━━━━━━━━━━━━

Build a SAFE, DETERMINISTIC, FAIL-CLOSED:

Result Aggregation Layer

that:
- collects parallel agent outputs
- merges execution results
- resolves execution state
- validates consistency
- prepares unified final state
- blocks unsafe completion

━━━━━━━━━━━━━━━━━━━━
MANDATORY ENGINEERING RULES
━━━━━━━━━━━━━━━━━━━━

1. HIGH COHESION / LOW COUPLING
- every module has ONE responsibility
- no tight dependencies
- dependency injection preferred

2. FILE SIZE LIMIT
- NO file above 250 lines
- split logically
- extract helpers/services/types

3. CLEAN PLACEMENT
ALL files MUST be placed logically.

TARGET ROOT:

server/quantum/aggregation/

4. ROOT CAUSE ENGINEERING
- solve architecture issue
- NOT superficial patching
- analyze true merge problem

5. PRODUCTION STANDARDS
- strongly typed
- scalable
- readable naming
- secure
- testable
- deterministic

6. CODE REVIEW QUALITY
- self-review architecture
- remove dead code
- avoid hacks
- avoid hidden side effects

7. FAIL-CLOSED DESIGN
- NO completion without validation
- unsafe merges MUST fail
- corrupted state MUST block completion

8. TELEMETRY REQUIRED
Every:
- aggregation
- merge
- conflict
- retry
- validation
- collapse
MUST emit telemetry events.

━━━━━━━━━━━━━━━━━━━━
TARGET ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━

CREATE:

server/quantum/aggregation/
├── result-aggregator.ts
├── merge-engine.ts
├── conflict-detector.ts
├── aggregation-validator.ts
├── collapse-engine.ts
├── aggregation-telemetry.ts
├── aggregation-types.ts
├── merge-strategies/
│   ├── union-merge.ts
│   ├── precedence-merge.ts
│   ├── confidence-merge.ts
│   └── ast-safe-merge.ts
├── state/
│   ├── aggregation-store.ts
│   └── aggregation-session.ts
└── index.ts

━━━━━━━━━━━━━━━━━━━━
MAIN RESPONSIBILITIES
━━━━━━━━━━━━━━━━━━━━

# RESULT AGGREGATOR

Collect:
- parallel agent outputs
- tool results
- runtime observations
- verification outputs
- generated patches
- file mutations

by:
- runId
- waveId
- agentId
- executionId

━━━━━━━━━━━━━━━━━━━━
# MERGE ENGINE

Merge:
- file updates
- patch outputs
- metadata
- runtime evidence
- execution summaries

Support:
✅ union merge
✅ precedence merge
✅ confidence merge
✅ AST-safe merge

━━━━━━━━━━━━━━━━━━━━
# CONFLICT DETECTOR

Detect:
❌ same-file writes
❌ patch overlaps
❌ stale writes
❌ invalid state merges
❌ runtime ownership conflicts
❌ duplicate execution outputs

━━━━━━━━━━━━━━━━━━━━
# AGGREGATION VALIDATOR

Validate:
✅ merged state consistency
✅ no corrupted outputs
✅ deterministic merge order
✅ valid runtime evidence
✅ valid file ownership
✅ verification integrity

If invalid:
❌ BLOCK COMPLETION

━━━━━━━━━━━━━━━━━━━━
# COLLAPSE ENGINE

Convert:
N parallel outputs
→ ONE final execution state

This is the:
“superposition collapse”
layer.

━━━━━━━━━━━━━━━━━━━━
# TELEMETRY

Emit:
aggregation.started
aggregation.completed
aggregation.failed
merge.conflict
merge.retry
collapse.completed
collapse.failed

Integrate with:
server/orchestration/telemetry/
server/infrastructure/events/

━━━━━━━━━━━━━━━━━━━━
REQUIRED IMPLEMENTATION DETAILS
━━━━━━━━━━━━━━━━━━━━

# 1. AGGREGATION STORE

Implement:
- active aggregation sessions
- run-based isolation
- wave tracking
- result tracking

Use:
Map<runId, AggregationSession>

━━━━━━━━━━━━━━━━━━━━
# 2. DETERMINISTIC MERGE ORDER

Merge order MUST be deterministic.

Priority:
1. verified outputs
2. higher confidence
3. orchestration precedence
4. timestamp fallback

━━━━━━━━━━━━━━━━━━━━
# 3. FILE CONFLICT SAFETY

Before merge:
detect:
- overlapping file mutations
- same-file writes
- stale diffs

If unsafe:
❌ reject merge
❌ emit telemetry
❌ escalate to orchestration

━━━━━━━━━━━━━━━━━━━━
# 4. FAIL-CLOSED AGGREGATION

If:
- unresolved conflict
- corrupted state
- invalid runtime evidence
- merge inconsistency

THEN:
❌ block completion
❌ block verification
❌ emit critical telemetry

━━━━━━━━━━━━━━━━━━━━
# 5. EVENT BUS INTEGRATION

Integrate with:

server/infrastructure/events/bus.ts

Required events:
- quantum.aggregation.started
- quantum.aggregation.completed
- quantum.merge.conflict
- quantum.collapse.completed
- quantum.validation.failed

━━━━━━━━━━━━━━━━━━━━
# 6. ORCHESTRATION INTEGRATION

Integrate into:

server/engine/graph/graph-engine.ts

After:
parallel-runner completes wave

Flow:

wave completed
→ ResultAggregator.collect()
→ MergeEngine.merge()
→ ConflictDetector.validate()
→ CollapseEngine.finalize()
→ AggregationValidator.verify()
→ graph continues

━━━━━━━━━━━━━━━━━━━━
# 7. TELEMETRY INTEGRATION

Integrate:
server/orchestration/telemetry/

Trace:
- aggregation duration
- merge conflicts
- retry counts
- collapse duration
- failed merges

━━━━━━━━━━━━━━━━━━━━
# 8. SECURITY + SAFETY

Prevent:
❌ arbitrary overwrite
❌ race-condition merges
❌ unsafe runtime state mutation
❌ stale execution overwrite
❌ invalid completion authorization

━━━━━━━━━━━━━━━━━━━━
# 9. TESTING

Create:
server/quantum/aggregation/__tests__/

Test:
✅ parallel merge
✅ deterministic ordering
✅ conflict rejection
✅ fail-closed behavior
✅ telemetry emission
✅ invalid state blocking
✅ collapse correctness

━━━━━━━━━━━━━━━━━━━━
# 10. DOCUMENTATION

Generate:

RESULT_AGGREGATOR_IMPLEMENTATION_REPORT.md

Include:
1. Architecture overview
2. File tree
3. Merge lifecycle
4. Conflict resolution flow
5. Deterministic ordering strategy
6. Fail-closed guarantees
7. Telemetry integration
8. Event flow
9. Runtime safety
10. Integration points
11. Scalability analysis
12. Remaining risks
13. Future improvements
14. Replit-level similarity %
15. Quantum-readiness impact %

━━━━━━━━━━━━━━━━━━━━
IMPORTANT CONSTRAINTS
━━━━━━━━━━━━━━━━━━━━

❌ NO giant files
❌ NO architecture rewrite
❌ NO hidden side effects
❌ NO unsafe mutation
❌ NO circular dependencies
❌ NO direct runtime ownership mutation

✅ incremental integration only
✅ production-safe design
✅ deterministic behavior
✅ observable execution
✅ replay-safe aggregation

━━━━━━━━━━━━━━━━━━━━
FINAL GOAL
━━━━━━━━━━━━━━━━━━━━

Transform the backend from:

“parallel-capable architecture”

INTO:

“safe deterministic multi-agent parallel operating system”

by implementing the missing:

RESULT AGGREGATION LAYER