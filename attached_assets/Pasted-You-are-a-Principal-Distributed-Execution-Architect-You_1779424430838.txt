You are a Principal Distributed Execution Architect.

Your mission is to DESIGN, FIX, COMPLETE, and VERIFY a TRUE production-grade DAG EXECUTION ENGINE for an autonomous AI operating system.

ROOT TARGET:
server/engine/
server/orchestration/
server/agents/
server/runtime/
server/tools/

You MUST deeply inspect the current implementation first before changing anything.

━━━━━━━━━━━━━━━━━━━━
MAIN GOAL
━━━━━━━━━━━━━━━━━━━━

Build or complete a REAL DAG-based execution engine that supports:

✅ parallel execution
✅ dependency-aware execution
✅ deterministic orchestration
✅ retry isolation
✅ checkpoint recovery
✅ execution replay
✅ telemetry tracing
✅ event-driven coordination
✅ fail-closed execution
✅ multi-agent scheduling

Target:
Replit/Cursor-level autonomous execution orchestration.

━━━━━━━━━━━━━━━━━━━━
MANDATORY ENGINEERING RULES
━━━━━━━━━━━━━━━━━━━━

1. HIGH COHESION, LOW COUPLING
- every module = one responsibility
- no cross-domain mutations
- no giant orchestration god objects
- all communication event-driven or interface-based

2. FILE SIZE LIMIT
- NO file above 250 LOC
- split responsibilities aggressively
- create focused modules

3. TARGETED PLACEMENT
Correct placement mandatory:

server/
├── engine/dag/
├── engine/scheduler/
├── engine/execution/
├── engine/replay/
├── engine/checkpoints/
├── engine/telemetry/
├── engine/state/
└── orchestration/

DO NOT place DAG logic inside:
❌ agents/
❌ preview/
❌ runtime/
❌ tools/

4. ROOT CAUSE ANALYSIS REQUIRED
Before fixing:
- trace current execution flow
- identify exact failure points
- identify fake DAG implementations
- identify sequential bottlenecks
- identify hidden orchestration conflicts

NO symptom patching.

5. HIGH-LEVEL ENGINEERING STANDARDS
All code must be:
✅ scalable
✅ observable
✅ testable
✅ deterministic
✅ typed
✅ secure
✅ replayable

NO hacks.
NO shortcuts.
NO magic hidden state.

6. CODE REVIEW MANDATORY
Generate:
- architecture review
- dependency review
- execution safety review
- replay safety review
- concurrency safety review

7. FAIL-SAFE DESIGN
Execution MUST stop unless:
✅ dependency verified
✅ runtime healthy
✅ checkpoint saved
✅ policies passed
✅ verification passed

NO silent failures.

8. CONSISTENT TELEMETRY
MANDATORY telemetry events:

- dag.node.created
- dag.node.ready
- dag.node.started
- dag.node.completed
- dag.node.failed
- dag.retry
- dag.rollback
- dag.parallel.start
- dag.parallel.complete
- dag.execution.completed

ALL actions traceable.

━━━━━━━━━━━━━━━━━━━━
PHASE 1 — ULTRA DEEP SCAN
━━━━━━━━━━━━━━━━━━━━

FIRST:
deeply inspect current system.

Trace:
- orchestration flow
- tool-loop flow
- planner flow
- execution routing
- runtime dependencies
- verification dependencies
- event bus integration

Detect:
❌ fake DAG logic
❌ sequential execution pretending parallel
❌ hidden dependency chains
❌ execution bottlenecks
❌ missing node isolation
❌ retry corruption
❌ missing checkpoints
❌ replay gaps

━━━━━━━━━━━━━━━━━━━━
PHASE 2 — DAG ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━

Design proper DAG system:

ExecutionNode
- id
- dependencies
- status
- retries
- telemetry
- checkpoint
- executionFn

ExecutionGraph
- node registry
- dependency graph
- state graph
- execution ordering

Scheduler
- ready-node detection
- parallel scheduling
- retry isolation
- concurrency limits

ExecutionCoordinator
- orchestration integration
- runtime sync
- verification sync

ReplayEngine
- deterministic replay
- event replay
- checkpoint restore

CheckpointManager
- snapshot save
- rollback restore

━━━━━━━━━━━━━━━━━━━━
PHASE 3 — PARALLEL EXECUTION
━━━━━━━━━━━━━━━━━━━━

Implement TRUE parallel execution.

Verify:
✅ independent nodes execute concurrently
✅ dependency nodes wait correctly
✅ retries isolated
✅ no shared mutable state
✅ runtime safe
✅ event order deterministic

Example:

Node A:
install deps

Node B:
generate components

Node C:
setup routes

Must run IN PARALLEL if independent.

━━━━━━━━━━━━━━━━━━━━
PHASE 4 — FAILURE RECOVERY
━━━━━━━━━━━━━━━━━━━━

Implement:

✅ node retry isolation
✅ rollback recovery
✅ failed subtree restart
✅ checkpoint restore
✅ replayable execution

If:
Node B fails

System must:
- NOT restart whole graph
- isolate failure
- retry subtree only

━━━━━━━━━━━━━━━━━━━━
PHASE 5 — EVENT BUS INTEGRATION
━━━━━━━━━━━━━━━━━━━━

Integrate with:

server/infrastructure/events/

All DAG lifecycle must emit events.

Frontend must receive:
- graph progress
- node state
- retries
- failures
- parallel execution status

━━━━━━━━━━━━━━━━━━━━
PHASE 6 — TELEMETRY
━━━━━━━━━━━━━━━━━━━━

Create COMPLETE telemetry system.

Trace:
- node timing
- dependency timing
- parallel throughput
- retry counts
- bottlenecks
- critical path

Generate execution graphs.

━━━━━━━━━━━━━━━━━━━━
PHASE 7 — ORCHESTRATION INTEGRATION
━━━━━━━━━━━━━━━━━━━━

Integrate with:
- ToolLoopAgent
- Planner
- VerificationCoordinator
- RuntimeManager
- RecoveryManager

Flow:

Planner
→ DAG creation
→ Scheduler
→ Parallel execution
→ Runtime verification
→ CompletionAuthority

━━━━━━━━━━━━━━━━━━━━
PHASE 8 — VERIFICATION
━━━━━━━━━━━━━━━━━━━━

Verify:

✅ parallel execution REAL
✅ no race conditions
✅ no deadlocks
✅ no circular dependencies
✅ replay works
✅ retries deterministic
✅ telemetry complete
✅ checkpoints restore
✅ recovery works

Stress-test:
- 100+ nodes
- crash mid execution
- replay graph
- partial retries
- parallel runtime operations

━━━━━━━━━━━━━━━━━━━━
PHASE 9 — FINAL REPORT
━━━━━━━━━━━━━━━━━━━━

Generate:

DAG_EXECUTION_XRAY_REPORT.md

Must include:

1. Before/After Architecture
2. Exact DAG Files
3. Exact Scheduler Files
4. Exact Replay Files
5. Exact Telemetry Files
6. Parallel Execution Proof
7. Dependency Graph Examples
8. Critical Path Analysis
9. Retry Isolation Analysis
10. Replay Verification
11. Checkpoint Verification
12. Event Flow Graph
13. Concurrency Safety Analysis
14. Race Condition Analysis
15. Deadlock Analysis
16. Runtime Integration Analysis
17. Orchestration Integration Analysis
18. Replit DAG Similarity %
19. Production Readiness %
20. Missing Features
21. Future Improvements

━━━━━━━━━━━━━━━━━━━━
FINAL GOAL
━━━━━━━━━━━━━━━━━━━━

Transform current execution system into:

“deterministic, replayable, parallel autonomous execution engine”

NOT:
“fake sequential orchestration pretending to be DAG”.

Use evidence-based engineering only.
No assumptions.
No giant rewrites.
Safe incremental refactor only.