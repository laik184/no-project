You are a Senior Full-Stack Systems Engineer and Runtime Synchronization Auditor.

Your task is to deeply scan the entire codebase and FIX issue:

F7 — No Conflict Resolution System

━━━━━━━━━━━━━━━━━━━━
MAIN PROBLEM
━━━━━━━━━━━━━━━━━━━━

Currently:
- Agent writes files directly
- User edits files manually
- Monaco editor tracks dirty state locally
- AI writes bypass editor synchronization
- No conflict detection exists
- No merge strategy exists
- No approval flow exists

This creates:
- overwritten user changes
- silent file corruption
- lost edits
- AI/user desynchronization
- unsafe autonomous editing

━━━━━━━━━━━━━━━━━━━━
YOUR GOAL
━━━━━━━━━━━━━━━━━━━━

Build a REAL conflict resolution system inspired by:
- Replit
- Cursor
- VSCode source control
- Git merge conflict systems

The system must:
- detect conflicts
- block unsafe writes
- generate diffs
- allow approval/reject
- preserve user changes
- synchronize AI + editor state

━━━━━━━━━━━━━━━━━━━━
DEEP SCAN REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Scan FULL FLOW:

Frontend:
- file explorer
- Monaco editor
- dirtyFiles state
- aiFiles state
- save system
- editor synchronization
- SSE file events
- file refresh logic

Backend:
- file_write tool
- fs routes
- sandbox writes
- event bus
- file.change events
- diff queue
- artifact writes
- agent file generation flow

━━━━━━━━━━━━━━━━━━━━
FIND THESE PROBLEMS
━━━━━━━━━━━━━━━━━━━━

1. AI writes bypassing editor state
2. Dirty editor files overwritten silently
3. Missing file version tracking
4. Missing conflict detection
5. Missing merge flow
6. Missing diff generation
7. Missing approval system
8. Missing rollback capability
9. Missing optimistic locking
10. Missing concurrent write protection

━━━━━━━━━━━━━━━━━━━━
IMPORTANT FIX REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Build architecture with:

1. FILE VERSIONING
Every file must have:
- version
- updatedAt
- updatedBy
- checksum/hash

2. CONFLICT DETECTION
Detect when:
- user modified file locally
- AI modified same file
- versions mismatch
- checksum mismatch

3. DIFF SYSTEM
Generate:
- unified diff
- before/after comparison
- line-level changes

4. APPROVAL FLOW
Before overwrite:
- emit agent.diff event
- frontend shows diff modal
- user can:
  - approve
  - reject
  - merge manually

5. SAFE WRITE PIPELINE
Replace direct fs.writeFile with:
read current
→ compare versions
→ detect conflict
→ generate diff
→ await decision
→ apply safely

━━━━━━━━━━━━━━━━━━━━
STRICT ENGINEERING RULES
━━━━━━━━━━━━━━━━━━━━

VERY IMPORTANT:

1. KEEP CODE SIMPLE
- avoid overengineering
- avoid enterprise complexity
- clean architecture only

2. LOW COUPLING
- no circular dependencies
- no cross-layer chaos
- isolate responsibilities

3. HIGH COHESION
- each module has one responsibility
- editor sync separated
- diff logic separated
- write coordination separated

4. SMALL FILES ONLY
- NO FILE OVER 250 LINES
- split services properly
- split utilities properly

5. CLEAN FOLDER STRUCTURE
Create clear architecture like:

server/
  collaboration/
    conflict-manager.ts
    diff-service.ts
    version-service.ts
    merge-service.ts
    approval-bus.ts

client/
  collaboration/
    diff-modal.tsx
    conflict-store.ts
    merge-panel.tsx

━━━━━━━━━━━━━━━━━━━━
IMPORTANT ARCHITECTURE GOALS
━━━━━━━━━━━━━━━━━━━━

The final system should support:
- AI + user editing same file safely
- future multiplayer editing
- future multi-agent editing
- rollback
- checkpointing
- collaborative workflows

━━━━━━━━━━━━━━━━━━━━
CHECK FOR EXISTING SYSTEMS
━━━━━━━━━━━━━━━━━━━━

Search for:
- diff_queue
- artifacts
- dirtyFiles
- aiFiles
- file.change
- chokidar
- Monaco editor hooks
- save-file APIs
- fs routes
- sandbox utilities

Reuse existing infrastructure if possible.

DO NOT duplicate systems unnecessarily.

━━━━━━━━━━━━━━━━━━━━
OUTPUT FORMAT
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Root Cause Report
2. Current Broken Flow
3. Conflict Risk Analysis
4. Existing Systems Reusable
5. New Architecture Design
6. File Structure Plan
7. Backend Flow
8. Frontend Flow
9. Event Flow
10. Versioning Strategy
11. Diff Strategy
12. Approval Workflow
13. Rollback Strategy
14. Exact Files To Modify
15. Exact Files To Create
16. Step-by-Step Refactor Plan
17. Safety Checks
18. Concurrency Protections
19. Final Clean Architecture

━━━━━━━━━━━━━━━━━━━━
IMPORTANT
━━━━━━━━━━━━━━━━━━━━

DO NOT:
- rewrite whole system
- break existing runtime
- add huge frameworks
- add unnecessary abstractions

ONLY:
- fix conflict system
- preserve architecture
- improve synchronization
- make editing safe
- prepare for autonomous AI workflows

Act like you are fixing the file synchronization core of a billion-dollar AI IDE.