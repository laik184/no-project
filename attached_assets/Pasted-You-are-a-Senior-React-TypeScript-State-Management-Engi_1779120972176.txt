You are a Senior React + TypeScript State Management Engineer.

Your task is to deeply scan and FIX the following critical bug:

BUG ID:
F3 — Dirty State Mismatch

PROBLEM:
The editor dirty-state system is inconsistent and causes:
- lost user changes
- stale editor state
- incorrect save indicators
- AI-written files overwriting unsaved user edits
- editor memory desync
- file explorer state mismatch

MAIN GOAL:
Create a SAFE, CONSISTENT, and CENTRALIZED dirty-state architecture.

━━━━━━━━━━━━━━━━━━━━
SCAN THESE AREAS FIRST
━━━━━━━━━━━━━━━━━━━━

Frontend focus:
- client/src/components/file-explorer*
- client/src/components/editor*
- client/src/components/workspace*
- client/src/hooks/*
- client/src/context/*
- Monaco editor integration
- file save handlers
- file open/switch handlers
- AI file write event handlers
- SSE file-change subscriptions
- local editor state
- dirtyFiles state
- selectedFile state
- tabs state
- unsaved change logic

Backend focus:
- /api/save-file
- /api/read-file
- SSE /sse/files
- file.change events
- AI file_write tool events

━━━━━━━━━━━━━━━━━━━━
DETECT ALL PROBLEMS
━━━━━━━━━━━━━━━━━━━━

Find:
- duplicate dirty state sources
- stale React state
- race conditions
- async overwrite risks
- editor-tab desync
- AI writes bypassing dirty protection
- file switching without save protection
- unsaved state resets
- multiple competing stores
- stale closures
- incorrect useEffect dependencies
- optimistic update failures
- missing debouncing
- direct state mutation
- missing synchronization
- editor model mismatch
- Monaco model lifecycle leaks

━━━━━━━━━━━━━━━━━━━━
IMPORTANT FIX REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Create:
1. SINGLE SOURCE OF TRUTH
2. Predictable dirty-state flow
3. Safe save lifecycle
4. AI-write protection
5. Auto-sync system
6. Debounced persistence
7. File conflict prevention
8. Safe editor switching
9. Stable tab synchronization

━━━━━━━━━━━━━━━━━━━━
MANDATORY RULES
━━━━━━━━━━━━━━━━━━━━

RULES:
- use centralized state
- avoid duplicated dirty tracking
- prevent stale closures
- use immutable updates
- preserve user changes ALWAYS
- AI writes must NEVER overwrite unsaved user edits
- prevent save race conditions
- maintain predictable React flow

━━━━━━━━━━━━━━━━━━━━
CODE QUALITY RULES
━━━━━━━━━━━━━━━━━━━━

STRICT:
- clean folder structure
- modular architecture
- low coupling
- high cohesion
- reusable hooks
- reusable utilities
- max 250 lines per file
- separate business logic from UI
- avoid giant components
- avoid duplicated logic
- use TypeScript properly
- strong typing everywhere

━━━━━━━━━━━━━━━━━━━━
ARCHITECTURE TARGET
━━━━━━━━━━━━━━━━━━━━

Create architecture like:

editor-state/
├── dirty-state.store.ts
├── editor-sync.service.ts
├── file-conflict.guard.ts
├── autosave.manager.ts
├── editor-events.ts
└── hooks/
    ├── useDirtyState.ts
    ├── useEditorSync.ts
    └── useFilePersistence.ts

━━━━━━━━━━━━━━━━━━━━
EXPECTED FIXES
━━━━━━━━━━━━━━━━━━━━

Implement:
- centralized dirty-state store
- save queue
- autosave debounce
- file version tracking
- editor session tracking
- AI write conflict detection
- safe tab switching
- before-unload protection
- editor sync with SSE updates
- conflict resolution flow

━━━━━━━━━━━━━━━━━━━━
VERY IMPORTANT
━━━━━━━━━━━━━━━━━━━━

DO NOT:
- rewrite unrelated systems
- break Monaco integration
- create giant files
- introduce global chaos
- overwrite user changes

ONLY:
- deeply scan
- identify exact root causes
- fix architecture correctly
- create maintainable system
- preserve UX stability

━━━━━━━━━━━━━━━━━━━━
OUTPUT REQUIRED
━━━━━━━━━━━━━━━━━━━━

Generate:
1. Root cause analysis
2. Exact broken files
3. Dirty-state flow diagram
4. Conflict scenarios
5. Refactor architecture
6. New folder structure
7. Exact implementation plan
8. Safety protections
9. Final patched code
10. All modified files list

Act like you are fixing the editor architecture of Cursor/Replit-level IDE.