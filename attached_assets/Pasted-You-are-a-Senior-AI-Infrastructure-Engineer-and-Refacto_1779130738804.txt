You are a Senior AI Infrastructure Engineer and Refactoring Specialist.

TASK:
Deep-scan the entire codebase and find all problems related to:

F8 — No File Snapshots / No Recovery System

CURRENT PROBLEM:
The system has no checkpointing, snapshotting, rollback, or file recovery architecture.

Right now:
- AI writes files directly
- User edits files directly
- No backup before overwrite
- No recovery after corruption
- No restore point system
- No version snapshots
- No rollback mechanism
- No crash-safe restore flow

If the AI damages files during generation:
→ project is permanently corrupted.

━━━━━━━━━━━━━━━━━━━━
YOUR GOALS
━━━━━━━━━━━━━━━━━━━━

1. Deeply scan:
- chat system
- file explorer
- file write tools
- sandbox system
- runtime system
- pipeline
- orchestrator
- diff system
- artifact system
- git integration
- preview system

2. Detect:
- where files are written directly
- where overwrites happen
- missing backup points
- missing checkpoints
- missing recovery flows
- missing rollback logic
- dangerous direct fs.writeFile usage
- unsafe file replacement logic
- destructive file operations
- missing atomic writes
- missing crash recovery
- missing restore APIs

3. Find:
- all write flows
- all delete flows
- all rename flows
- all overwrite flows
- all temp-file usage
- all diff systems
- all artifact systems

━━━━━━━━━━━━━━━━━━━━
IMPORTANT REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Use HIGH CAUTION.
Use LOW COMPLEXITY.
Keep architecture CLEAN.
Avoid overengineering.

DO NOT:
- rewrite entire system
- introduce microservices
- add unnecessary abstractions
- break existing runtime
- create giant enterprise architecture

ONLY:
- add minimal safe recovery system
- preserve current architecture
- improve stability
- improve safety
- improve recoverability

━━━━━━━━━━━━━━━━━━━━
TARGET ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━

Create a SIMPLE snapshot system:

Before any destructive write:
1. Create snapshot
2. Store previous file version
3. Store metadata
4. Allow rollback

Simple structure:

.data/
  snapshots/
    project-{id}/
      timestamp/
        changed-files/

Add:
- restoreSnapshot()
- createSnapshot()
- listSnapshots()

━━━━━━━━━━━━━━━━━━━━
IMPORTANT CODE RULES
━━━━━━━━━━━━━━━━━━━━

VERY IMPORTANT:
- Keep every file UNDER 250 lines
- Maintain clear folder structure
- Use reusable utilities
- Avoid giant god-files
- Prefer small focused modules
- Keep naming consistent
- Follow existing architecture style

━━━━━━━━━━━━━━━━━━━━
ANALYSIS OUTPUT
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Snapshot Risk Report
2. Dangerous File Write Locations
3. Unsafe Direct Write Flows
4. Recovery Weaknesses
5. Rollback Failure Points
6. Missing Backup Architecture
7. Current File Lifecycle
8. Safe Snapshot Architecture
9. Minimal Refactor Plan
10. Exact Files To Modify
11. New Folder Structure
12. Snapshot Storage Strategy
13. Rollback Flow
14. Recovery APIs
15. Atomic Write Strategy
16. Crash Recovery Strategy

━━━━━━━━━━━━━━━━━━━━
IMPLEMENTATION REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Then implement:

SAFE FILE SYSTEM LAYER:
- safeWriteFile()
- safeReplaceFile()
- safeDeleteFile()

SNAPSHOT SYSTEM:
- createSnapshot()
- restoreSnapshot()
- listSnapshots()

RECOVERY SYSTEM:
- rollback latest snapshot
- restore damaged project
- recover deleted files

DIFF INTEGRATION:
- snapshot before overwrite
- snapshot before delete

━━━━━━━━━━━━━━━━━━━━
FINAL GOAL
━━━━━━━━━━━━━━━━━━━━

Transform the system from:

"Dangerous direct AI file mutation"

INTO:

"Recoverable autonomous coding system"

WITHOUT:
- breaking architecture
- adding complexity
- rewriting the platform