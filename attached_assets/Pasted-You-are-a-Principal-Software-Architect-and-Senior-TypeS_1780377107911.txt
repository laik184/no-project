You are a Principal Software Architect and Senior TypeScript Refactoring Engineer.

MISSION

Fix the architecture violations identified in the audit report.

DO NOT redesign the system.

DO NOT rewrite unrelated code.

DO NOT change runtime behavior.

ONLY remove the architecture violations while preserving existing functionality.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TARGET FILES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. server/agents/chat/chat-agent.ts

2. server/chat/orchestration/stream-manager.ts

3. server/chat/persistence/checkpoint-store.ts

4. server/agents/browser/core/browser-session.ts

5. ESLint configuration files

- .eslintrc.js
- .eslintrc.cjs
- eslint.config.js
- eslint.config.mjs

(use whichever exists)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FIX #1
REMOVE CHAT ↔ AGENT CYCLE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CURRENT PROBLEM

server/agents/chat/chat-agent.ts

imports:

server/chat/orchestration/stream-manager.ts

This creates an inverted dependency:

Chat Layer
↓
Chat Agent

AND

Chat Agent
↓
Chat Layer

This violates architecture ownership.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

REQUIRED FIX

Remove ALL imports from:

server/agents/chat/chat-agent.ts

to:

server/chat/*

Agent layer must never import chat layer.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

IMPLEMENTATION

Create a StreamWriter interface.

Example concept:

interface StreamWriter {
open(runId: string, projectId: number): void;
write(runId: string, chunk: string): void;
close(runId: string): string;
}

Do NOT hardcode this exact interface if existing code suggests a better shape.

Inject StreamWriter into runChatAgent().

Example:

runChatAgent({
runId,
projectId,
goal,
streamWriter
})

chat-agent must use only the interface.

It must not know about stream-manager.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Update:

server/chat/orchestration/chat-orchestrator.ts

to construct and inject the StreamWriter implementation.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FIX #2
STREAM MANAGER OWNERSHIP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File:

server/chat/orchestration/stream-manager.ts

Goal:

Remain owned by Chat layer.

Do NOT move it.

Do NOT expose it globally.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Requirements

- Export only a narrow interface.
- No agent imports.
- No tool imports.
- No orchestration imports.

Verify:

stream-manager becomes a pure chat-owned implementation.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FIX #3
REMOVE SHELL EXECUTION FROM CHAT LAYER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File:

server/chat/persistence/checkpoint-store.ts

Current problem:

Uses:

child_process.execFile()

or any shell execution.

Chat layer must not own process execution.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Find:

- execFile
- spawn
- exec
- execSync
- spawnSync

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Move shell/process logic into:

Preferred:

server/infrastructure/checkpoints/

OR

server/tools/terminal/

Choose whichever best matches current architecture.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

After refactor:

checkpoint-store.ts

must only:

- request checkpoint creation
- persist metadata
- read checkpoint data

It must not execute shell commands directly.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FIX #4
PLAYWRIGHT SAFE INITIALIZATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

File:

server/agents/browser/core/browser-session.ts

Current risk:

Playwright initializes during module import.

If Playwright fails:

entire agent layer can crash during startup.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Required fix:

Convert Playwright initialization to lazy loading.

Bad:

import { chromium } from "playwright";

const browser = await chromium.launch();

Good:

Initialize only when browser tool executes.

Example:

async function getBrowser() {
const { chromium } = await import("playwright");
...
}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Requirements

- No browser startup at module load.
- No side effects at import time.
- Preserve existing functionality.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FIX #5
ARCHITECTURE GUARD
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Create ESLint protection.

Goal:

Prevent future dependency violations.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

FORBIDDEN IMPORTS

server/orchestration/**
cannot import
server/chat/**

server/agents/**
cannot import
server/chat/**

server/tools/**
cannot import
server/chat/**

server/tools/**
cannot import
server/orchestration/**

server/repositories/**
cannot import
server/services/**

server/services/**
cannot import
server/controllers/**

━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Implement using:

no-restricted-imports

or

eslint-plugin-boundaries

Choose the simplest production-safe solution.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VALIDATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

After implementation generate:

1. Modified files list

2. Dependency graph before

3. Dependency graph after

4. Removed cycles

5. Architecture violations fixed

6. Remaining risks

7. Production readiness score

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
RULES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- No TODOs
- No placeholders
- No pseudocode
- No fake implementations
- No dead code
- No duplicate logic
- Preserve existing APIs where possible
- Preserve existing runtime behavior
- Keep files under project conventions
- Use actual project types and imports

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SUCCESS CRITERIA
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

After refactor:

✅ Agents do not import Chat

✅ Chat owns StreamManager

✅ Chat does not execute shell commands

✅ Browser agent does not initialize Playwright on import

✅ ESLint blocks future architecture violations

✅ No circular dependency remains

✅ No runtime behavior regressions