You are a Senior Realtime Systems Engineer and Architecture Auditor.

Your task is to deeply scan the entire codebase and FIX issue:

F5 — No collaborative state / no live sync architecture

━━━━━━━━━━━━━━━━━━━━
MAIN GOAL
━━━━━━━━━━━━━━━━━━━━

Find why:
- agent writes
- user edits
- Monaco editor state
- file explorer
- preview
- SSE updates
- runtime changes

are NOT synchronized in real-time.

The system currently has:
- stale editor state
- delayed refresh
- conflicting writes
- unsynced file updates
- no collaborative architecture
- no shared realtime state
- race conditions between AI + user
- broken live update flow

━━━━━━━━━━━━━━━━━━━━
SCAN THESE AREAS FIRST
━━━━━━━━━━━━━━━━━━━━

Frontend:
- client/src/components/file-explorer*
- client/src/components/editor*
- client/src/components/workspace*
- client/src/hooks/*
- client/src/lib/*
- Monaco integration
- SSE hooks
- WebSocket hooks
- Zustand/Context stores
- dirtyFiles logic
- aiFiles logic
- preview hooks

Backend:
- server/chat/streams/*
- server/infrastructure/events/*
- server/api/fs*
- server/agents/tools/categories/file-tools*
- chokidar watchers
- sandbox file writes
- file.change events
- EventBus architecture
- preview runtime
- runtime services

━━━━━━━━━━━━━━━━━━━━
DETECT ALL PROBLEMS
━━━━━━━━━━━━━━━━━━━━

Find:
- missing file.change emits
- direct fs.writeFile bypasses
- stale React state
- race conditions
- duplicate listeners
- SSE desync
- missing optimistic updates
- missing realtime store
- conflicting editor states
- unsynced preview updates
- watcher inconsistencies
- missing debounce systems
- missing shared state manager
- AI/user overwrite conflicts
- disconnected Monaco model state
- file watcher leaks
- stale cache problems

━━━━━━━━━━━━━━━━━━━━
IMPORTANT REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

1. Deep trace COMPLETE FLOW:

AI file_write
→ backend
→ event bus
→ SSE/WebSocket
→ frontend store
→ Monaco editor
→ file explorer
→ preview reload

Find EXACTLY where flow breaks.

2. Detect:
- fake realtime behavior
- UI-only refreshes
- manual refresh dependencies
- disconnected state containers
- duplicated stores
- broken subscriptions

3. Create a REAL collaborative architecture.

━━━━━━━━━━━━━━━━━━━━
FIX REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Build:
- unified realtime file state system
- shared live sync store
- realtime file event pipeline
- optimistic updates
- AI/user conflict prevention
- synchronized Monaco model updates
- realtime preview refresh
- debounced autosave
- atomic file updates
- event deduplication
- proper watcher cleanup
- version-based sync protection

━━━━━━━━━━━━━━━━━━━━
STRICT RULES
━━━━━━━━━━━━━━━━━━━━

- Keep code CLEAN
- Keep architecture SIMPLE
- Use LOW coupling
- Use HIGH cohesion
- Keep file size UNDER 250 lines
- Split responsibilities properly
- No god files
- No duplicate logic
- No fake realtime
- No unnecessary abstractions

━━━━━━━━━━━━━━━━━━━━
OUTPUT FORMAT
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Root cause analysis
2. Broken flow analysis
3. Exact files causing desync
4. Race condition report
5. Realtime architecture redesign
6. Event flow diagram
7. Correct collaborative workflow
8. File-by-file refactor plan
9. New folder structure
10. New realtime store architecture
11. SSE/WebSocket redesign
12. Monaco sync strategy
13. Preview sync strategy
14. Conflict resolution strategy
15. Autosave strategy
16. Cleanup/leak prevention strategy
17. Step-by-step implementation roadmap
18. Final production-grade architecture

IMPORTANT:
Mention exact files and exact architectural mistakes with evidence.
Do not give generic suggestions.