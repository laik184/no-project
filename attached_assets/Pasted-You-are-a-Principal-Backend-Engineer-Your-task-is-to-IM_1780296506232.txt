You are a Principal Backend Engineer.

Your task is to IMPLEMENT the complete backend module:

server/file-explorer/

The architecture has already been finalized.

DO NOT redesign architecture.

DO NOT create alternative structures.

DO NOT merge responsibilities.

Follow the provided folder structure EXACTLY.

━━━━━━━━━━━━━━━━━━━━
MISSION
━━━━━━━━━━━━━━━━━━━━

Generate production-ready code for the complete:

server/file-explorer/

module.

The implementation must support the frontend module:

client/src/components/file-explorer/

including:

- FileExplorer.tsx
- FileTreePanel.tsx
- TreeNode.tsx
- ContextMenu.tsx
- FileHistoryPanel.tsx
- OpenEditorsPanel.tsx
- PinnedFilesPanel.tsx
- RecentFilesPanel.tsx
- ProjectInsightsPanel.tsx
- AgentStatusPanel.tsx
- use-file-explorer.ts
- use-open-editors.ts
- use-pinned-files.ts
- use-recent-files.ts
- use-git-status.tsx

━━━━━━━━━━━━━━━━━━━━
IMPORTANT RULES
━━━━━━━━━━━━━━━━━━━━

STRICTLY FOLLOW:

- Single Responsibility Principle
- One responsibility per service
- No duplicate logic
- No inline filesystem operations in routes
- No business logic in controllers
- No business logic in routes
- No direct fs access outside repositories
- No circular dependencies
- No dead code
- No duplicate helpers

━━━━━━━━━━━━━━━━━━━━
CODEBASE RULES
━━━━━━━━━━━━━━━━━━━━

TypeScript only.

ESM imports only.

All imports must include .js extension where required by project standards.

No CommonJS.

No any types.

Strict typing everywhere.

Every public method must have return types.

Every folder must contain index.ts.

━━━━━━━━━━━━━━━━━━━━
PUBLIC ENTRY RULE
━━━━━━━━━━━━━━━━━━━━

Every folder MUST expose a public API.

Example:

services/tree/index.ts

export * from "./tree.service";

No internal file imports from outside.

Allowed:

import { TreeService } from "@/server/file-explorer/services";

Not allowed:

import { TreeService } from "@/server/file-explorer/services/tree/tree.service";

━━━━━━━━━━━━━━━━━━━━
IMPLEMENTATION ORDER
━━━━━━━━━━━━━━━━━━━━

PHASE 1

Generate:

- types/
- contracts/
- config/
- guards/
- validators/
- utils/

PHASE 2

Generate repositories:

- filesystem.repository.ts
- history.repository.ts
- recent.repository.ts
- pinned.repository.ts
- editors.repository.ts
- metadata.repository.ts

Repositories are the ONLY layer allowed to access:

- fs
- path
- chokidar
- database

━━━━━━━━━━━━━━━━━━━━
PHASE 3

Generate services:

tree.service.ts
read.service.ts
write.service.ts
create.service.ts
rename.service.ts
delete.service.ts
duplicate.service.ts
upload.service.ts
download.service.ts
search.service.ts
metadata.service.ts
clipboard.service.ts
history.service.ts
recent.service.ts
pinned.service.ts
open-editors.service.ts
git-status.service.ts
insights.service.ts

Rules:

Each file under 250 lines.

Split helpers when necessary.

━━━━━━━━━━━━━━━━━━━━
PHASE 4

Generate:

mappers/

tree.mapper.ts
file.mapper.ts

Frontend contract must match:

RawTreeNode

NOT legacy FileItem.

━━━━━━━━━━━━━━━━━━━━
PHASE 5

Generate realtime layer.

realtime/

Requirements:

- publish file events
- publish rename events
- publish delete events
- publish create events
- publish history events

Use existing infrastructure:

- bus
- sseManager
- TOPIC.FILE

CRITICAL:

Fix existing bug:

Current system publishes file changes to:

TOPIC.AGENT

New implementation MUST publish to:

TOPIC.FILE

━━━━━━━━━━━━━━━━━━━━
PHASE 6

Generate watchers.

watchers/

Requirements:

- chokidar-based
- file create detection
- file delete detection
- file rename detection
- directory create detection
- directory delete detection

Must publish realtime updates.

━━━━━━━━━━━━━━━━━━━━
PHASE 7

Generate orchestrator.

explorer.orchestrator.ts

Responsibilities:

- coordinate services
- no business logic
- transaction orchestration
- event sequencing

━━━━━━━━━━━━━━━━━━━━
PHASE 8

Generate controller layer.

file-explorer.controller.ts

Responsibilities:

- validate request
- call orchestrator
- return response

Nothing else.

━━━━━━━━━━━━━━━━━━━━
PHASE 9

Generate routes.

file-explorer.routes.ts

Required endpoints:

GET    /api/file-explorer/tree
GET    /api/file-explorer/read
POST   /api/file-explorer/create
POST   /api/file-explorer/write
POST   /api/file-explorer/rename
POST   /api/file-explorer/delete
POST   /api/file-explorer/duplicate
POST   /api/file-explorer/upload
GET    /api/file-explorer/download
GET    /api/file-explorer/history
GET    /api/file-explorer/search
GET    /api/file-explorer/metadata

━━━━━━━━━━━━━━━━━━━━
CRITICAL REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Fix all problems identified in audit:

- duplicated buildTree()
- duplicated safePath()
- duplicated SANDBOX_ROOT
- duplicated EXCLUDE
- inline filesystem operations
- missing rename service
- missing duplicate service
- missing read service
- wrong frontend response shape
- missing realtime updates

━━━━━━━━━━━━━━━━━━━━
RESPONSE FORMAT
━━━━━━━━━━━━━━━━━━━━

For every generated file:

Show:

1. file path
2. purpose
3. dependencies
4. complete code

Then continue to next file.

Generate files in dependency order.

Do not skip files.

Do not leave TODO comments.

Do not generate placeholders.

Produce a complete working implementation.