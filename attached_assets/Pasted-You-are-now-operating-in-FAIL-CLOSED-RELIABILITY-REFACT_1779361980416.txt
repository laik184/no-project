You are now operating in FAIL-CLOSED RELIABILITY REFACTOR MODE.

Your mission is to redesign and harden the ENTIRE verification + completion architecture to achieve near-production-grade hallucination resistance.

The current system is FAIL-OPEN.
You must transform it into a STRICT FAIL-CLOSED SYSTEM.

==================================================
CORE ARCHITECTURE PRINCIPLES
==================================================

MANDATORY RULES:

- Keep every file under 250 LOC
- High cohesion
- Low coupling
- No God files
- No mixed responsibilities
- No hidden shared mutable state
- Use dependency injection where needed
- Prefer composition over inheritance
- Immutable data flow preferred
- Event-driven coordination preferred
- Deterministic execution preferred
- Verification before completion ALWAYS
- Runtime truth > LLM reasoning
- Evidence > assumptions

==================================================
PRIMARY OBJECTIVE
==================================================

Eliminate ALL fake-success conditions.

The system must NEVER:
- return ok:true on verification exhaustion
- assume success without evidence
- trust LLM summaries
- trust runtime assumptions
- mark completion before deterministic verification
- allow silent degradation
- continue after failed verification gates

==================================================
SECTION 1 — FAIL-CLOSED REDESIGN
==================================================

Redesign the entire completion lifecycle.

CURRENT BROKEN FLOW:

LLM thinks task is done
→ verification partially fails
→ retries exhausted
→ system returns ok:true anyway

TARGET FLOW:

task_complete requested
→ RuntimeTruth verification
→ ALL gates pass?
      YES → VERIFIED SUCCESS
      NO  → rollback + FAILED state

ABSOLUTE RULE:

Verification exhaustion MUST ALWAYS fail the run.

NEVER:
- degrade gracefully
- silently continue
- return warning-success hybrids
- convert failures into partial success

==================================================
SECTION 2 — ARCHITECTURE REFACTOR
==================================================

Split the system into small cohesive modules.

MANDATORY:
- every file < 250 LOC
- single responsibility per module
- no giant orchestrator files
- no shared mutable singleton state
- no circular dependencies

Create clean module boundaries for:

1. VerificationCoordinator
2. RuntimeTruthEngine
3. EvidenceGate
4. CompletionAuthority
5. RetryPolicyEngine
6. RecoveryCoordinator
7. CheckpointValidator
8. BuildVerifier
9. ImportGraphVerifier
10. PreviewVerifier
11. ProcessHealthVerifier
12. VerificationAuditLog
13. FailureClassifier
14. RollbackExecutor
15. VerificationStateMachine

For EACH module define:

- responsibility
- inputs
- outputs
- failure modes
- dependencies
- event contracts
- state ownership boundaries

==================================================
SECTION 3 — COMPLETION AUTHORITY SYSTEM
==================================================

The LLM must NEVER decide completion.

Create:

CompletionAuthority

ONLY this module can mark:
- VERIFIED_SUCCESS
- VERIFIED_FAILURE

LLM can ONLY:
- propose completion

CompletionAuthority must require:
- runtime verification
- filesystem verification
- import verification
- TypeScript verification
- process verification
- preview verification

If ANY fail:
→ completion denied

==================================================
SECTION 4 — EVIDENCE GATING
==================================================

Implement strict evidence-based verification.

RULE:

NO EVIDENCE
= NO SUCCESS

Every success claim must require evidence.

Examples:

Claim:
"Server running"

Required evidence:
- process alive
- stable HTTP 200
- no crash loop
- successful probe window

Claim:
"TypeScript valid"

Required evidence:
- actual tsc --noEmit exit 0
- NOT log scanning

Claim:
"Imports valid"

Required evidence:
- AST import graph verification
- export existence checks

Claim:
"Preview working"

Required evidence:
- DOM validation
- no runtime overlay
- no console fatal errors
- interactive elements exist

==================================================
SECTION 5 — VERIFICATION PIPELINE
==================================================

Create deterministic staged verification.

STAGE 1 — STATIC
- AST validation
- import graph validation
- circular dependency detection
- export validation

STAGE 2 — BUILD
- tsc --noEmit
- npm dependency integrity
- build pipeline validation

STAGE 3 — RUNTIME
- process health
- port stability
- crash-loop detection
- runtime logs

STAGE 4 — PREVIEW
- DOM verification
- hydration verification
- interaction verification
- console error inspection

STAGE 5 — STATE RECONCILIATION
- compare expected vs actual state
- ensure task postconditions satisfied

ANY stage failure:
→ immediate FAILED state

==================================================
SECTION 6 — RETRY SYSTEM REFACTOR
==================================================

Current problem:
blind repetitive retries.

Implement:

RetryPolicyEngine

Requirements:
- retries must diverge
- repeated strategies forbidden
- semantic retry similarity detection
- strategy blacklisting
- retry confidence scoring
- retry exhaustion → HARD FAIL

NEVER:
- same retry loop
- retry without new evidence
- retry based on assumptions

==================================================
SECTION 7 — RUNTIME TRUTH ENGINE
==================================================

Build:

RuntimeTruthEngine

This becomes the ONLY runtime authority.

Responsibilities:
- process truth
- HTTP truth
- filesystem truth
- TypeScript truth
- dependency truth
- import truth
- preview truth

The LLM must NEVER infer runtime state.

The runtime engine must OBSERVE actual state continuously.

==================================================
SECTION 8 — CHECKPOINT + ROLLBACK SYSTEM
==================================================

Redesign rollback architecture.

Requirements:
- immutable checkpoints
- graded checkpoints
- post-restore verification
- rollback validation
- deterministic recovery

Checkpoint grades:

GRADE_A
- verified runtime
- verified build
- verified imports
- verified preview

GRADE_B
- verified build
- verified imports

GRADE_C
- filesystem only

Recovery can ONLY restore:
- Grade A
- Grade B

After rollback:
MANDATORY reverification.

==================================================
SECTION 9 — EVENT-DRIVEN STATE MACHINE
==================================================

Build deterministic verification state machine.

States:

IDLE
→ VERIFYING_STATIC
→ VERIFYING_BUILD
→ VERIFYING_RUNTIME
→ VERIFYING_PREVIEW
→ RECONCILING_STATE
→ VERIFIED_SUCCESS

Failure path:

ANY FAILURE
→ FAILED
→ RECOVERY_REQUIRED
→ ROLLBACK
→ REVERIFY
→ HALT if still failing

No silent state mutation allowed.

==================================================
SECTION 10 — ANTI-HALLUCINATION SAFETY
==================================================

Implement:

1. Fake-success detection
2. Runtime contradiction detection
3. Context drift detection
4. Repeated retry detection
5. Invalid completion detection
6. Missing evidence detection
7. Unverified memory detection
8. Stale context rejection

The system must aggressively distrust:
- LLM summaries
- inferred completion
- stale memory
- partial verification
- assumed runtime states

==================================================
SECTION 11 — FILE STRUCTURE RULES
==================================================

MANDATORY:

Every file under 250 LOC.

Refactor giant files into:
- coordinators
- validators
- policies
- adapters
- event handlers
- repositories
- contracts

Folder structure must reflect:
- bounded contexts
- explicit ownership
- deterministic dependencies

NO:
- utils dumping ground
- mega orchestrators
- giant service classes
- cross-module hidden mutations

==================================================
SECTION 12 — DEPENDENCY INJECTION
==================================================

All major systems must use explicit DI.

Avoid:
- hidden globals
- singleton mutable state
- implicit dependencies

Use:
- constructor injection
- explicit interfaces
- immutable contracts
- event buses

==================================================
SECTION 13 — OUTPUT REQUIREMENTS
==================================================

Provide:

1. Full fail-closed architecture redesign
2. Module breakdown
3. File structure
4. Event flow diagrams
5. Verification state machine
6. Retry system redesign
7. RuntimeTruthEngine design
8. CompletionAuthority implementation plan
9. Rollback lifecycle
10. EvidenceGate architecture
11. Dependency injection design
12. Cohesion/coupling analysis
13. Anti-hallucination mechanisms
14. Migration roadmap
15. Production hardening strategy

Be brutally honest.

Do NOT give generic architecture advice.

Think like:
- reliability engineer
- distributed systems architect
- autonomous systems engineer
- compiler/runtime engineer
- production infrastructure architect

Design this like a mission-critical autonomous execution platform.