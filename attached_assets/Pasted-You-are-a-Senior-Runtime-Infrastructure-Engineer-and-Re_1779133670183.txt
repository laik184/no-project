You are a Senior Runtime Infrastructure Engineer and Replit-Style Preview System Architect.

Your task is to deeply scan and rebuild the ENTIRE Preview Runtime System to behave like Replit/Cursor/Lovable production-grade preview architecture.

FOCUS DIRECTORIES:
- server/preview/
- client/src/pages/preview/

GOAL:
Make preview lifecycle fully autonomous, synchronized, stable, observable, and production-grade.

━━━━━━━━━━━━━━━━━━━━
CURRENT PROBLEM
━━━━━━━━━━━━━━━━━━━━

Preview system is unstable and fragmented.

Current symptoms:
- "Preview will be available soon" forever
- blank preview
- 502 proxy failures
- runtime desync
- app starts but preview does not load
- server process alive but iframe dead
- port mismatch
- stale runtime state
- infinite loading
- preview not reconnecting
- runtime crashes not detected

━━━━━━━━━━━━━━━━━━━━
DETECT ALL FAILURE TYPES
━━━━━━━━━━━━━━━━━━━━

Detect and classify:

1. Runtime Boot Failure
2. Preview Initialization Failure
3. Dev Server Startup Failure
4. Port Binding Failure
5. Runtime Hydration Failure
6. Preview Proxy Failure
7. Build Pipeline Stall
8. Runtime Crash
9. Infinite Preview Loading
10. Frontend Render Failure

━━━━━━━━━━━━━━━━━━━━
DEEP SCAN REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

BACKEND SCAN:
- process spawning
- child_process lifecycle
- runtime registry
- port allocation
- port reuse
- process cleanup
- process recovery
- proxy forwarding
- websocket proxying
- stdout/stderr capture
- health monitoring
- runtime events
- preview events
- crash handling
- restart logic
- zombie process detection
- stale process cleanup

FRONTEND SCAN:
- preview iframe
- reconnect logic
- loading states
- runtime polling
- preview state management
- websocket/sse listeners
- hydration handling
- timeout handling
- error overlays
- refresh behavior
- preview lifecycle UI
- mobile responsiveness

━━━━━━━━━━━━━━━━━━━━
TRACE COMPLETE FLOW
━━━━━━━━━━━━━━━━━━━━

Trace EXACT lifecycle:

AI Agent
→ file generation
→ npm install
→ server_start
→ child_process.spawn
→ runtime registry
→ port allocation
→ preview proxy
→ iframe URL
→ frontend render
→ hydration
→ live reload
→ runtime monitoring

Find EXACTLY where flow breaks.

━━━━━━━━━━━━━━━━━━━━
REPLIT-STYLE REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Preview must work EXACTLY like Replit:

✅ instant runtime detection
✅ automatic port discovery
✅ realtime preview refresh
✅ crash recovery
✅ auto reconnect
✅ hot reload
✅ runtime health checks
✅ loading state transitions
✅ preview error overlay
✅ mobile preview support
✅ websocket forwarding
✅ persistent runtime state
✅ automatic restart
✅ runtime logs streaming
✅ multi-project isolation

━━━━━━━━━━━━━━━━━━━━
IMPORTANT ARCHITECTURE RULES
━━━━━━━━━━━━━━━━━━━━

✅ USE:
- single runtime registry
- centralized process manager
- event-driven architecture
- modular services
- health monitoring
- runtime synchronization
- process lifecycle tracking
- scalable structure

❌ AVOID:
- multiple process maps
- duplicated runtime state
- fragmented preview systems
- direct spawn scattered everywhere
- polling hacks
- fake loading states
- stale iframe URLs

━━━━━━━━━━━━━━━━━━━━
CREATE THESE SYSTEMS
━━━━━━━━━━━━━━━━━━━━

Backend architecture:

server/
├── preview/
│   ├── runtime/
│   │   ├── process-registry.ts
│   │   ├── runtime-manager.ts
│   │   ├── process-health.ts
│   │   ├── process-events.ts
│   │   ├── runtime-recovery.ts
│   │   ├── port-manager.ts
│   │   └── runtime-types.ts
│   │
│   ├── proxy/
│   │   ├── preview-proxy.ts
│   │   ├── websocket-proxy.ts
│   │   └── proxy-events.ts
│   │
│   └── monitoring/
│       ├── preview-health.ts
│       ├── crash-detector.ts
│       └── runtime-monitor.ts

Frontend architecture:

client/src/pages/preview/
├── services/
│   ├── preview-runtime.ts
│   ├── preview-health.ts
│   ├── preview-events.ts
│   ├── preview-reconnect.ts
│   └── preview-state.ts
│
├── hooks/
│   ├── usePreviewRuntime.ts
│   ├── usePreviewHealth.ts
│   └── usePreviewReconnect.ts
│
├── components/
│   ├── PreviewFrame.tsx
│   ├── PreviewLoader.tsx
│   ├── PreviewError.tsx
│   ├── RuntimeCrashOverlay.tsx
│   ├── RuntimeStatus.tsx
│   └── PreviewToolbar.tsx

━━━━━━━━━━━━━━━━━━━━
IMPORTANT CONSTRAINTS
━━━━━━━━━━━━━━━━━━━━

VERY IMPORTANT:

- Keep EVERY file under 250 lines
- Use HIGH cohesion
- Use LOW coupling
- Clean folder structure
- Modular architecture only
- Production-grade patterns only
- No giant files
- No duplicated state
- No fake logic

━━━━━━━━━━━━━━━━━━━━
REQUIRED FIXES
━━━━━━━━━━━━━━━━━━━━

1. Create SINGLE ProcessRegistry
2. Remove ALL duplicate runtime maps
3. Centralize port allocation
4. Centralize child_process lifecycle
5. Add runtime health monitoring
6. Add crash recovery
7. Add automatic reconnect
8. Add websocket forwarding
9. Add realtime preview updates
10. Add proper loading lifecycle
11. Add runtime status events
12. Add preview timeout detection
13. Add zombie cleanup
14. Add runtime persistence
15. Add preview auto-refresh
16. Add preview error overlays
17. Add HMR support
18. Add runtime synchronization

━━━━━━━━━━━━━━━━━━━━
OUTPUT FORMAT
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Root Cause Analysis
2. Preview Runtime Architecture Diagram
3. Runtime Lifecycle Flow
4. Current vs Correct Replit Architecture
5. All Runtime Bugs
6. All Preview Bugs
7. Port Management Problems
8. Proxy Problems
9. Runtime Synchronization Problems
10. Frontend State Problems
11. Exact Break Points
12. Files To Modify
13. Refactor Strategy
14. Production-Grade Final Architecture
15. Step-by-Step Safe Migration Plan

Then IMPLEMENT the fixes.

IMPORTANT:
- preserve existing functionality
- do not break runtime
- do not rewrite unrelated systems
- implement scalable architecture only