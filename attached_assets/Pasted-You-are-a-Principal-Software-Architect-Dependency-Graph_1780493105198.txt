You are a Principal Software Architect, Dependency Graph Auditor, Layer Ownership Specialist, and Repository Governance Reviewer.

MISSION

Perform a COMPLETE DEEP SCAN of the entire backend architecture.

I do NOT want assumptions.

I do NOT want theoretical architecture advice.

I want actual code evidence.

Use actual imports only.

Use actual file paths only.

Build the real dependency graph from source code.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
AUDIT TARGET
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

server/

Focus:

server/services/

server/repositories/

server/tools/

server/agents/

server/orchestration/

server/infrastructure/

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 1
BUILD COMPLETE DEPENDENCY GRAPH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Recursively scan all imports.

Generate:

FILE
↓ imports
FILE
↓ imports
FILE

Continue until leaf nodes.

Do not stop at folder boundaries.

Build the real dependency graph.

Use actual code only.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 2
SERVICE OWNERSHIP AUDIT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Scan:

server/services/**

For every service file generate:

Service File
Imports
Imported Layer

Classify:

Repository
Infrastructure
Service
Tool
Agent
Orchestration
Chat
Memory

Verify:

1. Does any service import db directly?

Examples:

server/infrastructure/db/*
server/infrastructure/index.ts

2. Does any service import bus directly?

3. Does any service import runtimeManager directly?

4. Does any service bypass repositories?

Generate evidence.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 3
REPOSITORY OWNERSHIP AUDIT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Scan:

server/repositories/**

For every repository file generate:

Repository File
Imports
Imported Layer

Verify:

1. Does any repository import a service?

2. Does any repository import a tool?

3. Does any repository import an agent?

4. Does any repository import orchestration?

5. Does any repository import chat?

Generate exact file paths.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 4
TOOL OWNERSHIP AUDIT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Scan:

server/tools/**

Verify:

1. Does any tool import repositories directly?

2. Does any tool bypass services?

3. Does any tool import infrastructure directly?

4. Does any tool import agents?

5. Does any tool import orchestration?

Generate:

Tool File
Imported Dependency
Layer

Classify:

SAFE
WARNING
VIOLATION

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 5
AGENT OWNERSHIP AUDIT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Scan:

server/agents/**

Verify:

1. Does any agent import repositories directly?

2. Does any agent import services directly?

3. Does any agent import chat?

4. Does any agent import orchestration?

5. Does any agent bypass tools?

Generate exact dependency chains.

Example:

Agent
↓
Service
↓
Repository

OR

Agent
↓
Repository

Mark violations.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 6
DEPENDENCY DIRECTION VALIDATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Expected direction:

Infrastructure
↑
Repositories
↑
Services
↑
Tools
↑
Agents
↑
Orchestration
↑
Chat

Check every import against this rule.

Detect:

Upward imports

Examples:

Repository
↓
Service

Tool
↓
Agent

Agent
↓
Orchestration

Orchestration
↓
Chat

Generate violations.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 7
DIRECT DB ACCESS SCAN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Search entire codebase for:

import { db }

Search:

server/services/**

server/tools/**

server/agents/**

Generate:

File
Import Path
Layer

Verify:

Only repositories should own DB access.

Flag violations.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 8
DIRECT INFRASTRUCTURE ACCESS SCAN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Search for imports of:

bus
db
runtimeManager
sseManager
TOPIC
processRegistry

Generate:

File
Layer
Imported Symbol

Classify:

Allowed
Needs Review
Violation

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 9
BYPASS DETECTION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Detect:

Service
↓
Infrastructure

Tool
↓
Repository

Agent
↓
Repository

Agent
↓
Infrastructure

Tool
↓
Infrastructure

Repository
↓
Service

Generate every bypass chain found.

Use actual code only.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 10
ARCHITECTURE SCORECARD
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Calculate:

Service → Repository Compliance %

Repository Isolation %

Tool Isolation %

Agent Isolation %

Dependency Direction Compliance %

Direct DB Access Violations

Infrastructure Bypass Violations

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FINAL REPORT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Full Dependency Graph

2. Service Ownership Report

3. Repository Ownership Report

4. Tool Ownership Report

5. Agent Ownership Report

6. Direct DB Access Report

7. Infrastructure Access Report

8. Dependency Direction Violations

9. Bypass Violations

10. Architecture Scorecard

11. Critical Files

12. Recommended Fixes

13. Final Verdict

One of:

✅ CLEAN ARCHITECTURE

⚠️ ARCHITECTURE SMELLS

🚨 HIGH RISK COUPLING

💥 CRITICAL LAYER VIOLATIONS

Do not guess.

Use actual code only.

Show exact file paths for every conclusion.