You are a Principal Runtime Stability Engineer.

Your task is to perform a SAFE, PRODUCTION-GRADE MEMORY LEAK FIX across the runtime infrastructure.

━━━━━━━━━━━━━━━━━━━━
MISSION
━━━━━━━━━━━━━━━━━━━━

Find and permanently fix ALL confirmed runtime memory leaks identified in:

RUNTIME_MANAGER_XRAY_REPORT.md

Main confirmed leaks:

1. runEventBuffers leak
2. telemetry eventStore leak
3. possible orphan runtime state retention
4. possible stale execution graph retention

You MUST:
- deeply verify root causes
- implement safe cleanup lifecycle
- preserve runtime replay safety
- preserve telemetry replay window
- avoid breaking orchestration
- avoid breaking SSE replay
- avoid breaking recovery systems

━━━━━━━━━━━━━━━━━━━━
MANDATORY ENGINEERING RULES
━━━━━━━━━━━━━━━━━━━━

1. HIGH COHESION, LOW COUPLING
- Every module must have ONE responsibility only.
- No tight runtime dependencies.
- Cleanup ownership must be deterministic.

2. FILE SIZE LIMIT
- NO file may exceed 250 lines.
- Split utilities/services if needed.

3. TARGETED PLACEMENT
- Place code ONLY in logically correct bounded contexts.
- No random utility pollution.

4. ROOT CAUSE FIX ONLY
- Do NOT patch symptoms.
- Find actual retention sources.
- Trace object lifecycle fully.

5. SENIOR ENGINEERING STANDARDS
- Readable naming
- Strong typing
- Scalable cleanup design
- Security-safe cleanup
- No hacks
- No shortcuts

6. CODE REVIEW MENTALITY
- Review every cleanup path.
- Verify no replay regression.
- Verify no telemetry regression.

7. FAIL-SAFE DESIGN
- Runtime completion MUST fail if cleanup validation fails.
- Validation required before merge.

8. CONSISTENT TELEMETRY
- Every cleanup event
- every eviction
- every retention failure
- every cleanup retry
MUST emit telemetry.

NO exceptions.

━━━━━━━━━━━━━━━━━━━━
SCAN TARGETS
━━━━━━━━━━━━━━━━━━━━

Deep scan:

server/execution-graph/
server/telemetry/
server/runtime/
server/infrastructure/runtime/
server/infrastructure/events/
server/orchestration/
server/chat/
server/preview/

Trace:
- object retention
- singleton ownership
- event listener retention
- buffer lifecycle
- replay lifecycle
- runtime ownership
- graph ownership
- telemetry ownership

━━━━━━━━━━━━━━━━━━━━
ROOT CAUSE ANALYSIS REQUIRED
━━━━━━━━━━━━━━━━━━━━

Find EXACTLY:

1. Where runEventBuffers are created
2. Why they are never released
3. Which lifecycle should own cleanup
4. Which event should trigger eviction
5. Which replay systems depend on them
6. Which orchestration systems consume them

Same for:
- telemetry eventStore
- graph snapshots
- runtime caches
- SSE replay buffers

━━━━━━━━━━━━━━━━━━━━
FIX REQUIRED
━━━━━━━━━━━━━━━━━━━━

# 1. runEventBuffers FIX

Find:
- creation lifecycle
- usage lifecycle
- replay requirements

Implement:
✅ deterministic cleanup
✅ run completion eviction
✅ run failure eviction
✅ delayed replay-safe eviction
✅ memory-safe retention policy

Recommended:
- TTL-based cleanup
- LRU eviction if needed

Verify:
- no replay breakage
- no graph corruption
- no orchestration regression

━━━━━━━━━━━━━━━━━━━━
# 2. telemetry eventStore FIX

Find:
- retention ownership
- replay consumers
- telemetry readers

Implement:
✅ per-run cleanup
✅ delayed retention window
✅ replay-safe eviction
✅ bounded memory policy

Verify:
- telemetry replay still works
- observability preserved
- no stale memory growth

━━━━━━━━━━━━━━━━━━━━
# 3. EVENT LISTENER SAFETY

Verify:
✅ listeners removed correctly
✅ no duplicate listeners
✅ no orphan subscriptions
✅ no hidden retention closures

Check:
- SSE hubs
- event bus
- runtime listeners
- orchestration listeners

━━━━━━━━━━━━━━━━━━━━
# 4. RUNTIME CACHE SAFETY

Find:
- stale runtime maps
- stale preview maps
- stale process maps
- stale graph maps

Implement:
✅ deterministic cleanup
✅ project shutdown cleanup
✅ crash cleanup
✅ timeout cleanup

━━━━━━━━━━━━━━━━━━━━
# 5. CLEANUP ORCHESTRATION

Design:
Cleanup lifecycle must be:

run.started
→ retain

run.completed
→ delayed cleanup scheduled

run.failed
→ delayed cleanup scheduled

recovery.started
→ preserve buffers temporarily

recovery.completed
→ cleanup eligible

runtime.stopped
→ force cleanup

━━━━━━━━━━━━━━━━━━━━
VALIDATION REQUIRED
━━━━━━━━━━━━━━━━━━━━

After fixes verify:

✅ replay still works
✅ SSE replay still works
✅ orchestration replay still works
✅ graph rendering still works
✅ telemetry rendering still works
✅ runtime recovery still works
✅ no listener leaks
✅ no buffer growth over time
✅ no memory spikes during stress tests

━━━━━━━━━━━━━━━━━━━━
STRESS TEST REQUIRED
━━━━━━━━━━━━━━━━━━━━

Run:
- repeated runtime start/stop
- repeated crash/recovery
- repeated SSE reconnect
- repeated orchestration replay
- repeated graph rendering

Measure:
- heap growth
- retained objects
- listener count
- map sizes
- replay buffers

Verify:
✅ memory stabilizes
✅ buffers evict correctly
✅ listeners remain constant

━━━━━━━━━━━━━━━━━━━━
IMPLEMENT SAFE TELEMETRY
━━━━━━━━━━━━━━━━━━━━

Emit telemetry:

memory.cleanup.started
memory.cleanup.completed
memory.cleanup.failed
memory.eviction
memory.retention.extended
memory.leak.detected

Include:
- runId
- projectId
- retainedSize
- cleanupDuration
- evictionReason

━━━━━━━━━━━━━━━━━━━━
GENERATE FINAL REPORT
━━━━━━━━━━━━━━━━━━━━

Create:

MEMORY_LEAK_FIX_REPORT.md

━━━━━━━━━━━━━━━━━━━━
REPORT MUST INCLUDE
━━━━━━━━━━━━━━━━━━━━

1. Root Cause Analysis
2. Exact Leak Sources
3. Exact Retained Objects
4. Exact Retention Chains
5. Before/After Memory Graphs
6. Cleanup Lifecycle Design
7. Replay Safety Verification
8. SSE Replay Verification
9. Telemetry Verification
10. Event Listener Verification
11. Runtime Cache Verification
12. Stress Test Results
13. Heap Stability Results
14. Listener Stability Results
15. Replay Stability Results
16. Exact Files Modified
17. Exact Cleanup Hooks Added
18. Exact Telemetry Events Added
19. Final Memory Stability Score
20. Remaining Risks
21. Recommended Next Runtime Stabilizations

━━━━━━━━━━━━━━━━━━━━
STRICT SAFETY RULES
━━━━━━━━━━━━━━━━━━━━

❌ NO giant rewrites
❌ NO breaking replay systems
❌ NO removing telemetry
❌ NO unsafe eviction
❌ NO blind cleanup
❌ NO breaking orchestration
❌ NO breaking SSE replay
❌ NO breaking runtime recovery

ONLY:
SAFE INCREMENTAL STABILIZATION.

━━━━━━━━━━━━━━━━━━━━
FINAL GOAL
━━━━━━━━━━━━━━━━━━━━

Transform runtime memory behavior from:

“long-running memory growth risk”

INTO:

“stable production-grade deterministic memory lifecycle system”

with:
✅ bounded memory
✅ replay-safe cleanup
✅ deterministic retention
✅ zero listener leaks
✅ stable long-running runtime execution