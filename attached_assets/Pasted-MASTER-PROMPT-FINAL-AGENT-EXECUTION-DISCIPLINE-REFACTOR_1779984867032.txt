MASTER PROMPT — FINAL AGENT EXECUTION DISCIPLINE REFACTOR

You are a principal AI systems architect performing a FINAL execution-discipline correction pass.

Your task is to fix the remaining architecture violations inside:

server/agents/browser/
server/agents/coderx/
server/agents/terminal/
server/agents/verifier/

This is NOT a redesign task.

This is a STRICT architecture-discipline + execution-lifecycle correction task.

You MUST preserve the current architecture direction:

Orchestrator
 ↓
Agent
 ↓
Loop
 ↓
Step Runner
 ↓
Dispatcher Client
 ↓
Tool Dispatcher
 ↓
Tool Registry
 ↓
Tool

DO NOT:

- redesign orchestration
- redesign tools
- redesign registry
- redesign dispatcher
- add frameworks
- add DI systems
- add factories
- add event buses
- add abstraction layers
- add plugin systems

Keep everything:

- direct
- layered
- production realistic
- execution-disciplined
- maintainable

---

PRIMARY GOALS

Fix these remaining problems:

Agent| Problem
browser| ❌ broken
coderx| ⚠ partial
terminal| ⚠ over-layered
verifier| ⚠ over-layered

---

TARGET EXECUTION DISCIPLINE

ALL agents MUST follow EXACT SAME execution chain:

agent.ts
 ↓
loop.ts
 ↓
step-runner.ts
 ↓
dispatcher-client.ts
 ↓
tool-dispatcher.ts
 ↓
tool-registry.ts
 ↓
tool.handler()

NO extra routing layers.

NO dispatcher bypasses.

NO direct tool imports.

NO tool internals inside agents.

---

PHASE 1 — DEEP SCAN (MANDATORY)

Before modifying ANYTHING:

Deep scan these folders completely:

server/agents/browser/
server/agents/coderx/
server/agents/terminal/
server/agents/verifier/

AND:

server/tools/
server/orchestration/

Analyze:

1. imports
2. runtime call graph
3. dispatcher usage
4. routing chains
5. execution ownership
6. primitive execution leakage
7. tool bypasses
8. telemetry duplication
9. retry duplication
10. circular dependencies
11. direct tool imports
12. inconsistent dispatcher APIs
13. cross-layer coupling
14. lifecycle violations

Generate full runtime graph BEFORE modifications.

DO NOT modify during scan phase.

---

PHASE 2 — FIX BROWSER AGENT

---

TARGET FILES

server/agents/browser/execution/browser-loop.ts
server/agents/browser/validation/state-validator.ts
server/agents/browser/coordination/dispatcher-client.ts

---

CURRENT VIOLATIONS

launchBrowser()
closeBrowser()

are directly imported from:

server/tools/browser/session/browser-lifecycle.ts

❌ FORBIDDEN

---

REQUIRED FIX

Browser session lifecycle MUST route through dispatcher-client.

---

REQUIRED FINAL FLOW

browser-loop.ts
 ↓
dispatcher-client.ts::executeTool()
 ↓
tool-dispatcher.ts
 ↓
browser_launch tool

AND:

browser_close tool

---

REQUIRED RULES

Browser agent MUST NEVER:

- import tool internals
- import browser-lifecycle.ts
- access tool session APIs directly
- bypass dispatcher

---

REQUIRED VALIDATION

Remove ALL imports from:

server/tools/browser/

inside:

server/agents/browser/

unless:

- shared types only
- shared interfaces only

---

PHASE 3 — FIX CODERX AGENT

---

TARGET FILES

server/agents/coderx/execution/step-runner.ts
server/agents/coderx/coordination/coding-routing.ts
server/agents/coderx/coordination/dispatcher-client.ts

---

CURRENT VIOLATION

step-runner
 ↓
coding-routing.ts
 ↓
dispatcher-client.ts

⚠ unnecessary extra routing layer.

---

REQUIRED FIX

Replace with:

step-runner.ts
 ↓
dispatcher-client.ts

DIRECT execution.

---

REQUIRED RULES

"coding-routing.ts"
must NOT own execution dispatch anymore.

If file becomes unused:
mark deprecated or delete safely.

---

PHASE 4 — FIX TERMINAL AGENT

---

TARGET FILES

server/agents/terminal/execution/step-runner.ts
server/agents/terminal/coordination/execution-routing.ts
server/agents/terminal/coordination/tool-coordinator.ts
server/agents/terminal/coordination/dispatcher-client.ts

---

CURRENT VIOLATION

step-runner
 ↓
execution-routing.ts
 ↓
tool-coordinator.ts
 ↓
dispatcher-client.ts

❌ over-layered execution chain.

---

REQUIRED FIX

Replace with:

step-runner.ts
 ↓
dispatcher-client.ts

DIRECT execution.

---

REQUIRED RULES

"execution-routing.ts"
must NOT own execution dispatch.

"tool-coordinator.ts"
must NOT be part of runtime execution path.

Can remain ONLY for:

- planning
- coordination metadata
- capability mapping

NOT runtime tool dispatch.

---

PHASE 5 — FIX VERIFIER AGENT

---

TARGET FILES

server/agents/verifier/execution/step-runner.ts
server/agents/verifier/coordination/verification-routing.ts
server/agents/verifier/coordination/tool-coordinator.ts
server/agents/verifier/coordination/dispatcher-client.ts
server/agents/verifier/types/verifier.types.ts

---

CURRENT VIOLATION

step-runner
 ↓
verification-routing.ts
 ↓
tool-coordinator.ts
 ↓
dispatcher-client.ts

❌ over-layered.

---

REQUIRED FIX

Replace with:

step-runner.ts
 ↓
dispatcher-client.ts

DIRECT execution.

---

REQUIRED TYPE FIX

Remove imports from:

server/tools/verifier/lib/

inside:

server/agents/verifier/types/

Move shared contracts into:

- shared/types
  OR
- agent-local contracts

NO tool-lib coupling.

---

PHASE 6 — TELEMETRY DISCIPLINE

---

TARGET FILES

ALL:

coordination/dispatcher-client.ts

inside:

- terminal
- verifier
- planner
- supervisor

---

CURRENT VIOLATION

dispatcher-client duplicates telemetry already owned by tool-dispatcher.

---

REQUIRED FIX

REMOVE:

- metric recording
- audit recording
- telemetry duplication

from dispatcher-client.

---

TOOL DISPATCHER MUST REMAIN SOLE OWNER OF:

- metrics
- audit
- timeout
- retry

---

PHASE 7 — RETRY DISCIPLINE

Deep scan:

- retry-manager.ts
- tool-dispatcher.ts

---

CURRENT PROBLEM

agent retry × tool retry

causes retry explosion.

---

REQUIRED FIX

Separate scopes:

Tool dispatcher retry:

- transient execution only

Agent retry:

- workflow-level retry only

DO NOT compound retries.

---

PHASE 8 — DISPATCHER API NORMALIZATION

Normalize ALL dispatcher-client APIs.

---

REQUIRED STANDARD

ALL agents MUST expose:

executeTool(
  toolName,
  input,
  context
)

NO:

- runTool
- dispatchTool
- execute
- invokeTool

Use ONE naming convention globally.

---

PHASE 9 — VALIDATION

After fixes verify:

✅ no direct tool imports remain

✅ no browser lifecycle bypass exists

✅ no over-layered runtime execution chains remain

✅ all step-runners call dispatcher-client directly

✅ no tool internals leak into agents

✅ no duplicate telemetry remains

✅ no retry explosion remains

✅ no circular dependencies introduced

✅ runtime graph matches target architecture

---

REQUIRED FINAL EXECUTION FLOW

ALL agents MUST become:

agent.ts
 ↓
loop.ts
 ↓
step-runner.ts
 ↓
dispatcher-client.ts
 ↓
tool-dispatcher.ts
 ↓
tool-registry.ts
 ↓
tool.handler()

Uniform execution discipline across entire system.

---

REPORTING PHASE (MANDATORY)

Generate ALL reports below.

---

REPORT 1 — BEFORE EXECUTION GRAPH

Include:

- current runtime graph
- current routing chains
- current bypasses
- current ownership violations
- current retry flow
- current telemetry flow

---

REPORT 2 — AFTER EXECUTION GRAPH

Include:

- corrected runtime graph
- corrected ownership
- corrected execution flow
- corrected retry ownership
- corrected telemetry ownership
- removed bypasses
- removed routing layers

---

REPORT 3 — FILE MODIFICATION REPORT

For EACH modified file:

File| Why Modified| Violation Fixed

---

REPORT 4 — FINAL SCORECARD

Score:

- layering
- execution discipline
- ownership purity
- dispatcher purity
- lifecycle clarity
- runtime stability
- coupling
- maintainability

0 → 10 each.

---

HARD FAILURE RULES

STOP immediately if:

- Tool → Agent dependency introduced
- circular dependency introduced
- dispatcher bypass introduced
- direct primitive execution added
- registry coupling added
- orchestration moved into tools
- tool-dispatcher loses centralized ownership

---

FINAL TARGET

This system must become:

Uniform
Layered
Dispatcher-driven
Execution-disciplined
Production-grade
Multi-agent runtime

with ZERO execution ambiguity.