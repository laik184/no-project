NURA-X TOOL LAYER GENERATION MASTER PROMPT

You are a Principal Software Architect.

Generate a complete production-grade Tool Layer.

Follow Clean Architecture strictly.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ARCHITECTURE RULES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Dependency Flow:

Agent
↓
Tool
↓
Service
↓
Repository
↓
Persistence
↓
Infrastructure

Tools MUST NOT import:

- Repository
- Persistence
- Infrastructure
- Database
- Redis
- Drizzle
- Prisma

Tools may import ONLY:

- Services
- Tool contracts
- Shared types

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
GENERATE STRUCTURE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

server/tools/

├── terminal/
│   ├── terminal-tool.ts
│   └── index.ts
│
├── package/
│   ├── install-package-tool.ts
│   ├── uninstall-package-tool.ts
│   └── index.ts
│
├── runtime/
│   ├── start-runtime-tool.ts
│   ├── restart-runtime-tool.ts
│   ├── stop-runtime-tool.ts
│   └── index.ts
│
├── git/
│   ├── git-status-tool.ts
│   ├── git-commit-tool.ts
│   ├── git-restore-tool.ts
│   └── index.ts
│
├── checkpoint/
│   ├── create-checkpoint-tool.ts
│   ├── restore-checkpoint-tool.ts
│   └── index.ts
│
├── preview/
│   ├── open-preview-tool.ts
│   └── index.ts
│
├── registry/
│   ├── tool-registry.ts
│   └── index.ts
│
└── index.ts

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TOOL CONTRACT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Create:

server/tools/contracts/tool.ts

interface Tool<TInput,TOutput> {

readonly id: string;

readonly description: string;

execute(input:TInput): Promise<TOutput>;
}

All tools must implement this contract.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SERVICE IMPORT RULES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

terminal-tool.ts

imports:

ProcessService

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

install-package-tool.ts

imports:

PackageService
ProcessService

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

uninstall-package-tool.ts

imports:

PackageService

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

start-runtime-tool.ts

imports:

RuntimeService

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

restart-runtime-tool.ts

imports:

RuntimeService

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

stop-runtime-tool.ts

imports:

RuntimeService

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

git-status-tool.ts

imports:

GitService

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

git-commit-tool.ts

imports:

GitService

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

git-restore-tool.ts

imports:

GitService

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

create-checkpoint-tool.ts

imports:

CheckpointService

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

restore-checkpoint-tool.ts

imports:

CheckpointService

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

open-preview-tool.ts

imports:

PreviewService

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TOOL REGISTRY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Create:

tool-registry.ts

Requirements:

- register(tool)
- unregister(toolId)
- get(toolId)
- execute(toolId,input)
- list()

Use singleton registry.

Provide full TypeScript implementation.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
INDEX FILES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Generate:

terminal/index.ts

package/index.ts

runtime/index.ts

git/index.ts

checkpoint/index.ts

preview/index.ts

registry/index.ts

tools/index.ts

Export everything through barrels.

No direct deep imports.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CODE QUALITY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Requirements:

✓ TypeScript strict mode

✓ SOLID principles

✓ Dependency Injection ready

✓ No circular dependencies

✓ No any types

✓ No architecture violations

✓ Production-ready code

Generate COMPLETE code.

Do not generate pseudocode.

Generate all files.