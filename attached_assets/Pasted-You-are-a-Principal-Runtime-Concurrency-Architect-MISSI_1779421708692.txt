You are a Principal Runtime Concurrency Architect.

MISSION:
Implement a PRODUCTION-GRADE Spawn Concurrency Lock system for the runtime infrastructure.

ROOT TARGET:
server/infrastructure/process/

━━━━━━━━━━━━━━━━━━━━
PROBLEM TO FIX
━━━━━━━━━━━━━━━━━━━━

Current runtime architecture allows a possible race condition:

Two concurrent:
- server_start
- runtimeManager.start()
- orchestration retries
- recovery restart calls

can trigger MULTIPLE SPAWNS for the SAME projectId simultaneously.

This can cause:
❌ duplicate npm run dev processes
❌ port conflicts
❌ runtime corruption
❌ orphan processes
❌ preview desync
❌ inconsistent RuntimeStore state
❌ race-condition crashes

This must be permanently fixed.

━━━━━━━━━━━━━━━━━━━━
ROOT CAUSE ANALYSIS REQUIRED
━━━━━━━━━━━━━━━━━━━━

Before implementing:
1. Deeply trace ALL runtime start flows
2. Trace ALL callers of:
   - runtimeManager.start()
   - processRegistry.start()
   - server_start tool
3. Identify:
   - concurrent execution paths
   - retry paths
   - orchestration restart paths
   - recovery restart paths
4. Verify:
   - async race conditions
   - duplicate spawn windows
   - missing mutex protections

DO NOT implement blind fixes.

Use evidence-based analysis only.

━━━━━━━━━━━━━━━━━━━━
TARGET ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━

Implement:

✅ per-project spawn lock
✅ deterministic runtime ownership
✅ concurrency-safe startup lifecycle
✅ safe retry handling
✅ timeout protection
✅ telemetry integration
✅ fail-safe unlock cleanup

━━━━━━━━━━━━━━━━━━━━
IMPLEMENTATION RULES
━━━━━━━━━━━━━━━━━━━━

Create a dedicated bounded-context module:

server/infrastructure/process/spawn-lock/

Allowed files:
- spawn-lock.ts
- spawn-lock.types.ts
- spawn-lock.telemetry.ts
- spawn-lock.utils.ts

NO file above 250 lines.

━━━━━━━━━━━━━━━━━━━━
REQUIRED BEHAVIOR
━━━━━━━━━━━━━━━━━━━━

If:
projectId already starting

Then:
❌ reject duplicate spawn
OR
⚠️ wait for existing startup promise

NEVER allow:
multiple active spawns for same projectId.

━━━━━━━━━━━━━━━━━━━━
REQUIRED FEATURES
━━━━━━━━━━━━━━━━━━━━

# 1. Per-Project Lock

Implement:
```ts
acquire(projectId)
release(projectId)
isLocked(projectId)

Use:

Map<string, SpawnLockState> OR equivalent deterministic ownership model.


━━━━━━━━━━━━━━━━━━━━

2. Startup Promise Tracking

Track:

startup promise

start timestamp

owner

retries

timeout


Required:

awaitExistingStart(projectId)

If startup already running: reuse same promise safely.

━━━━━━━━━━━━━━━━━━━━

3. Timeout Protection

If: startup hangs

Auto-release lock after configurable timeout.

Default: 45 seconds.

Must: emit telemetry event.

━━━━━━━━━━━━━━━━━━━━

4. Fail-Safe Cleanup

Lock MUST release on: ✅ success ✅ failure ✅ crash ✅ timeout ✅ thrown exception

Use: try/finally mandatory.

━━━━━━━━━━━━━━━━━━━━

5. Telemetry Integration

MANDATORY logging:

spawn.lock.acquired spawn.lock.reused spawn.lock.released spawn.lock.timeout spawn.lock.failed spawn.lock.rejected

Include:

projectId

runtimeId

timestamp

owner

duration


━━━━━━━━━━━━━━━━━━━━

6. RuntimeStore Integration

Verify: RuntimeStore remains deterministic.

Never allow:

duplicate runtime states

duplicate process ownership

inconsistent lifecycle states


━━━━━━━━━━━━━━━━━━━━

7. Recovery Integration

Verify: RecoveryManager restart flows respect spawn lock.

Prevent: recovery restart races.

━━━━━━━━━━━━━━━━━━━━

8. Orchestration Integration

Verify:

orchestration retries

tool-loop retries

reflection retries


cannot trigger duplicate runtime spawns.

━━━━━━━━━━━━━━━━━━━━

9. SSE Integration

Emit lifecycle-safe events:

runtime.start.pending runtime.start.reused runtime.start.completed runtime.start.failed

Ensure frontend preview remains synchronized.

━━━━━━━━━━━━━━━━━━━━ MANDATORY ENGINEERING RULES ━━━━━━━━━━━━━━━━━━━━

Follow STRICTLY:

1. High Cohesion, Low Coupling



each module single responsibility

no tight coupling

no cross-domain mutation


2. Code Size Constraint



every file under 250 lines


3. Targeted Placement



runtime concurrency logic ONLY inside: server/infrastructure/process/spawn-lock/


4. Root Cause Fix



solve actual concurrency race

NOT symptoms


5. Production-Grade Standards



typed

readable

scalable

secure

no hacks


6. Code Review Quality



self-review architecture before finalizing


7. Fail-Safe Runtime



no silent failure

no dangling locks

no unsafe retries


8. Consistent Telemetry



ALL lock actions logged


━━━━━━━━━━━━━━━━━━━━ VERIFY THESE SCENARIOS ━━━━━━━━━━━━━━━━━━━━

Test:

✅ double server_start calls ✅ orchestration retries ✅ recovery restart race ✅ rapid restart spam ✅ timeout during startup ✅ process crash during startup ✅ simultaneous preview refresh ✅ concurrent agent retries

Verify: ONLY ONE runtime process exists.

━━━━━━━━━━━━━━━━━━━━ TRACE COMPLETE FLOW ━━━━━━━━━━━━━━━━━━━━

Trace:

User → Chat → ToolLoop → server_start → runtimeManager.start() → processRegistry.start() → spawnLock.acquire() → child_process.spawn()

Ensure: NO duplicate execution path.

━━━━━━━━━━━━━━━━━━━━ REQUIRED OUTPUT ━━━━━━━━━━━━━━━━━━━━

Generate:

SPAWN_LOCK_IMPLEMENTATION_REPORT.md

━━━━━━━━━━━━━━━━━━━━ REPORT MUST INCLUDE ━━━━━━━━━━━━━━━━━━━━

1. Root Cause Analysis


2. Concurrency Race Graph


3. Exact Duplicate Spawn Paths


4. Lock Architecture Diagram


5. Exact Files Created


6. Exact Files Modified


7. Runtime Flow Before/After


8. Recovery Flow Verification


9. Orchestration Retry Verification


10. ToolLoop Retry Verification


11. RuntimeStore Verification


12. SSE Synchronization Verification


13. Telemetry Events Added


14. Timeout Handling Verification


15. Fail-Safe Cleanup Verification


16. Concurrent Start Stress Test Results


17. Runtime Stability Improvements


18. Runtime Reliability Improvements


19. Remaining Runtime Risks


20. Replit Runtime Similarity Increase %



━━━━━━━━━━━━━━━━━━━━ FINAL GOAL ━━━━━━━━━━━━━━━━━━━━

Convert runtime startup system from:

“possible concurrent spawn race”

INTO:

“deterministic, concurrency-safe, production-grade runtime lifecycle manager”

NO assumptions. NO fake fixes. NO giant files. NO unsafe locks. NO architecture pollution.