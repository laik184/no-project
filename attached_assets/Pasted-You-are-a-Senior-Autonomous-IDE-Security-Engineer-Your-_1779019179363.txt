You are a Senior Autonomous IDE Security Engineer.

Your task is to deeply scan the codebase and FIX the following critical architecture flaw:

━━━━━━━━━━━━━━━━━━━━
TARGET ISSUE
━━━━━━━━━━━━━━━━━━━━

ID: T8
Problem: No Diff Approval System
Impact: Dangerous AI file writes without user approval

Current dangerous behavior:
- AI directly writes files using file_write
- Existing files can be overwritten instantly
- No diff review
- No approval gate
- No rollback checkpoint
- No user confirmation
- AI can accidentally destroy project code

This must be converted into a SAFE HUMAN-IN-THE-LOOP WRITE SYSTEM.

━━━━━━━━━━━━━━━━━━━━
SCAN THESE SYSTEMS FIRST
━━━━━━━━━━━━━━━━━━━━

Deep scan:
- server/agents/tools/categories/
- server/chat/
- server/infrastructure/
- client/src/components/
- client/src/hooks/
- client/src/pages/

Focus on:
- file_write tool
- file_replace tool
- agent events
- SSE streams
- frontend chat stream rendering
- diff queue systems
- file explorer
- event bus
- approval flows
- existing DB tables
- pending patch systems
- conflict systems

━━━━━━━━━━━━━━━━━━━━
FIND FIRST
━━━━━━━━━━━━━━━━━━━━

Before fixing:
1. Find ALL direct file write paths
2. Find ALL overwrite paths
3. Find ALL dangerous fs.writeFile calls
4. Find ALL agent-generated writes
5. Find ALL patch systems already existing
6. Find ALL diff-related code
7. Find ALL approval UI components
8. Find ALL event types related to files
9. Find ALL SSE flows for agent events
10. Find ALL rollback/checkpoint systems

Generate evidence-based findings first.

━━━━━━━━━━━━━━━━━━━━
THEN IMPLEMENT SAFE ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━

NEW REQUIRED FLOW:

AI wants to modify file
        ↓
Generate unified diff
        ↓
Store pending patch
        ↓
Emit "agent.diff" event
        ↓
Frontend renders diff modal
        ↓
User clicks:
   APPROVE / REJECT
        ↓
Backend waits asynchronously
        ↓
If approved:
    apply patch
If rejected:
    cancel write
        ↓
Agent continues

━━━━━━━━━━━━━━━━━━━━
MANDATORY REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

1. NEVER directly overwrite existing files
2. ALL overwrites must require approval
3. New files may bypass approval (optional)
4. Existing files MUST use diff approval
5. Must support async waiting
6. Must support timeout
7. Must support rejection handling
8. Must support cancellation
9. Must support rollback
10. Must support future multi-user scaling

━━━━━━━━━━━━━━━━━━━━
STRICT IMPLEMENTATION RULES
━━━━━━━━━━━━━━━━━━━━

IMPORTANT:
- Keep architecture CLEAN
- Keep files SMALL
- Max 250 lines per file
- Avoid giant god files
- Use modular structure
- Use clear folder naming
- Avoid circular dependencies
- Avoid duplicate event systems
- Avoid duplicate buses
- Avoid fake implementations
- Avoid UI-only placeholders

━━━━━━━━━━━━━━━━━━━━
REQUIRED BACKEND STRUCTURE
━━━━━━━━━━━━━━━━━━━━

Create clean architecture like:

server/
├── approvals/
│   ├── diff-approval.service.ts
│   ├── diff-session.store.ts
│   ├── diff-generator.ts
│   ├── diff.types.ts
│   └── diff-timeout.ts
│
├── events/
│   └── approval.events.ts
│
├── routes/
│   └── diff-approval.routes.ts

━━━━━━━━━━━━━━━━━━━━
REQUIRED FRONTEND STRUCTURE
━━━━━━━━━━━━━━━━━━━━

client/src/
├── features/diff-approval/
│   ├── DiffApprovalModal.tsx
│   ├── DiffViewer.tsx
│   ├── useDiffApproval.ts
│   ├── diff-approval.store.ts
│   └── diff.types.ts

━━━━━━━━━━━━━━━━━━━━
REQUIRED FEATURES
━━━━━━━━━━━━━━━━━━━━

Backend:
- diff generation
- pending approval sessions
- approval promises
- timeout handling
- apply/reject logic
- rollback snapshots
- event emission
- DB persistence support

Frontend:
- live diff modal
- syntax highlighted diff
- approve/reject buttons
- pending state
- timeout state
- agent waiting indicator
- optimistic updates

━━━━━━━━━━━━━━━━━━━━
IMPORTANT SECURITY REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Protect against:
- race conditions
- double approvals
- stale writes
- path traversal
- invalid patch application
- file corruption
- concurrent writes
- orphan approval sessions

━━━━━━━━━━━━━━━━━━━━
CHECK EXISTING SYSTEMS
━━━━━━━━━━━━━━━━━━━━

Reuse existing:
- diff_queue tables
- patch systems
- event buses
- SSE streams
- file history systems
- rollback utilities

DO NOT rebuild existing working infrastructure.

━━━━━━━━━━━━━━━━━━━━
OUTPUT FORMAT
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Deep scan findings
2. Existing dangerous write paths
3. Existing reusable systems
4. Architecture flaws
5. New safe architecture
6. Folder structure
7. File-by-file implementation plan
8. Event flow diagram
9. Approval lifecycle
10. Timeout lifecycle
11. Rollback strategy
12. Concurrency strategy
13. Security hardening
14. Refactor plan
15. Production-ready implementation

━━━━━━━━━━━━━━━━━━━━
MOST IMPORTANT GOAL
━━━━━━━━━━━━━━━━━━━━

Transform the AI from:
"Dangerous autonomous overwriter"

Into:
"Safe approval-based coding assistant"

The final system must feel like:
- Cursor diff approval
- Replit approvals
- GitHub PR review
- Production-grade AI safety

ONLY produce:
- real implementations
- scalable architecture
- maintainable code
- modular structure
- evidence-based fixes