You are a Senior TypeScript Architect.

TASK

Fix ONLY the three architecture issues below inside:

server/file-explorer/orchestrator/explorer.orchestrator.ts

Do NOT perform any other refactor.

Do NOT rename files.

Do NOT move files.

Do NOT create new features.

Do NOT modify business logic.

Do NOT modify API contracts.

Do NOT modify runtime behavior.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ISSUE 1
REALTIME IMPORT MISSING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Verify whether:

server/file-explorer/realtime/file-events.service.ts

or

server/file-explorer/realtime/index.ts

exists.

If it exists:

Add the correct import to explorer.orchestrator.ts.

Use the existing realtime service instance.

Do not create a new realtime implementation.

Do not change event logic.

Only wire the existing realtime dependency correctly.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ISSUE 2
TYPES IMPORT MISSING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Verify whether:

server/file-explorer/types/index.ts

exports any types currently used by explorer.orchestrator.ts.

If exported types are used by the orchestrator:

Add the required type-only imports.

Use:

import type { ... }

Do not create new types.

Do not modify existing types.

Do not add unused imports.

If no orchestrator type is exported from types/index.ts, leave unchanged and report the finding.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ISSUE 3
DIRECT REPOSITORY IMPORTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Remove direct repository usage from:

import { historyRepository }
from '../../repositories/file-system/index.ts';

import { gitRepository }
from '../../repositories/file-system/index.ts';

Repository layer must not be accessed directly by explorer.orchestrator.ts.

Verify existing services inside:

server/services/filesystem/index.ts

Locate the correct service abstraction already responsible for:

- history access
- git status access

Refactor explorer.orchestrator.ts so it talks only to services.

Required direction:

explorer.orchestrator.ts
↓
services/filesystem/index.ts
↓
repositories/file-system/index.ts

Forbidden direction:

explorer.orchestrator.ts
↓
repositories/file-system/index.ts

Do not create duplicate services.

Do not create temporary wrappers.

Use existing services only.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VERIFICATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

After changes verify:

1. No repository imports remain in explorer.orchestrator.ts.

2. Realtime dependency is imported correctly if available.

3. Types are imported only when actually used.

4. explorer.orchestrator.ts depends only on:
   
   - contracts
   - services
   - config
   - realtime
   - types (type-only)

5. TypeScript build remains unchanged.

6. Runtime behavior remains unchanged.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
OUTPUT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Provide:

- Removed imports
- Added imports
- Service replacements used
- Verification results

Do not perform any work outside these three fixes.