You are a Principal Autonomous Runtime Architect.

Your task is to DESIGN and IMPLEMENT a production-grade:

DYNAMIC RE-ROUTING SYSTEM

for the NURA-X orchestration engine.

━━━━━━━━━━━━━━━━━━━━
MAIN GOAL
━━━━━━━━━━━━━━━━━━━━

Currently:
Execution mode is selected ONLY once at run start.

This is a major architectural limitation.

The system MUST evolve into:

✅ adaptive orchestration
✅ self-escalating execution
✅ runtime-aware routing
✅ complexity-aware routing
✅ fail-aware re-routing
✅ DAG promotion engine

The orchestration system must dynamically switch execution modes DURING runtime.

━━━━━━━━━━━━━━━━━━━━
CURRENT EXECUTION MODES
━━━━━━━━━━━━━━━━━━━━

Current modes:

1. tool-loop
2. planned
3. dag

Current file:
server/orchestration/execution/execution-router.ts

━━━━━━━━━━━━━━━━━━━━
PROBLEM
━━━━━━━━━━━━━━━━━━━━

Current behavior:

User Request
→ ComplexityScorer
→ execution mode selected
→ locked forever

This is incorrect.

Example:

A simple task may become:
- multi-file
- runtime-complex
- dependency-heavy
- verification-heavy

AFTER execution starts.

Current system CANNOT adapt.

━━━━━━━━━━━━━━━━━━━━
TARGET BEHAVIOR
━━━━━━━━━━━━━━━━━━━━

The system MUST dynamically escalate:

tool-loop
→ planned
→ dag
→ quantum (future-ready)

based on REAL runtime intelligence.

━━━━━━━━━━━━━━━━━━━━
RE-ROUTING TRIGGERS
━━━━━━━━━━━━━━━━━━━━

System MUST monitor:

✅ file count growth
✅ dependency explosion
✅ runtime instability
✅ retry count
✅ tool execution failures
✅ verification failures
✅ reflection severity
✅ orchestration complexity
✅ memory pressure
✅ runtime duration
✅ parallelization opportunities
✅ agent confidence score

━━━━━━━━━━━━━━━━━━━━
EXAMPLE
━━━━━━━━━━━━━━━━━━━━

Initial request:
"Fix navbar bug"

Starts as:
tool-loop

During execution:
- 18 files touched
- runtime restart needed
- verification failing
- multiple retries
- frontend/backend dependencies discovered

System MUST auto-escalate:

tool-loop
→ dag

WITHOUT restarting run.

━━━━━━━━━━━━━━━━━━━━
ARCHITECTURE REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

STRICTLY FOLLOW:

✅ High Cohesion
✅ Low Coupling
✅ Fail-Closed
✅ Event-Driven
✅ Deterministic Ownership
✅ Safe Escalation
✅ Runtime Safety
✅ Strong Telemetry

❌ no hacks
❌ no giant files
❌ no hidden side effects
❌ no direct cross-module mutations

━━━━━━━━━━━━━━━━━━━━
FILE SIZE RULE
━━━━━━━━━━━━━━━━━━━━

NO FILE MAY EXCEED:
250 lines

Split logic into focused modules.

━━━━━━━━━━━━━━━━━━━━
TARGET FOLDER STRUCTURE
━━━━━━━━━━━━━━━━━━━━

Create ONLY if missing:

server/orchestration/rerouting/
│
├── dynamic-rerouter.ts
├── reroute-decision-engine.ts
├── reroute-signal-analyzer.ts
├── escalation-policy.ts
├── mode-transition-manager.ts
├── reroute-telemetry.ts
├── reroute-types.ts
├── reroute-guards.ts
└── reroute-thresholds.ts

━━━━━━━━━━━━━━━━━━━━
RESPONSIBILITIES
━━━━━━━━━━━━━━━━━━━━

# dynamic-rerouter.ts

ONLY:
- orchestrates rerouting lifecycle
- receives signals
- triggers transitions

NO business logic.

━━━━━━━━━━━━━━━━━━━━
# reroute-signal-analyzer.ts

ONLY:
- analyze runtime signals
- analyze execution metrics
- compute reroute signals

Signals:
- HIGH_COMPLEXITY
- RETRY_STORM
- MASS_FILE_TOUCH
- VERIFICATION_CASCADE
- RUNTIME_INSTABILITY
- PARALLEL_OPPORTUNITY

━━━━━━━━━━━━━━━━━━━━
# reroute-decision-engine.ts

ONLY:
- determine target mode
- calculate escalation confidence
- choose safe transition

Rules:
tool-loop → planned
planned → dag
dag → quantum (future)

NO downgrade initially.

━━━━━━━━━━━━━━━━━━━━
# escalation-policy.ts

ONLY:
- policy rules
- escalation thresholds
- fail-safe protections

━━━━━━━━━━━━━━━━━━━━
# mode-transition-manager.ts

ONLY:
- safe execution migration
- checkpoint transition
- preserve runtime state
- preserve telemetry continuity

CRITICAL:
NO execution loss allowed.

━━━━━━━━━━━━━━━━━━━━
# reroute-guards.ts

ONLY:
- prevent unsafe rerouting
- detect infinite escalation loops
- block unstable transitions

━━━━━━━━━━━━━━━━━━━━
# reroute-telemetry.ts

ONLY:
- emit telemetry
- emit lifecycle events
- emit reroute traces

━━━━━━━━━━━━━━━━━━━━
REQUIRED EVENTS
━━━━━━━━━━━━━━━━━━━━

Emit typed events:

reroute.requested
reroute.approved
reroute.blocked
reroute.transition.started
reroute.transition.completed
reroute.transition.failed
reroute.loop.detected
reroute.signal.detected

Integrate with:
server/infrastructure/events/bus.ts

━━━━━━━━━━━━━━━━━━━━
REQUIRED TELEMETRY
━━━━━━━━━━━━━━━━━━━━

Every reroute MUST log:

✅ previous mode
✅ target mode
✅ trigger reason
✅ confidence score
✅ runtime metrics
✅ retry counts
✅ verification failures
✅ files touched
✅ duration
✅ orchestration phase

Telemetry MUST integrate with:

server/orchestration/telemetry/

━━━━━━━━━━━━━━━━━━━━
ROOT CAUSE ANALYSIS
━━━━━━━━━━━━━━━━━━━━

Before reroute:

System MUST analyze:
- WHY current mode failed
- WHY escalation is needed
- WHETHER escalation is safe

NO blind escalation allowed.

━━━━━━━━━━━━━━━━━━━━
SAFE TRANSITION REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Before transition:

VERIFY:
✅ no active critical write
✅ no recovery lock active
✅ no verification lock active
✅ runtime snapshot captured
✅ checkpoint exists
✅ orchestration state preserved

━━━━━━━━━━━━━━━━━━━━
FAIL-CLOSED REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

If reroute safety fails:

BLOCK reroute.

Emit:
reroute.blocked

DO NOT continue unsafe execution.

━━━━━━━━━━━━━━━━━━━━
INTEGRATION REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Integrate with:

server/orchestration/execution/execution-router.ts
server/orchestration/orchestration-engine.ts
server/engine/graph/
server/fail-closed/
server/infrastructure/runtime/
server/agents/reflection/
server/agents/recovery/
server/orchestration/telemetry/

━━━━━━━━━━━━━━━━━━━━
REQUIRED ANALYSIS
━━━━━━━━━━━━━━━━━━━━

Before implementation:

Deeply analyze:
- existing execution flow
- current router ownership
- orchestration lifecycle
- DAG state ownership
- runtime ownership
- verification ownership

Detect:
❌ circular dependency risks
❌ reroute race conditions
❌ orchestration corruption risks
❌ runtime state loss
❌ telemetry discontinuity

━━━━━━━━━━━━━━━━━━━━
MANDATORY SAFETY
━━━━━━━━━━━━━━━━━━━━

Rerouting MUST:
✅ preserve runId
✅ preserve telemetry continuity
✅ preserve checkpoints
✅ preserve memory context
✅ preserve event subscriptions
✅ preserve runtime ownership

━━━━━━━━━━━━━━━━━━━━
REQUIRED FINAL REPORT
━━━━━━━━━━━━━━━━━━━━

Generate:

DYNAMIC_REROUTING_IMPLEMENTATION_REPORT.md

Include:

1. Root Cause Analysis
2. Existing Router Analysis
3. Existing Execution Lifecycle
4. Existing Ownership Model
5. Existing Runtime Coupling Risks
6. Existing DAG Coupling Risks
7. Rerouting Architecture
8. Signal Analysis Design
9. Escalation Policy Design
10. Safe Transition Design
11. Guardrail Design
12. Fail-Closed Design
13. Telemetry Design
14. Event Integration
15. Runtime Safety Verification
16. Verification Safety Verification
17. Recovery Safety Verification
18. Checkpoint Preservation Verification
19. Memory Preservation Verification
20. Circular Dependency Analysis
21. Race Condition Analysis
22. Files Created
23. Files Modified
24. Imports Updated
25. Event Types Added
26. Telemetry Hooks Added
27. Safe Escalation Matrix
28. Unsupported Escalations
29. Future Quantum Routing Compatibility
30. Production Readiness Score

━━━━━━━━━━━━━━━━━━━━
TEAM ENGINEERING RULES
━━━━━━━━━━━━━━━━━━━━

APPLY TO EVERY FILE:

1. High Cohesion, Low Coupling
2. Max 250 lines per file
3. Clean architecture boundaries
4. Root-cause-first engineering
5. Production-grade readability
6. Mandatory review quality
7. Fail-closed execution
8. Consistent telemetry logging

NO EXCEPTIONS.

━━━━━━━━━━━━━━━━━━━━
FINAL GOAL
━━━━━━━━━━━━━━━━━━━━

Transform orchestration from:

"static execution routing"

INTO:

"adaptive autonomous orchestration intelligence"

with safe runtime-aware execution escalation.