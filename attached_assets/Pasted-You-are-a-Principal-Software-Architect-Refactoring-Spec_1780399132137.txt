You are a Principal Software Architect, Refactoring Specialist, Dependency Graph Engineer, Runtime Architecture Auditor, and Clean Architecture Enforcer.

MISSION

Perform a COMPLETE IMPLEMENTATION AUDIT and ARCHITECTURE REFACTOR of:

server/tools/filesystem/

server/file-explorer/services/

server/file-explorer/repositories/

server/infrastructure/

This is NOT a reporting task.

This is NOT an analysis-only task.

Your job is to:

1. Audit
2. Identify violations
3. Refactor
4. Create missing layers
5. Wire dependencies
6. Update imports
7. Remove architecture violations
8. Validate runtime flow
9. Produce final clean architecture

Target Architecture:

Tool
↓
Service
↓
Repository
↓
Infrastructure

No exceptions.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 1
FULL DEPENDENCY AUDIT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Scan every file inside:

server/tools/filesystem/

server/file-explorer/services/

server/file-explorer/repositories/

server/infrastructure/

Build actual dependency graph.

Show:

File
↓ imports
File
↓ imports
File

Detect:

* direct node:fs access
* fs/promises access
* child_process access
* lib layer bypasses
* missing service layer
* missing repository layer
* dead code
* duplicate logic

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 2
TOOL AUDIT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Audit every filesystem tool.

Example:

fs_read_file
fs_read_lines
fs_read_folder
fs_file_metadata
fs_write_file
fs_append_file
fs_ensure_file
fs_write_if_absent
fs_patch_file
fs_patch_all
fs_replace_line
fs_insert_at
fs_replace_all
fs_delete_file
fs_delete_folder
fs_delete_multiple
fs_move_file
fs_move_folder
fs_rename_file
fs_rename_folder
fs_clone_file
fs_clone_folder
fs_search_text
fs_search_regex
fs_find_by_name
fs_find_by_extension
fs_find_by_pattern
fs_ascii_tree
fs_folder_structure
fs_create_folder
fs_create_folders
fs_folder_entries
fs_folder_names
fs_subfolder_entries
fs_file_entries
fs_find_imports
fs_find_exports
fs_find_symbol_usages
fs_scan_folder
fs_scan_extension

For every tool verify:

Does it import a service?

YES / NO

If NO:

Refactor immediately.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 3
FORCE TOOL → SERVICE WIRING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Replace every architecture violation.

Current Bad Pattern:

Tool
↓
lib/*
↓
node:fs

Must become:

Tool
↓
Service
↓
Repository
↓
Infrastructure

Examples:

fs_read_file
↓
readService

fs_write_file
↓
writeService

fs_delete_file
↓
deleteService

fs_rename_file
↓
renameService

fs_clone_file
↓
duplicateService

fs_file_metadata
↓
metadataService

fs_search_text
↓
searchService

fs_folder_structure
↓
treeService

fs_scan_folder
↓
scannerService

fs_find_imports
↓
dependencyAnalysisService

Update imports.

Update implementations.

Remove direct lib execution from tools.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 4
SERVICE AUDIT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Audit every service.

Verify:

readService
writeService
createService
deleteService
renameService
duplicateService
searchService
treeService
metadataService
historyService
clipboardService
gitStatusService
insightsService
downloadService
uploadService
openEditorsService
pinnedService
recentService
dependencyAnalysisService
scannerService

Rule:

Every service must own business logic.

Every service must call repository.

No service may call node:fs directly.

No service may call child_process directly.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 5
SERVICE → REPOSITORY WIRING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Verify every service imports repository.

If repository missing:

Create repository.

Wire service.

Examples:

treeService
↓
filesystemRepository

downloadService
↓
downloadRepository
OR
filesystemRepository

gitStatusService
↓
gitRepository

If repository does not exist:

Create it.

Move low-level logic there.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 6
ELIMINATE DIRECT ACCESS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Search entire codebase for:

node:fs
node:fs/promises
fs
fs/promises
child_process
execSync
execFile

inside:

server/tools/filesystem/
server/file-explorer/services/

If found:

Move logic into repository layer.

Rewrite imports.

Update callers.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 7
LIB LAYER CONSOLIDATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Audit:

server/tools/filesystem/lib/

Determine:

Can logic be migrated into repositories?

Goal:

Remove lib as a runtime dependency for tools.

Target:

Tool
↓
Service
↓
Repository

If lib utilities are still useful:

Repository
↓
lib utility

Allowed.

Tool
↓
lib

NOT allowed.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 8
CREATE MISSING REPOSITORIES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

If required create:

git.repository.ts

download.repository.ts

scanner.repository.ts

dependency-analysis.repository.ts

or merge into filesystemRepository when appropriate.

Choose architecture with minimum duplication.

Explain reasoning.

Implement it.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 9
RUNTIME VALIDATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Generate actual runtime chain for every tool.

Example:

fs_read_file
↓
readService
↓
filesystemRepository
↓
node:fs

fs_write_file
↓
writeService
↓
filesystemRepository
↓
node:fs

fs_find_imports
↓
dependencyAnalysisService
↓
filesystemRepository
↓
node:fs

Verify no layer is skipped.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 10
DEAD CODE REMOVAL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Identify:

Unused lib files

Unused repositories

Unused services

Unused utilities

Unused exports

Remove or mark for deletion.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 11
FINAL IMPLEMENTATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Actually perform refactor.

Create missing files.

Update imports.

Update exports.

Update index.ts barrels.

Fix compile errors.

Fix type errors.

Fix broken references.

Ensure build passes.

Ensure runtime flow passes.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
PHASE 12
FINAL VERIFICATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Produce final matrix:

Tool
↓
Service
↓
Repository
↓
Infrastructure

for all filesystem tools.

No tool may directly access:

* lib
* node:fs
* child_process
* repositories

No service may directly access:

* node:fs
* child_process

Only repositories may access infrastructure.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FINAL OUTPUT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Files Modified

2. Files Created

3. Imports Updated

4. Repositories Added

5. Services Added

6. Runtime Graph

7. Architecture Compliance Score

8. Remaining Violations

9. Final Verdict

Choose one:

✅ CLEAN ARCHITECTURE ACHIEVED

⚠️ PARTIAL REFACTOR

🚨 VIOLATIONS REMAIN

Do not stop at reporting.

Implement all fixes required to achieve:

Tool
↓
Service
↓
Repository
↓
Infrastructure
