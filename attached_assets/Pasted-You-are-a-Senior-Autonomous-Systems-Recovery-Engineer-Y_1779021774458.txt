You are a Senior Autonomous Systems Recovery Engineer.

Your task is to deeply scan the entire codebase and FIX the following critical architecture issue:

━━━━━━━━━━━━━━━━━━━━
TARGET ISSUE
━━━━━━━━━━━━━━━━━━━━

T10 — No Checkpoint System
T9  — No Safe Recovery

Current problem:
- Agent writes files directly into the sandbox
- No rollback exists
- No recovery snapshots exist
- No restore points exist
- If the AI corrupts files, crashes runtime, or breaks the app, there is NO safe recovery path
- Agent runs are destructive and irreversible

This is a CRITICAL production blocker.

━━━━━━━━━━━━━━━━━━━━
GOAL
━━━━━━━━━━━━━━━━━━━━

Build a production-grade:
1. Checkpoint System
2. Rollback System
3. Safe Recovery Architecture
4. Recovery Manager
5. Snapshot Pipeline

The system must behave like:
- Replit checkpoints
- Cursor restore points
- Git-style recovery
- Autonomous safe execution

━━━━━━━━━━━━━━━━━━━━
IMPORTANT RULES
━━━━━━━━━━━━━━━━━━━━

VERY IMPORTANT:
- Use HIGH CAUTION
- LOW COUPLING
- CLEAR FOLDER STRUCTURE
- SMALL SAFE CHANGES
- DO NOT rewrite the whole architecture
- DO NOT break working systems
- DO NOT modify unrelated logic
- DO NOT create giant files

STRICT FILE RULE:
- No file should exceed 250 lines
- Split logic into focused modules
- Use clean architecture

━━━━━━━━━━━━━━━━━━━━
SCAN THESE SYSTEMS
━━━━━━━━━━━━━━━━━━━━

Deep scan:
- server/
- server/chat/
- server/agents/
- server/infrastructure/
- sandbox/
- runtime/
- preview/
- orchestration/
- pipeline/
- tool-loop/
- file tools
- git tools
- runtime tools

Find:
- all file write flows
- all destructive operations
- all runtime mutations
- all places where rollback is impossible
- all missing recovery hooks
- all missing persistence layers

━━━━━━━━━━━━━━━━━━━━
YOU MUST DETECT
━━━━━━━━━━━━━━━━━━━━

1. Direct fs.writeFile usage
2. Unsafe overwrite operations
3. Runtime state mutations
4. Missing snapshot points
5. Missing rollback handlers
6. Missing recovery APIs
7. Missing process recovery
8. Missing crash recovery
9. Missing checkpoint persistence
10. Missing run restoration
11. Missing recovery metadata
12. Missing safe execution boundaries

━━━━━━━━━━━━━━━━━━━━
DESIGN A REAL SYSTEM
━━━━━━━━━━━━━━━━━━━━

Create:

CHECKPOINT ENGINE:
- createCheckpoint()
- restoreCheckpoint()
- listCheckpoints()
- deleteCheckpoint()

RECOVERY ENGINE:
- safeRollback()
- autoRecovery()
- crashRecovery()
- failedRunRecovery()

SNAPSHOT STORAGE:
- metadata
- changed files
- timestamps
- runtime state
- process state
- git refs
- agent run state

━━━━━━━━━━━━━━━━━━━━
REQUIRED ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━

Create clean structure like:

server/infrastructure/checkpoints/
  checkpoint-manager.ts
  checkpoint-store.ts
  checkpoint-types.ts
  restore-engine.ts
  rollback-engine.ts

server/infrastructure/recovery/
  recovery-manager.ts
  crash-recovery.ts
  runtime-recovery.ts

server/infrastructure/snapshots/
  snapshot-engine.ts
  snapshot-diff.ts

━━━━━━━━━━━━━━━━━━━━
REQUIRED FEATURES
━━━━━━━━━━━━━━━━━━━━

1. BEFORE agent writes files:
   → auto create checkpoint

2. BEFORE runtime restart:
   → save runtime snapshot

3. BEFORE destructive operation:
   → create rollback point

4. AFTER failed run:
   → auto recovery attempt

5. AFTER crash:
   → restore previous stable checkpoint

6. Recovery must support:
   - files
   - runtime
   - preview state
   - process state
   - git state

━━━━━━━━━━━━━━━━━━━━
GIT INTEGRATION
━━━━━━━━━━━━━━━━━━━━

Use lightweight git-based checkpoints when possible:
- git add
- git stash
- git commit
- temporary refs

BUT:
- must work even if git fails
- include filesystem fallback snapshots

━━━━━━━━━━━━━━━━━━━━
IMPORTANT IMPLEMENTATION RULES
━━━━━━━━━━━━━━━━━━━━

Use:
- dependency injection
- small modules
- typed interfaces
- async-safe operations
- atomic writes
- transaction-safe recovery

Avoid:
- giant singleton classes
- circular dependencies
- orchestration recursion
- direct runtime coupling

━━━━━━━━━━━━━━━━━━━━
SAFETY REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Must prevent:
- corrupted rollback
- partial restore
- double recovery
- infinite recovery loops
- recursive checkpoint creation
- snapshot corruption

Add:
- recovery locks
- rollback validation
- checkpoint integrity checks
- recovery timeout guards

━━━━━━━━━━━━━━━━━━━━
OUTPUT REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Generate:

1. Root-cause analysis
2. All unsafe write locations
3. All missing recovery points
4. New architecture design
5. Folder structure
6. Recovery lifecycle
7. Checkpoint lifecycle
8. Safe rollback flow
9. Recovery flow diagram
10. Exact files to modify
11. New files to create
12. Minimal safe patches
13. Migration strategy
14. Safety validation strategy
15. Production hardening checklist

━━━━━━━━━━━━━━━━━━━━
IMPORTANT
━━━━━━━━━━━━━━━━━━━━

DO NOT:
- give generic advice
- redesign unrelated systems
- modify frontend unnecessarily
- create huge files
- create tight coupling

ONLY:
- fix checkpoint architecture
- implement safe recovery
- stabilize execution safety
- make autonomous runs reversible

Act like you are fixing a billion-dollar autonomous coding platform.