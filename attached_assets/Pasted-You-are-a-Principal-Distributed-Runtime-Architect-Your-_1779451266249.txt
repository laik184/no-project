You are a Principal Distributed Runtime Architect.

Your task is to DESIGN + IMPLEMENT a production-grade Worker Pool System for the NURA-X autonomous agent infrastructure.

━━━━━━━━━━━━━━━━━━━━
MISSION
━━━━━━━━━━━━━━━━━━━━

Create a centralized Worker Pool + Priority Scheduler architecture that upgrades the backend from:

❌ ad-hoc Promise.all() execution
❌ uncontrolled async spawning
❌ unbounded concurrency
❌ non-deterministic execution

INTO:

✅ deterministic worker orchestration
✅ centralized concurrency control
✅ intelligent scheduling
✅ backpressure-safe execution
✅ telemetry-aware distributed execution
✅ Replit-level runtime coordination

━━━━━━━━━━━━━━━━━━━━
MANDATORY ENGINEERING RULES
━━━━━━━━━━━━━━━━━━━━

1. HIGH COHESION, LOW COUPLING
- Every module must have ONE responsibility only.
- No tight coupling between orchestration, workers, runtime, verification, or memory.
- Use interfaces/contracts/events for communication.

2. FILE SIZE LIMIT
- NO file may exceed 250 lines.
- Split responsibilities aggressively.

3. TARGETED LOCATION
- Every file must be placed in the correct bounded context.
- No root pollution.
- No misplaced services.

4. ROOT CAUSE ENGINEERING
- Do not patch symptoms.
- Analyze WHY uncontrolled execution exists.
- Replace architectural weakness with deterministic ownership.

5. SENIOR ENGINEERING STANDARDS
- Strong typing
- Readable names
- Clear comments
- Secure execution
- Timeout protection
- Resource governance
- No hacks
- No shortcuts

6. CODE REVIEW READY
- Structure code as if reviewed by Staff/Principal engineers.

7. FAIL-SAFE DESIGN
- Worker execution MUST fail-closed.
- No silent worker crashes.
- No lost tasks.
- No untracked retries.

8. TELEMETRY REQUIRED
Every:
- task submission
- task start
- task completion
- task failure
- retry
- timeout
- worker allocation
- worker saturation
- queue overflow

MUST emit telemetry.

━━━━━━━━━━━━━━━━━━━━
TARGET LOCATION
━━━━━━━━━━━━━━━━━━━━

Create:

server/quantum/
├── scheduler/
│   ├── worker-pool.ts
│   ├── priority-queue.ts
│   ├── worker-slot.ts
│   ├── task-router.ts
│   ├── worker-events.ts
│   ├── worker-metrics.ts
│   ├── worker-types.ts
│   ├── worker-errors.ts
│   ├── queue-policy.ts
│   └── backpressure-controller.ts
│
├── execution/
│   ├── parallel-executor.ts
│   ├── execution-batch.ts
│   ├── execution-limiter.ts
│   ├── execution-timeout.ts
│   └── execution-retry.ts
│
├── telemetry/
│   ├── worker-telemetry.ts
│   ├── queue-telemetry.ts
│   └── execution-trace.ts
│
└── integration/
    ├── graph-engine-bridge.ts
    ├── orchestration-bridge.ts
    ├── tool-loop-bridge.ts
    └── runtime-bridge.ts

━━━━━━━━━━━━━━━━━━━━
MAIN GOALS
━━━━━━━━━━━━━━━━━━━━

Implement:

✅ WorkerPool
✅ PriorityQueue
✅ TaskRouter
✅ BackpressureController
✅ ExecutionLimiter
✅ RetryEngine
✅ TimeoutProtection
✅ WorkerTelemetry
✅ QueueOverflowProtection
✅ DeterministicOwnership

━━━━━━━━━━━━━━━━━━━━
ROOT PROBLEMS TO FIX
━━━━━━━━━━━━━━━━━━━━

Current backend issues:

❌ Promise.all() uncontrolled execution
❌ No worker ownership
❌ No centralized scheduler
❌ No backpressure
❌ No queue overflow protection
❌ No work stealing
❌ No concurrency governance
❌ No runtime-aware scheduling
❌ No task prioritization
❌ No telemetry-aware execution
❌ No saturation protection

━━━━━━━━━━━━━━━━━━━━
WORKER POOL REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

WorkerPool MUST support:

✅ configurable max workers
✅ async task execution
✅ worker leasing
✅ worker lifecycle tracking
✅ queue-based scheduling
✅ priority execution
✅ timeout protection
✅ retry policies
✅ cancellation
✅ graceful shutdown
✅ overload protection
✅ telemetry emission
✅ execution tracing
✅ deterministic execution IDs

━━━━━━━━━━━━━━━━━━━━
PRIORITY QUEUE REQUIREMENTS
━━━━━━━━━━━━━━━━━━━━

Implement heap-based priority queue.

Priority levels:

CRITICAL
HIGH
NORMAL
LOW

Task shape:

{
  id
  runId
  priority
  timeoutMs
  retries
  taskType
  executionMode
  metadata
}

Queue MUST support:

✅ enqueue
✅ dequeue
✅ peek
✅ reprioritize
✅ cancellation
✅ queue metrics
✅ overflow handling

━━━━━━━━━━━━━━━━━━━━
BACKPRESSURE CONTROL
━━━━━━━━━━━━━━━━━━━━

Implement:

BackpressureController

Features:

✅ queue size threshold
✅ saturation detection
✅ overload rejection
✅ adaptive concurrency
✅ execution throttling
✅ emergency slowdown
✅ retry cooldowns

NO unbounded execution allowed.

━━━━━━━━━━━━━━━━━━━━
EXECUTION LIMITER
━━━━━━━━━━━━━━━━━━━━

Implement:

ExecutionLimiter

Features:

✅ max concurrent executions
✅ per-run limits
✅ per-agent limits
✅ runtime-aware limits
✅ memory-aware limits
✅ token-aware limits

━━━━━━━━━━━━━━━━━━━━
TIMEOUT + RETRY
━━━━━━━━━━━━━━━━━━━━

Implement:

ExecutionTimeout
ExecutionRetry

Features:

✅ hard timeout
✅ soft timeout
✅ retry policies
✅ exponential backoff
✅ retry telemetry
✅ retry exhaustion handling

━━━━━━━━━━━━━━━━━━━━
WORKER TELEMETRY
━━━━━━━━━━━━━━━━━━━━

MANDATORY:

Emit telemetry events:

worker.created
worker.assigned
worker.started
worker.completed
worker.failed
worker.timeout
worker.retry
worker.cancelled
worker.overloaded
queue.saturated
queue.overflow
execution.rejected

Integrate with:

server/orchestration/telemetry/
server/infrastructure/events/

━━━━━━━━━━━━━━━━━━━━
GRAPH ENGINE INTEGRATION
━━━━━━━━━━━━━━━━━━━━

Integrate with:

server/engine/graph/

Replace:

❌ Promise.allSettled()

WITH:

✅ WorkerPool.submit()

Flow:

graph-engine
→ node-scheduler
→ worker-pool
→ execution-limiter
→ worker-slot
→ telemetry
→ completion

━━━━━━━━━━━━━━━━━━━━
TOOL LOOP INTEGRATION
━━━━━━━━━━━━━━━━━━━━

Integrate with:

server/agents/core/tool-loop/

Implement:

ParallelToolExecutor

Rules:

✅ read-only tools parallel-safe
✅ write tools serialized
✅ runtime mutation serialized
✅ memory writes protected
✅ execution traceable

━━━━━━━━━━━━━━━━━━━━
RUNTIME SAFETY
━━━━━━━━━━━━━━━━━━━━

Worker system MUST:

✅ prevent runtime starvation
✅ prevent event-loop blocking
✅ prevent queue explosion
✅ prevent infinite retries
✅ prevent orphan tasks
✅ prevent hidden failures

━━━━━━━━━━━━━━━━━━━━
REQUIRED ANALYSIS
━━━━━━━━━━━━━━━━━━━━

Before implementation:

Deep scan:

server/engine/
server/orchestration/
server/runtime/
server/tools/
server/agents/core/tool-loop/

Determine:

- current execution flow
- Promise.all usage
- concurrency risks
- task ownership gaps
- scheduling gaps
- retry gaps
- telemetry gaps

━━━━━━━━━━━━━━━━━━━━
REQUIRED FINAL REPORT
━━━━━━━━━━━━━━━━━━━━

Generate:

WORKER_POOL_IMPLEMENTATION_REPORT.md

Include:

1. Root Cause Analysis
2. Previous Execution Problems
3. Previous Concurrency Risks
4. Previous Promise.all Risks
5. Previous Queue Risks
6. New WorkerPool Architecture
7. New Scheduling Model
8. Queue Architecture
9. Backpressure Design
10. Retry Design
11. Timeout Design
12. Telemetry Architecture
13. Graph Integration Details
14. ToolLoop Integration Details
15. Runtime Protection Details
16. Overflow Protection
17. Saturation Protection
18. Concurrency Governance
19. Deterministic Ownership Model
20. Files Created
21. Files Modified
22. Imports Updated
23. Event Flows Added
24. Telemetry Events Added
25. Queue Metrics
26. Performance Impact
27. Scalability Improvements
28. Stability Improvements
29. Replit Similarity %
30. Remaining Weak Areas
31. Future Distributed Upgrade Path

━━━━━━━━━━━━━━━━━━━━
FINAL GOAL
━━━━━━━━━━━━━━━━━━━━

Transform NURA-X from:

“parallel-capable backend”

INTO:

“deterministic distributed autonomous execution infrastructure”

using:
- safe worker orchestration
- bounded concurrency
- intelligent scheduling
- production-grade runtime governance
- fail-closed execution principles