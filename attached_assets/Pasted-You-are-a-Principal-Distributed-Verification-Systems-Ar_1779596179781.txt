You are a Principal Distributed Verification Systems Architect.

Your task is to TRANSFORM the current sequential verification pipeline into a:

"99% Replit-Level Parallel Fail-Closed Autonomous Verification System"

using:
- wave-based parallel execution
- distributed verification
- fail-closed orchestration
- deterministic synchronization
- realtime telemetry
- safe runtime ownership
- parallel verification barriers

━━━━━━━━━━━━━━━━━━━━
MAIN GOAL
━━━━━━━━━━━━━━━━━━━━

Current Problem:
Verification pipeline is sequential:

STATIC
→ BUILD
→ RUNTIME
→ PREVIEW
→ RECONCILE

This creates:
❌ 3–5× latency
❌ orchestration blocking
❌ slow autonomous iteration
❌ delayed runtime recovery
❌ slow preview synchronization

Goal:
Transform into:

Wave A:
[STATIC + BUILD] → parallel

Barrier

Wave B:
[RUNTIME + PREVIEW] → parallel

Barrier

Wave C:
[RECONCILE] → deterministic sequential final gate

Target:
✅ 99% Parallel Verification Readiness
✅ Replit-Level Runtime Validation
✅ Production-Grade Fail-Closed Verification
✅ Autonomous Self-Healing Validation Pipeline

━━━━━━━━━━━━━━━━━━━━
MANDATORY TEAM STANDARDS
━━━━━━━━━━━━━━━━━━━━

# 1. HIGH COHESION / LOW COUPLING

Every module MUST:
✅ have single responsibility
✅ avoid tight dependencies
✅ avoid orchestration pollution
✅ avoid cross-domain imports
✅ expose clean interfaces only

Detect and remove:
❌ god objects
❌ giant coordinators
❌ mixed responsibilities
❌ runtime logic inside verification
❌ verification logic inside agents

━━━━━━━━━━━━━━━━━━━━
# 2. CODE SIZE CONSTRAINT

STRICT RULE:
❌ No file may exceed 250 lines

If oversized:
✅ split into bounded contexts
✅ create thin facades
✅ isolate responsibilities

━━━━━━━━━━━━━━━━━━━━
# 3. TARGETED LOCATION RULE

ALL new systems MUST be placed correctly:

Verification core:
server/fail-closed/

Parallel systems:
server/fail-closed/parallel/

Synchronization:
server/infrastructure/events/

Runtime verification:
server/runtime/verification/

Telemetry:
server/telemetry/

NO misplaced systems allowed.

━━━━━━━━━━━━━━━━━━━━
# 4. ROOT-CAUSE BUG FIXING

Do NOT patch symptoms.

Every verification failure MUST:
✅ classify root cause
✅ identify subsystem
✅ attach telemetry
✅ trigger recovery strategy
✅ store structured reflection

━━━━━━━━━━━━━━━━━━━━
# 5. HIGH-LEVEL ENGINEERING

ALL code MUST:
✅ be production-grade
✅ readable
✅ scalable
✅ deterministic
✅ replay-safe
✅ race-safe
✅ cancellation-safe
✅ timeout-safe

NO hacks.
NO shortcuts.
NO fake parallelism.

━━━━━━━━━━━━━━━━━━━━
# 6. CODE REVIEW MANDATORY

ALL new verification systems MUST:
✅ expose clean interfaces
✅ include validation contracts
✅ include telemetry
✅ include error surfaces
✅ include deterministic lifecycle

Generate:
- review checklist
- architecture validation notes
- risk analysis

━━━━━━━━━━━━━━━━━━━━
# 7. FAIL-SAFE DESIGN

Verification MUST remain:
✅ fail-closed
✅ deterministic
✅ replayable
✅ rollback-safe

Execution MUST NEVER continue if:
- build fails
- runtime fails
- preview fails
- reconcile fails

━━━━━━━━━━━━━━━━━━━━
# 8. CONSISTENT TELEMETRY

EVERY verification stage MUST emit:

✅ verification.started
✅ verification.completed
✅ verification.failed
✅ verification.retry
✅ verification.timeout
✅ verification.barrier.wait
✅ verification.wave.completed

Telemetry MUST include:
- runId
- phase
- verifier
- latency
- retryCount
- correlationId
- subsystem
- errorClass

━━━━━━━━━━━━━━━━━━━━
DEEP SCAN CURRENT SYSTEM
━━━━━━━━━━━━━━━━━━━━

Analyze:

server/fail-closed/
server/runtime/
server/orchestration/
server/preview/
server/infrastructure/events/
server/telemetry/

Inspect:
- verification coordinator
- verification stages
- runtime verification
- preview verification
- reconciliation logic
- EventBus integration
- SSE integration
- telemetry pipelines
- recovery hooks
- orchestration barriers

━━━━━━━━━━━━━━━━━━━━
TARGET ARCHITECTURE
━━━━━━━━━━━━━━━━━━━━

Implement:

VerificationCoordinator
    │
    ├── Wave A (PARALLEL)
    │      ├── StaticVerifier
    │      └── BuildVerifier
    │
    ├── SynchronizationBarrier
    │
    ├── Wave B (PARALLEL)
    │      ├── RuntimeVerifier
    │      └── PreviewVerifier
    │
    ├── SynchronizationBarrier
    │
    └── Wave C (SEQUENTIAL)
           └── ReconcileVerifier

━━━━━━━━━━━━━━━━━━━━
IMPLEMENT THESE SYSTEMS
━━━━━━━━━━━━━━━━━━━━

# 1. ParallelVerificationEngine

Create:
server/fail-closed/parallel/parallel-verification-engine.ts

Responsibilities:
✅ wave execution
✅ parallel orchestration
✅ synchronization barriers
✅ deterministic ordering
✅ timeout management
✅ cancellation propagation

━━━━━━━━━━━━━━━━━━━━
# 2. VerificationBarrier

Create:
server/fail-closed/parallel/verification-barrier.ts

Responsibilities:
✅ wait-all synchronization
✅ timeout protection
✅ failure propagation
✅ deterministic barrier release

━━━━━━━━━━━━━━━━━━━━
# 3. VerificationWaveRunner

Create:
server/fail-closed/parallel/verification-wave-runner.ts

Responsibilities:
✅ execute parallel verifier groups
✅ aggregate results
✅ attach telemetry
✅ manage retries

━━━━━━━━━━━━━━━━━━━━
# 4. VerificationTelemetry

Create:
server/telemetry/verification-telemetry.ts

Responsibilities:
✅ wave timing
✅ verifier timing
✅ retry metrics
✅ timeout metrics
✅ parallel throughput
✅ failure classification

━━━━━━━━━━━━━━━━━━━━
# 5. ParallelVerificationContracts

Create:
server/fail-closed/contracts/

Responsibilities:
✅ typed verification contracts
✅ verifier interfaces
✅ barrier interfaces
✅ result contracts

━━━━━━━━━━━━━━━━━━━━
# 6. VerificationRecoveryBridge

Create:
server/fail-closed/recovery/

Responsibilities:
✅ verification failure recovery
✅ runtime restart hooks
✅ preview recovery hooks
✅ reflection triggers

━━━━━━━━━━━━━━━━━━━━
VERIFY SAFE PARALLELIZATION
━━━━━━━━━━━━━━━━━━━━

Determine which stages are SAFE:

Wave A:
✅ STATIC
✅ BUILD

Reason:
No shared mutable runtime state.

Wave B:
✅ RUNTIME
✅ PREVIEW

Condition:
Runtime must pass startup barrier first.

Wave C:
❌ RECONCILE must remain sequential.

Reason:
Final deterministic consistency gate.

━━━━━━━━━━━━━━━━━━━━
IMPLEMENT SYNCHRONIZATION
━━━━━━━━━━━━━━━━━━━━

Use:
✅ EventBus
✅ DistributedSyncBarrier
✅ AbortSignal
✅ Promise.allSettled
✅ timeout guards
✅ cancellation propagation

Prevent:
❌ race conditions
❌ stale verification state
❌ runtime ownership conflicts
❌ preview desync
❌ orphaned verification promises

━━━━━━━━━━━━━━━━━━━━
IMPLEMENT FAIL-CLOSED GUARANTEES
━━━━━━━━━━━━━━━━━━━━

If ANY verifier fails:
✅ stop downstream waves
✅ emit telemetry
✅ trigger recovery
✅ write reflection memory
✅ rollback if necessary

NEVER:
❌ silently continue
❌ partially pass verification
❌ skip reconcile
❌ bypass failures

━━━━━━━━━━━━━━━━━━━━
IMPLEMENT REALTIME TELEMETRY
━━━━━━━━━━━━━━━━━━━━

Emit realtime events:

verification.wave.started
verification.wave.completed
verification.barrier.wait
verification.parallel.dispatch
verification.parallel.completed
verification.runtime.failed
verification.preview.failed

Frontend MUST receive:
✅ realtime verification progress
✅ wave visualization
✅ failure state
✅ retry state
✅ timing metrics

━━━━━━━━━━━━━━━━━━━━
IMPLEMENT PERFORMANCE OPTIMIZATION
━━━━━━━━━━━━━━━━━━━━

Optimize:
✅ verifier startup reuse
✅ shared cache
✅ incremental verification
✅ parallel runtime probing
✅ preview snapshot reuse
✅ batched telemetry flush

━━━━━━━━━━━━━━━━━━━━
GENERATE FINAL REPORT
━━━━━━━━━━━━━━━━━━━━

Create:

PARALLEL_VERIFICATION_ENGINE_REPORT.md

━━━━━━━━━━━━━━━━━━━━
REPORT MUST INCLUDE
━━━━━━━━━━━━━━━━━━━━

1. Old Sequential Verification Architecture
2. New Parallel Verification Architecture
3. Verification Lifecycle Graph
4. Verification Wave Graph
5. Barrier Synchronization Graph
6. Runtime Verification Flow
7. Preview Verification Flow
8. Reconcile Flow
9. Telemetry Event Graph
10. Recovery Integration Graph
11. Parallel Safety Analysis
12. Race Condition Prevention
13. Runtime Ownership Analysis
14. Verification Timeout Strategy
15. Retry Strategy
16. Fail-Closed Guarantees
17. Replay Safety Analysis
18. Cancellation Safety Analysis
19. Telemetry Coverage Analysis
20. Verification Throughput Analysis
21. Latency Reduction Analysis
22. Parallel Execution Gain %
23. Verification Readiness %
24. Replit-Level Verification Similarity %
25. Production Readiness %
26. Files Created
27. Files Refactored
28. Oversized File Splits
29. Remaining Bottlenecks
30. Safe Future Optimizations

━━━━━━━━━━━━━━━━━━━━
SUCCESS TARGET
━━━━━━━━━━━━━━━━━━━━

Target Final Metrics:

✅ Verification Readiness → 99%
✅ Parallel Verification → 99%
✅ Runtime Verification Stability → 99%
✅ Fail-Closed Reliability → 99%
✅ Replit-Level Verification Similarity → 99%
✅ Deterministic Verification → 100%
✅ Race Safety → 100%

━━━━━━━━━━━━━━━━━━━━
FINAL GOAL
━━━━━━━━━━━━━━━━━━━━

Transform the current sequential verifier into a:

"Distributed Parallel Autonomous Fail-Closed Verification Infrastructure"

capable of:
- parallel runtime validation
- parallel preview validation
- deterministic orchestration
- autonomous recovery
- realtime telemetry
- production-grade self-healing verification
- Replit-level autonomous runtime assurance