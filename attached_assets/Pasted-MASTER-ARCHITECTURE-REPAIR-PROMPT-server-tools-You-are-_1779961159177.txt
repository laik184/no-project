MASTER ARCHITECTURE REPAIR PROMPT — "server/tools/"

You are a world-class principal systems architect, autonomous tooling engineer, runtime infrastructure specialist, TypeScript platform refactoring expert, and production-grade execution systems engineer.

Your mission is to DEEPLY REPAIR, STABILIZE, and COMPLETE the architecture migration of ONLY:

server/tools/

This is NOT a feature task.

This is a CRITICAL TOOL EXECUTION ARCHITECTURE REPAIR OPERATION.

You must think like:

- Replit runtime infrastructure engineers
- Cursor execution architecture engineers
- production systems architects
- autonomous execution platform engineers
- secure runtime infrastructure teams

==================================================
CRITICAL EXECUTION RULE

FIRST:
PERFORM A COMPLETE DEEP SCAN.

ONLY AFTER SCAN:
start architecture repair.

DO NOT immediately rewrite code.

==================================================
STRICT SCOPE

ONLY TOUCH:

server/tools/

DO NOT MODIFY:

- server/agents/
- server/orchestration/
- frontend
- unrelated infrastructure

==================================================
DEEP SCAN REQUIREMENTS

Deeply inspect ALL files recursively inside:

server/tools/

INCLUDING:

- registry/
- core/
- shared/
- telemetry/
- filesystem/
- terminal/
- verifier/
- browser/
- coding/
- codegen/

Detect:

- hidden coupling
- Tool → Agent imports
- dispatcher bypasses
- duplicate execution paths
- shadow executors
- unsafe sandbox usage
- missing loader architecture
- invalid registry ownership
- unsafe type casts
- duplicated monitoring
- timeout fragmentation
- invalid exports
- raw execution bypasses
- circular dependency risks
- invalid observability
- security gaps
- dead abstractions
- migration leftovers

==================================================
MANDATORY FIXES

##################################################
FIX 1 — CREATE tool-loader.ts
##################################################

CREATE:

server/tools/registry/tool-loader.ts

Responsibilities:

- import ALL tool categories
- register ALL tools
- validate registration integrity
- call sealRegistry()

NOTHING ELSE.

FINAL FLOW:

main.ts
↓
loadAllTools()
↓
registerTool()
↓
sealRegistry()
↓
system ready

REMOVE:
tool registration logic from main.ts

==================================================

##################################################
FIX 2 — EDIT verifier/index.ts
##################################################

PROBLEM:
verifier/index.ts exports raw functions:

- runBuild()
- runTypeCheck()
- etc.

This creates:
DISPATCHER BYPASS.

Tools can execute without:

- metrics
- audit
- timeout
- retry
- permission validation

FIX:
REMOVE ALL raw execution exports.

KEEP ONLY:

- ToolDefinition exports
- registerVerifierTools()

ALL execution MUST go through:
tool-dispatcher.ts

FINAL STATE:
NO direct execution paths.

==================================================

##################################################
FIX 3 — CREATE shared/string-utils.ts
##################################################

CREATE:

server/tools/shared/string-utils.ts

MOVE:

- toPascalCase
- toKebabCase
- naming utilities

FROM:
server/agents/coderx/utils.ts

TO:
server/tools/shared/string-utils.ts

THEN:
update ALL coding tools to import from:
tools/shared/string-utils.ts

FINAL STATE:
NO Tool → Agent imports.

==================================================

##################################################
FIX 4 — EDIT browser/session/browser-lifecycle.ts
##################################################

PROBLEM:
browser tool imports from:
server/agents/browser/core/

This violates architecture direction.

WRONG:
Tool → Agent

CORRECT:
Agent → Tool

FIX:
MOVE:

- launchBrowserSession
- closeBrowserSession

INTO:
server/tools/browser/session/

Then:
agents/browser imports FROM tools layer.

FINAL STATE:
tools own runtime implementations.

==================================================

##################################################
FIX 5 — DELETE LATER core/execute-tool.ts
##################################################

FILE:
server/tools/core/execute-tool.ts

PROBLEM:
deprecated shadow executor.

Currently:
routes internally to dispatch()

BUT:
still creates:

- confusion
- migration leftovers
- duplicate entry points

FIX:

1. detect all imports
2. migrate all consumers to:
   tool-dispatcher.ts
3. AFTER migration complete:
   DELETE FILE

FINAL STATE:
ONE dispatcher exists.

==================================================

##################################################
FIX 6 — DELETE LATER core/tool-context.ts
##################################################

FILE:
server/tools/core/tool-context.ts

PROBLEM:
legacy shim with dangerous argument swap risk:

- projectId
- runId

This creates silent runtime bugs.

FIX:

1. migrate all callers to:
   shared/context-builder.ts
2. verify argument ordering
3. AFTER migration:
   DELETE FILE

FINAL STATE:
ONE context system exists.

==================================================

##################################################
FIX 7 — EDIT blocked-commands.ts
##################################################

FILE:
server/tools/terminal/security/blocked-commands.ts

PROBLEM:
regex misses:

- rm -r -f
- rm --recursive --force

Current protection incomplete.

FIX:
STOP validating raw command strings.

AFTER:
parse-shell-args.ts

validate parsed args array.

Detect:

- recursive delete flags
- dangerous force flags
- chained dangerous combinations

FINAL STATE:
production-grade command validation.

==================================================

##################################################
FIX 8 — EDIT coding/*
##################################################

PROBLEM:
coding tools still use:

- as unknown as ToolDefinition
- unsafe casts

FIX:
replace ALL unsafe tool definitions with:

- defineTool()
- defineCodingTool()

ALL tools MUST become:
strictly typed.

REMOVE:

- as any
- as unknown as

FINAL STATE:
schema-safe type inference.

==================================================

##################################################
FIX 9 — EDIT browser/*
##################################################

PROBLEM:
browser tools still contain:

- unsafe casts
- monitoring overlap
- fragmented monitoring ownership

FIX:

1. replace unsafe casts
2. consolidate monitoring ownership
3. centralize health tracking

TARGET:

- health-monitor.ts
- performance-monitor.ts

ONLY.

FINAL STATE:
clean monitoring architecture.

==================================================

##################################################
FIX 10 — EDIT telemetry/index.ts
##################################################

PROBLEM:
telemetry categorization uses:
tool.split("_")[0]

This creates:

- invalid categories
- unknown buckets
- broken observability

FIX:
use:
tool metadata
or explicit category field.

FINAL STATE:
correct telemetry categorization.

==================================================
CRITICAL ARCHITECTURE TARGET

FINAL EXECUTION FLOW:

Executor Agent
↓
dispatcher-client.ts
↓
tool-dispatcher.ts
↓
tool-resolver.ts
↓
tool-registry.ts
↓
Tool Handler
↓
Metrics + Audit
↓
Result

==================================================
MANDATORY SECURITY RULES

1. ALL tools MUST use:
   ctx.sandboxRoot

2. NEVER:
   
   - raw projectId path building
   - raw shell execution
   - dispatcher bypass
   - runtime registry mutation

3. Registry MUST remain sealed after boot.

==================================================
STRICT ENGINEERING RULES

1. DO NOT break runtime behavior
2. DO NOT remove compatibility immediately
3. Migrate incrementally
4. Preserve APIs during migration
5. No circular imports
6. No giant files (>250 lines)
7. High cohesion
8. Low coupling
9. Fail-closed security
10. Production-grade architecture only

==================================================
MANDATORY OUTPUT

Generate:

##################################################

1. DEEP SCAN REPORT
   ##################################################

Detect:

- architecture violations
- coupling
- security risks
- dispatcher bypasses
- invalid imports
- duplicate systems
- unsafe execution paths

==================================================

##################################################
2. FILE-BY-FILE REPAIR REPORT
##################################################

For EACH modified file:

- issue
- fix
- migration strategy
- compatibility strategy
- risk level

==================================================

##################################################
3. FINAL EXECUTION FLOW
##################################################

Show corrected architecture flow.

==================================================

##################################################
4. FINAL DIRECTORY STRUCTURE
##################################################

Generate corrected:
server/tools/

architecture.

==================================================

##################################################
5. SAFETY VALIDATION
##################################################

Verify:

- ONE dispatcher
- ONE registry
- NO Tool → Agent imports
- NO dispatcher bypass
- registry sealed
- secure sandboxing
- metrics working
- audit working
- strict typing

==================================================
SUCCESS CRITERIA

At the end:

✅ ONE dispatcher exists
✅ ONE registry exists
✅ tool-loader.ts exists
✅ centralized registration flow
✅ registry sealed after boot
✅ no Tool → Agent imports
✅ no verifier dispatcher bypass
✅ no unsafe tool casts
✅ centralized monitoring
✅ production-grade telemetry
✅ secure command parsing
✅ strict type safety
✅ deterministic execution pipeline
✅ production-grade architecture

==================================================
FINAL INSTRUCTION

Be brutally strict.

Detect:

- fake abstractions
- migration leftovers
- hidden execution paths
- shadow infrastructure
- security risks
- architectural violations

Act like:
a principal runtime systems engineer preparing this platform for real production deployment at scale.

Stabilize first.
Centralize second.
Simplify third.
Optimize last.