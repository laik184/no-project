MASTER PROMPT — EXECUTOR ↔ DISPATCHER WIRING

You are a senior TypeScript systems architect.

Your task is to perform STRICT execution wiring for the Executor Agent.

Goal:
Connect Executor Agent execution flow to the centralized tool execution pipeline using "dispatcher-client.ts".

This is ONLY a wiring + discipline task.

DO NOT redesign architecture.
DO NOT add abstractions.
DO NOT create extra systems.
DO NOT modify unrelated files.

---

TARGET ARCHITECTURE

Final execution flow MUST become:

executor-agent.ts
 ↓
execution-loop.ts
 ↓
step-runner.ts
 ↓
dispatcher-client.ts
 ↓
tool-dispatcher.ts
 ↓
tool-registry.ts
 ↓
tool

---

PRIMARY GOAL

Ensure:

✅ Agent → Dispatcher Client → Tool Dispatcher

❌ Agent → Tool Registry

❌ Agent → Tool Internals

❌ Agent → Tool Loader

❌ Agent → Primitive Execution

---

PHASE 1 — DEEP SCAN (MANDATORY)

Before modifying anything:

Deep scan these files:

server/agents/executor/executor-agent.ts
server/agents/executor/execution/execution-loop.ts
server/agents/executor/execution/step-runner.ts
server/agents/executor/coordination/dispatcher-client.ts

server/tools/registry/tool-dispatcher.ts
server/tools/registry/tool-registry.ts

Detect:

1. direct tool imports
2. dispatcher bypasses
3. primitive execution leakage
4. tool registry access from agents
5. duplicate execution paths
6. broken execution ownership
7. circular dependencies
8. invalid runtime flow

DO NOT modify anything during scan phase.

---

PHASE 2 — CREATE/FIX dispatcher-client.ts

Required file:

server/agents/executor/coordination/dispatcher-client.ts

This file becomes:

ONLY communication bridge

between executor agent and tool system.

---

dispatcher-client.ts RESPONSIBILITY

ONLY:

- import dispatcher
- call dispatcher
- normalize tool execution request
- return ToolExecutionResult

NEVER:

- execute tools directly
- access primitive APIs
- own workflows
- orchestrate retries
- orchestrate execution loops

---

REQUIRED IMPORT

import { dispatch }
from "@/server/tools/registry/tool-dispatcher";

---

REQUIRED API

executeTool(
  toolName,
  input,
  context
)

OR equivalent clean naming.

---

REQUIRED EXECUTION

return dispatch(
  toolName,
  input,
  context
);

---

PHASE 3 — FIX step-runner.ts

Required file:

server/agents/executor/execution/step-runner.ts

---

REQUIRED CHANGES

Replace ALL:

- direct tool execution
- direct registry access
- primitive execution
- tool imports

WITH:

import { executeTool }
from "../coordination/dispatcher-client";

---

REQUIRED FLOW

step-runner.ts
 ↓
executeTool()
 ↓
dispatcher-client.ts
 ↓
dispatch()
 ↓
tool-dispatcher.ts

---

PHASE 4 — EXECUTION OWNERSHIP VALIDATION

Enforce strict ownership boundaries.

---

AGENT RESPONSIBILITY

Allowed:

workflow execution
step coordination
task orchestration
runtime state
retry policy

Forbidden:

tool registry access
primitive execution
tool loading
tool registration

---

DISPATCHER RESPONSIBILITY

Allowed:

tool routing
timeout
retry wrapper
metrics
audit
permission validation

Forbidden:

agent activation
workflow orchestration
execution planning

---

TOOL RESPONSIBILITY

Allowed:

primitive execution

Forbidden:

workflow ownership
agent activation
dispatcher control

---

PHASE 5 — VALIDATION

After changes:

Verify:

✅ executor agent only talks to dispatcher-client.ts

✅ dispatcher-client.ts only talks to tool-dispatcher.ts

✅ no direct tool imports remain in executor/

✅ no direct registry access remains in executor/

✅ no primitive execution remains in executor/

✅ no circular dependencies introduced

✅ runtime flow matches target architecture

---

REQUIRED FINAL FLOW

executor-agent.ts
 ↓
execution-loop.ts
 ↓
step-runner.ts
 ↓
dispatcher-client.ts
 ↓
tool-dispatcher.ts
 ↓
tool-registry.ts
 ↓
tool

---

HARD RULES

FORBIDDEN:

executor → tool-registry
executor → tool-loader
executor → tool internals
executor → primitive APIs

ALLOWED:

executor → dispatcher-client
dispatcher-client → tool-dispatcher
tool-dispatcher → tool-registry
tool-registry → tool

---

REPORTING PHASE (MANDATORY)

Generate TWO reports.

---

REPORT 1 — BEFORE

Include:

- current execution flow
- direct imports found
- dispatcher bypasses
- ownership violations
- runtime execution graph

---

REPORT 2 — AFTER

Include:

- corrected execution flow
- dispatcher wiring status
- removed violations
- final dependency graph
- ownership validation
- runtime execution graph

---

FINAL TARGET

Executor system must become:

Executor Agent
 ↓
Dispatcher Client
 ↓
Centralized Tool Dispatcher
 ↓
Tool Registry
 ↓
Tool

STRICT layered architecture.