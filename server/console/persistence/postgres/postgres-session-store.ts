/**
 * server/console/persistence/postgres/postgres-session-store.ts
 *
 * Sessions are in-memory only (ephemeral by design).
 * This stub exists for architectural completeness.
 * The actual state lives in server/shared/console/session-state.ts.
 */

export const postgresSessionStore = {
  /** Sessions are ephemeral — no DB persistence required. */
};
