/**
 * server/console/persistence/postgres/postgres-runtime-store.ts
 *
 * Runtime state is in-memory only (ephemeral by design).
 * This stub exists for architectural completeness.
 * The actual state lives in server/shared/console/runtime-state.ts.
 */

export const postgresRuntimeStore = {
  /** Runtime state is ephemeral — no DB persistence required. */
};
