# Duplicate Endpoint Report

Generated by routing architecture fix scan.

---

## Duplicate #1 — Run Start

| Property | Details |
|----------|---------|
| **Conflict** | Two routes start a run by calling `chatOrchestrator.startRun()` |
| **Location A** | `POST /api/chat/run` → `server/chat/api/chat.routes.ts` → `chatController.startRun` |
| **Location B** | `POST /api/run` → `server/chat/api/run-start.router.ts` → inline handler |
| **Frontend Uses** | `POST /api/run` (confirmed in `client/src/api.ts`, `useAgentRunner.ts`) |
| **Recommended Source of Truth** | `POST /api/run` in `run-start.router.ts` |
| **Action** | Remove `POST /run` from `chat.routes.ts`; remove `chatController.startRun` |

---

## Duplicate #2 — Run Cancel

| Property | Details |
|----------|---------|
| **Conflict** | Two routes cancel a run by calling `chatOrchestrator.cancelRun()` |
| **Location A** | `POST /api/chat/runs/:runId/cancel` → `server/chat/api/run.routes.ts` → `runController.cancel` |
| **Location B** | `POST /api/run/:runId/cancel` → `server/chat/api/run-start.router.ts` → inline handler |
| **Frontend Uses** | `POST /api/run/:runId/cancel` (confirmed in `useAgentRunner.ts` line 68) |
| **Recommended Source of Truth** | `POST /api/run/:runId/cancel` in `run-start.router.ts` |
| **Action** | Remove `POST /:runId/cancel` from `run.routes.ts`; remove `runController.cancel` |

---

## Duplicate #3 — SSE Mounted at Root

| Property | Details |
|----------|---------|
| **Conflict** | SSE router built with absolute path `/api/chat/stream` mounted at root `/` |
| **Location** | `main.ts`: `app.use('/', chatOrchestrator.buildSseRouter())` |
| **Problem** | Every incoming request traverses this router; absolute path inside a router is non-standard and error-prone |
| **Recommended Source of Truth** | SSE route at relative path `/stream` added directly to `chatRouter` (mounted at `/api/chat`) |
| **Action** | Remove `buildSseRouter()` function; add SSE route to `chatRouter` at `/stream`; remove root mount from `main.ts` |

---

## Duplicate #4 — Heartbeat Double-Start

| Property | Details |
|----------|---------|
| **Conflict** | `heartbeatManager.start()` invoked twice |
| **Location A** | `server/chat/index.ts` line 26 — module-level side effect on import |
| **Location B** | `chatOrchestrator.startPersistence()` in `main.ts` — explicit controlled call |
| **Problem** | Module-level side effect runs before server is ready; second call is redundant (even if idempotent) |
| **Recommended Source of Truth** | Single call via `chatOrchestrator.startPersistence()` in `main.ts` |
| **Action** | Remove module-level `heartbeatManager.start()` from `server/chat/index.ts` |

---

## Duplicate #5 — Global Multer Middleware

| Property | Details |
|----------|---------|
| **Conflict** | `multer` applied globally to all routes via `app.use(upload.single('file'))` |
| **Location** | `main.ts` lines 30–31 |
| **Problem** | Parses multipart bodies on `GET /health`, `GET /api/projects`, all run routes, etc. — wasteful and risks interference |
| **Recommended Source of Truth** | Multer scoped only to `POST /api/chat/attachments/upload` |
| **Action** | Remove global mount from `main.ts`; create local multer instance in `attachment.routes.ts` |

---

## Gap #1 — Missing GET /api/run/active

| Property | Details |
|----------|---------|
| **Conflict** | Frontend calls `GET /api/run/active?projectId=N` (in `useRunRecovery.ts`) but no handler is registered |
| **Backend has** | `GET /api/chat/runs/active` via `runController.listActive` |
| **Frontend uses** | `GET /api/run/active` (404 currently) |
| **Action** | Add `GET /active` to `run-start.router.ts` delegating to `runController.listActive` |

---

## Gap #2 — Frontend Upload Path Mismatch

| Property | Details |
|----------|---------|
| **Conflict** | Frontend calls `POST /api/chat/upload` but backend only registers `POST /api/chat/attachments/upload` |
| **Frontend** | `client/src/components/chat/ChatInput.tsx` line 29 |
| **Backend** | `server/chat/api/attachment.routes.ts` — route registered under `/attachments` prefix |
| **Action** | Add `/upload` alias route to `chat.routes.ts` that applies multer and delegates to `attachmentController.upload` |
